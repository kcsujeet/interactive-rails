# Session Log: 2026-07-19

## Staff-review fix burndown, batch 1 (Act 6 L40-L47 + partial Acts 2/3/4)

**What changed**

Began fixing the P0-P3 backlog in `docs/staff-review-2026-07-18.md`. Fan-out fix agents were used; several dropped on usage/socket limits, but one Act 6 agent completed L40-L47 thoroughly before its socket dropped. This batch is the verified-green subset.

- **Act 6 L40-L47 (complete):**
  - L40: Rack ordering-semantics feedback corrected (append order runs BotDetector second, Logger last; a bot-rejected request is never logged); health-check reward uses `/up` with the early-return exemption; middleware placement reconciled between content and build.
  - L41: denied CORS preflight falls through to the app (404) instead of a fabricated 200.
  - L42: bot-flood reconciled with L40's BotDetector (legitimate-looking traffic the bot filter does not catch); answer leaks removed from the problem block and the throttle feedback.
  - L43: `add-column-default` probe now shows the volatile default locking (constant default is instant on PG11+); a real strong_migrations blocked scenario added; `[8.0]` migration version.
  - L44: Solid Queue "dashboard" claim fixed (Mission Control Jobs is separate); orphaned-records premise reframed to a legitimate maintenance task.
  - L45: PK/indexed-FK seq-scan frames corrected to the genuinely unindexed lookup; "Redis with AOF" archive tier removed (no Redis in the Solid stack).
  - L46: RecordNotFound-as-500 corrected to 404; reconciled onto the Rails 8 `Rails.error` API; undefined `context`/`ErrorReporter` fixed.
  - L47: observe premise rewritten to be honest about L40's JSON logger. Fixed a leak the agent left where the observe/early-build preview named "lograge" (the fix tool): observe now describes the symptom (timings trapped in interleaved prose), and the build's working-on-step-0 preview shows the before-state log instead of "lograge installed".
- **Act 2 L9/L10 (partial):** token column consistency, `email_address` throughout (dropped the `email:` rename suggestion), `before_action` feedback leak rewritten, JWT feedback softened, `expired-token` reframed as revoked session, `allow_unauthenticated_access only: [:create]` (no `:new` in API-only); L10 find_by-returns-nil (not raises), `find_each(&:encrypt)` backfill, honest normalizes framing, injectable SQLi payload, SSN downcase dropped.
- **Act 3 L15/L16:** casing promise removed from L15 trigger/step (the fix only strips); L16 background-job feedback corrected (no job backend yet is the honest reason; deliver_later is the real production pattern), transaction tradeoff comment added.
- **Act 4 L21 (partial):** N+1 detector fixes in progress.
- **lib:** registered three new legitimate cross-level references in the `CROSS_REFERENCES` pin map (L10 to L9 normalizes, L45 to L43 safe migrations, L46 to L44 recurring jobs), verified each reference is correct in context.

**Why**

Executing the "fix all" request against the staff review. Committing in verified-green batches so allocation drops do not lose work.

**Checks**

tsc clean, 1844 unit tests pass, 178-page build succeeds, biome clean on all 31 changed files.

**Remaining backlog**

Act 1 (L1-L8), Act 2 (L11-L14 + finish L9/L10), Act 3 (L18-L20), Act 4 (L22-L29 + finish L21), Act 5 (L30-L39), Act 6 (L48/L49/L50), Act 7 (L51-L58), cross-cutting docs (stale cumulative-patterns.md numbering, retired 8-act docs). Net-new content (debugging level, checkout level, boss levels, Ruby on-ramp, L18 full redesign) tracked separately; L18's broken teachings are being corrected in place.

**Learned**

- Fan-out fix agents are effective per-level but drop unpredictably on socket/allocation limits after long runs. Mitigation: keep batches verifiable and commit each green batch immediately; on resume, `git status` reveals exactly how far the dropped agent got (the Act 6 agent had finished L40-L47 and left L48-L50 untouched, cleanly).
- Agents can leave their own strict tests failing (L47 lograge leak). Always run the act's test suite after a dropped agent before trusting its output.

## Staff-review fix burndown, batch 2 (Act 5 L30-L39 + Act 6 L48-L50)

**What changed**

Three concurrent fix agents, all completed successfully.

- **Act 5 L30-L34:** L30 removed the redundant `add_index` (t.references polymorphic already indexes) and added a reviewable-type allowlist before `constantize`; L31 fixed the CRITICAL false discard default-scope claim (discard adds no default scope; `.kept` is explicit) and added a blocked scenario; L32 fixed the BoostPost/BoostProduct fossil and the ConnectionError-as-422 (now 500); L33 removed lock_version from the before-state and split shared reward frames so each scenario's numbers match its terminal; L34 fixed the presign-time vs attach-time validation animation, defined the validator methods, unified memory numbers, and corrected the active_storage:install output suffix.
- **Act 5 L35-L39:** L35 removed socialplatform.com fossils and pointed the reset link at the frontend page (not the API route helper); L36 removed the fabricated Solid Queue `unique :until_executed` macro (real: `limits_concurrency`/at-least-once + idempotency), fixed the install command to `bin/rails solid_queue:install` -> production.rb, and made the built job/service/worker-log consistent; L37 corrected the fabricated Solid Cable replay semantics (retention is only a trim cutoff; no client backlog) and switched auth to the session-based lookup; L38 replaced the removed Stoplight builder API with constructor kwargs across six surfaces, fixed perform_in -> set(wait:).perform_later, and ENV -> credentials; L39 removed `skip_before_action :verify_authenticity_token` (nonexistent in API-only), corrected the construct_event "verifies forever" claim (default 300s tolerance), and made the handler re-fetch amount from the API. Deleted the fossil Level36Encryption.test.ts; created real Level35/Level36 tests.
- **Act 6 L48-L50:** L48 made the RFC 9745 `Deprecation: @<unix-ts>` the correct answer (was the non-conformant `true` the level itself flagged as a mistake) and fixed every wrong-weekday HTTP-date; L49 replaced the false Kamal accessory-worker claim with a `job` role under servers: (accessories are not updated on deploy), reframed the build around Rails 8 already scaffolding Kamal at `rails new`, and fixed `kamal push` -> `kamal build push`; L50 replaced Devise's `authenticate` routing DSL with a Rails routing `constraints` object for the Flipper::UI mount and fixed the "30-minute redeploy" motivation to the honest contrast with L49's 2-second `kamal rollback`.
- **lib:** agents registered new legitimate cross-level references (L33->L32, L48/L49/L50 references) in the pin map.

**Checks**

tsc clean, 1907 unit tests pass, 178-page build, biome clean on all 48 changed files. No literal em dashes.

**Remaining backlog after batch 2**

Act 1 (L1-L8), Act 2 (L11-L14 + finish L9/L10), Act 3 (L18-L20), Act 4 (L22-L29 + finish L21), Act 7 (L51-L58), cross-cutting docs.

## Staff-review fix burndown, batch 3 partial (Act 1 L1-L7, Act 2 L11-L13, Act 7 L51-L54)

**What changed**

Three concurrent fix agents; all dropped on socket close after substantial work (99/71/91 tool uses), leaving partial-but-consistent state (verified: all level tests pass). Completed levels this batch:

- **Act 1 L1-L7:** L1 system-gem wording; L2 homework `--database=postgresql` + image_processing version; L3 false 255-char limit + generator answer leak; L4 bang-vs-non-bang content reconciled + decimal literals; L5 placeholder Review node removed + off-by-one preview fixed; L6 CRITICAL `namespace 'api'` (string is valid) + the L6/L7 controller-exists seam made honest (reward ends at dispatch, not fake 200s); L7 CRITICAL missing-controller 500 -> 404. L8 not reached.
- **Act 2 L11-L13:** L11 dead-code gating fixed so the final policy_scope/rescue_from code is reachable + placeholder "Policy (missing)" node removed + bearer-token (not cookie) inspector; L12 `uniqueness: true` (not case_sensitive:false over ciphertext) + restored cumulative model state; L13 step-0/step-1 de-duplicated + compound-frame actor is now the malicious owner. L14 not reached.
- **Act 7 L51-L54:** L51 replica answer leak + MVCC-not-lock-contention frames; L52 CRITICAL removed `connected_to(database:)` (removed in Rails 7.0) + RLS owner-bypass honesty + single tenant-resolution mechanism; L53 CRITICAL `ShardRecord.connected_to` (not Base) + native ShardSelector (not hand-rolled middleware, resolving the L51 contradiction); L54 Statesman no longer punished + update_all exploit honesty. L55-L58 not reached.
- **Baselines:** removed now-fixed files from `KNOWN_STRANDED_GUARDS` (L5, L6, L11) and `KNOWN_OBSERVE_PHASE_PLACEHOLDERS` (L11) in scripts/__tests__/level-reveal-consistency.test.ts; pinned new cross-references (L12->L10, L53->L51).

**Checks**

tsc clean, 1907 unit tests pass (bun run test), 178-page build, biome clean.

**Remaining after batch 3 partial**

Act 1 L8; Act 2 L14 (+finish L9/L10 verify); Act 3 L18-L20; Act 4 L21-L29; Act 7 L55-L58; cross-cutting docs (stale cumulative-patterns.md, retired 8-act docs).

## Staff-review fix burndown, batch 4 (Act 4 L21-L29, Act 3 L18-L20, Act 7 L55-L58)

**What changed**

Four concurrent fix agents, all completed successfully.

- **Act 4 L21-L25:** L21 Prosopite taught with real around_action activation + rescaled benchmark arithmetic (per-query times now sum to totals) + end-state made coherent (includes added so strict_loading does not raise); L22 the eager_load-vs-includes verdict unified across all six surfaces (includes is idiomatic; eager_load for explicit control); L23 memory numbers made internally consistent + in_batches/relation pluck + each_slice homework; L24 CRITICAL FK-index premise fixed (bare t.bigint so "no index" is true) + plausible consistent timings + disable_ddl_transaction!; L25 rescaled + attr_readonly/reset_counters honesty. L22-L24 triggers rewritten to incident voice.
- **Act 4 L26-L29:** L26 Pagy invalid-page consistency + RFC 8288; L27 CRITICAL pg_search now uses tsvector_column so the GIN index is real (weighted A/B ranking claim dropped) + genuine stemming example; L28 honest cache-hit line (MAX query runs) + race_condition_ttl scoped correctly + Rails 8 solid_cache reframe via --skip-solid + 224x arithmetic; L29 CRITICAL ETag mechanism split correctly (Rack::ETag bandwidth vs stale? compute short-circuit) + reward node-status bug + API-endpoint static-cache reframe.
- **Act 3 L18-L20:** L18 CRITICAL `params(A & B & C)` -> `params(A, B, C)` everywhere + initialize crash fixed against L16's real ApplicationService + layering paragraph + email_address + real Result shape; L19 unreachable final code fixed + SORTABLE_COLUMNS allowlist + e-commerce filters + schema drift + PORO consumer + honest .or/replica examples; L20 off-by-one + placeholder node removed + rescue_from StandardError added + unversioned homework + 1:1 discovery map. Created three new test files (L18/L19/L20 previously had none).
- **Act 7 L55-L58:** L55 CRITICAL reward reframed to an undeclared-dependency violation (what core Packwerk actually catches); L56 Wisper async feedback corrected + content switched from wisper-sidekiq to Solid Queue jobs + real grep command; L57 latency claim softened + route reconciled; L58 in-process-Wisper-needs-Kafka-backend honesty + swapped L52/L53 cross-refs fixed + instant-rollback semantics + de-duplicated quote.
- **Baselines/pins:** L20 and L29 removed from KNOWN_OBSERVE_PHASE_PLACEHOLDERS as fixed; new cross-references pinned (L21->Eager Loading, L25->The N+1 Problem, L18->Error Handling). L18/L19/L20 remain in KNOWN_STRANDED_GUARDS as valid (their reward phase passes STEP_DEFS.length, which the static heuristic reads as stranded against the 0-based render call; false positive, correctly baselined).

**Checks**

tsc clean, 1940 unit tests pass (bun run test), 178-page build, biome clean on all 44 changed files. No literal em dashes.

**Remaining after batch 4**

Act 1 L8; Act 2 L14 (+verify L9/L10); Act 7 nothing; cross-cutting docs (stale cumulative-patterns.md numbering, retired 8-act docs in content-structure.md / game-mechanics.md). These small remainders will be finished directly.

## Staff-review fix burndown, batch 5 FINAL (Act 1 L8, Act 2 L14, cross-cutting docs)

**What changed**

- **Act 1 L8 (direct):** fixed the backwards `bundle add` step description (the gem is NOT in the Gemfile yet; bundle add writes the entry AND installs). Removed the fabricated "100x faster than ActiveModelSerializers" claim (the jsonapi-serializer README makes no such multiplier claim; it points to a separate benchmarks repo) -> "benchmark much faster".
- **Act 2 L14 (agent):** CRITICAL RSpec `parallelize` corrected (that API is Minitest-only; RSpec uses parallel_tests); removed stale `spring rspec`; destroy-regression story unified onto the update action the step-6 spec actually tests; "404 (Pundit + scoped policy)" headline -> "403 (authorize)" matching L11; content factories rewritten to the real schema (email_address, no username, no status enum); EEEEEE -> FFFFFF (RSpec notation); fixtures-vs-factories contradiction reconciled. L9/L10 remainders verified ALREADY-DONE by batch 1.
- **Cross-cutting docs (direct):**
  - `cumulative-patterns.md` (the audit ground-truth; hardlinked across .agents and .claude): renumbered to the live 7-act registry. Fixed Act 1 L4/L5 swap; Act 3 header L16-L22 -> L15-L20 (added L15 Callbacks, removed the L21 Action Mailer / L22 Background Jobs entries that are actually Act 5); rewrote Act 4 (L21-L29) and Act 5 (L30-L39) with correct numbers, names, and current API facts (Prosopite around_action, includes-idiomatic, tsvector_column, Pagy current API, RFC 8288, discard no-default-scope, with_lock, presigned-local, generates_token_for, solid_queue:install/no-unique-macro, message_retention-is-trim-only, Stoplight kwargs, HMAC/re-fetch/at-least-once); fixed the infrastructure section numbers (Background Jobs L36+, Middleware L40+, Soft Deletes L31+, Safe Migrations L43+, Recurring Jobs L44+; added Error Monitoring L46+ and Observability L47+); replaced PostSearch/belongs_to :post fossils with Product; corrected the L18 `&`-composition claim to multi-arg params.
  - `content-structure.md` + `game-mechanics.md`: replaced the retired 8-act tree/table/breakdown (wrong act count, wrong level order, wrong names, act6-reliability, act8-mastery) with the live 7-act L1-L58 structure in all three places (the ASCII tree, the components-dir tree, the per-act breakdown, and the game-mechanics progression table). Replaced the drifted per-level pipeline-template inventory with a pointer to the level definitions as source of truth (re-deriving it would risk new wrong claims).

**Checks**

tsc clean, 1941 unit tests pass (bun run test), 178-page build, biome clean. No em dashes introduced (pre-existing ones in untouched cumulative-patterns.md case-study prose left as-is; internal skill doc, out of scope for this pass).

**Status: staff-review backlog COMPLETE.** All P0-P3 findings across Acts 1-7 fixed. Net-new content items from the review (a debugging level, a checkout/orders level, the unimplemented boss levels, a Ruby-language on-ramp) remain as a separate design backlog, not code fixes; L18's broken teachings were corrected in place rather than via the full structural redesign the review suggested.
