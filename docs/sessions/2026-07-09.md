# Session Log: 2026-07-09

## Session 1: Curriculum-wide teaching-quality audit (all 58 levels)

User judgment prompting this: "i don't think each level teaches the concepts in a good way" (manual audits fixed some levels; many remain). Ran a full-curriculum audit: 19 parallel read-only agents, 2-4 levels each, grading against a learner-perception rubric distilled from the audit-level skill. Full report: `docs/audits/2026-07-09-curriculum-teaching-audit.md`.

### Headline results

- Grades: 5 A (L14, L36, L38, L39, L49), 34 B, 16 C, 3 D (L29, L48, L55), 0 F.
- Perfect correlation between grade and pipeline: every A went through the modern design/audit pipeline; every D is untouched legacy.
- 10 P0 broken-teaching findings, worst: L48 is UNCOMPLETABLE (option-to-step mapping off-by-one; step 6 clicks dead), L29 teaches invalid `s-max-age` header, L33 ships broken Ruby as the correct answer, L41's visuals teach the classic CORS misconception its own probe debunks, L55 shows Packwerk as a runtime firewall, L57's microservice topology contradicts L58's never-extracted-billing premise.
- Systemic classes (fix by class): answer leaks in ~2/3 of levels (worst vector: Act 4's persistent right-panel API cheatsheets), reward dishonesty (~10 levels), factual Rails errors in legacy levels (~10), dead duplicate scenario buttons (5 levels), identical probe animations, placeholder future-concept nodes, e-commerce reframe residue, 2-option coin flips.

### Fixed immediately (own regressions from the 2026-07-05 swaps + one pre-existing)

- L52/L53 hardcoded `levelNumber` props were swapped with each other (each showed the other's number); missed by the swap because they're display props, not ids. Fixed.
- L49's three phase components showed `levelNumber={42}` (pre-existing fossil from old numbering). Fixed.
- tsc clean, 1701 tests pass.

### Lessons for the harness

- The render smoke e2e cannot catch the L48 class (level renders fine, cannot be completed). A full-playthrough e2e per level, or at least per interaction archetype, would.
- A per-level test helper asserting "every stress scenario id has a reward-frame entry" would kill the dead-button class.
- Homework/furtherReading flags from agents were false positives (they render post-completion only); noted in the report.

## Session 2: P0 fixes from the teaching audit (L48 + doc-verified factual batch)

### L48 API Versioning: uncompletable-level bug (P0)

TDD: wiring test written first (fails), then fixed. The click handler and render each held their own step->options literal, diverged by one (2-5 vs 3-6): wrong answers on steps 3-5 were silently swallowed, correct answers worked only because every array shares the id 'correct' (cross-array collision), and step 6 was dead. Now a single exported `OPTION_STEPS` map (question + options per step index) feeds both paths; step descriptions un-shifted (step 0 claimed both namespaces, step 1 described step 2, step 2 and 6 had none). New tests assert every step is exactly one of terminal/option, questions match their steps, one correct per step. Also fixed the test file's stale "Level 40" title.

### Factual-error batch (every claim verified against canonical docs first)

- **L29**: `s-max-age` -> `s-maxage` in 15 places (MDN Cache-Control).
- **L25**: ".count reads the column. Zero queries!" -> `.size` (Rails API: size/any? use counter caches internally; count always runs SQL); commonMistakes bullet no longer steers players to .count.
- **L43**: converted the probe + step 2 arc from `add_column :priority, default: 0` (instant on PG 11+, per strong_migrations README and the level's own content) to a volatile default (`gen_random_uuid()`), which genuinely rewrites the table; labels/discovery/test mirrors synced.
- **L45**: probe no longer shows a primary-key lookup seq-scanning 50M rows; now an unindexed order_number search, with the story pre-empting "just add an index" (4GB index for rows nobody reads). Fixed the "Level 45 added recurring jobs" self-reference (-> L44) and pinned it.
- **L51**: probe no longer claims SELECTs' shared locks block the INSERT (PostgreSQL MVCC: "reading never blocks writing"); reframed to connection-pool/CPU contention.
- **L54**: taught DSL now includes `no_direct_assignment: true` (AASM README); revert scenario raises NoDirectAssignmentError (was: InvalidTransition on a direct write, which AASM does not intercept); update_all scenario (unblockable at the model layer) replaced with `order.refund!` -> NoMethodError ("undefined states are unrepresentable").
- **L28**: dropped the false "Rails defaults to :null_store" (Rails 8 ships Solid Cache; :null_store is opt-in per the caching guide); the honest gap is that no code calls Rails.cache.
- **L40**: probe no longer claims X-Request-Id is missing (ActionDispatch::RequestId is default middleware, listed in the level's own stack); the honest gap is the id never appearing in log lines.
- **L47**: /up probe now returns 200-but-shallow (Rails generates the health route; verified in myapp routes.rb) instead of 404; HealthController options gain `allow_unauthenticated_access` (post-L9 ApplicationController requires auth); phantom Redis replaced with job queue.
- **L22**: filtered-scenario lanes un-inverted (includes auto-promotes to the same LEFT OUTER JOIN per the Rails querying guide; it was marked "suboptimal" while eager_load was "works", contradicting the build step); inspector "Required when filtering" corrected; whenToUse rewritten; `eager_load(c: :user)` typo; joins line no longer attributes the 101 queries to joins itself.
- **L33**: broken Ruby repaired in the CORRECT answer and all previews (`@quantity = amount` undefined local x5; contract `:amount`/decimal vs service `quantity:`; controller passed the params hash as a scalar; `result.order` on a Result defining :product); concurrent-checkout terminal aligned with the allowed frames (both commit serialized; rejection covered by the insufficient-stock scenario).

### Deferred to per-level deep passes (bigger than string surgery)

L41 CORS mechanism visuals, L55 Packwerk runtime-vs-CI reward redesign, L57/L58 world reconciliation, L28's now-redundant Solid Cache install steps, and the reward-dishonesty items on L8/L21/L34/L46/L52/L58.

### Checks

tsc clean, biome clean, build succeeds, bun test 1704 pass / 0 fail, e2e 59 pass / 0 fail.
