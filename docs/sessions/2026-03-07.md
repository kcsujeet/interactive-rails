# Session Log: 2026-03-07

## Session 1: Audit + Redesign L17 and L18 (Type 2 Static Intro)

Continuing the Act 3 refactoring level redesign pattern established with L16. Auditing each level against the updated audit-level skill and converting from Type 3 (interactive observe) to Type 2 (static intro).

### L17 Concerns: Audited + Redesigned

**Audit findings:**
- Wrong observe type: Type 3 (interactive discovery) for a code-structure problem
- Reward visual language mismatch: green boxes + FlowConnectors vs observe's red duplicated blocks
- StressTestPanel wrong (all 6 scenarios are "allowed", nothing to block)
- No reward loop closure on stated problems
- Missing `tag_list=` setter in build step 1

**Changes to** `frontend/src/features/act3-clean-architecture/components/Level17Concerns.tsx`:

Removed: `useDiscoveryGating`, `DiscoveryChecklist`, `ScenarioCards`, `StageInspector`, `StressTestPanel`, `useStressTest`, `ModelZone`, `ConcernLegend`, all flow animation state, `FlowConnector`.

Added:
- `POST_SECTIONS` and `COMMENT_SECTIONS` with `variant: 'core' | 'duplicated'`
- `AnnotatedCodeBlock` component with colored left borders
- Static intro showing side-by-side annotated code + callout
- Reward mirroring intro style (green borders) + concern zone + "Problems Solved" checklist
- `tag_list=` setter in build step 1 correct answer

Phase changed from `'observe'` to `'intro'`. File went from 1283 to ~530 lines.

### L18 Validation Contracts: Audited + Redesigned

**Audit findings:**
- Wrong observe type: Type 3 with discovery gating, ScenarioCards, StageInspector for a code-structure problem
- Reward visual language mismatch: horizontal Controller + FlowConnector + Contract layout vs observe's single controller zone with inline blocks
- StressTestPanel tests input validity (which the contract also does), but doesn't close the loop on stated structural problems
- No loop closure on intro's stated problems (one error per request, can't reuse, can't test cross-field rules)

**Changes to** `frontend/src/features/act3-clean-architecture/components/Level18ValidationContracts.tsx`:

Removed: `useDiscoveryGating`, `DiscoveryChecklist`, `ScenarioCards`, `StageInspector`, `StressTestPanel`, `useStressTest`, `FlowConnector`, `ContractLegend`, `VALIDATION_BLOCKS`, `DISCOVERY_DEFS`, `SCENARIOS`, `SCENARIO_DISCOVERY_MAP`, `OBSERVE_FLOW`, `BLOCK_INSPECTOR_MAP`, `BLOCK_DISCOVERY_MAP`, `STRESS_SCENARIOS`, `REWARD_FLOW`, `CONTRACT_SCHEMAS`, all flow animation state.

Added:
- `INTRO_SECTIONS` array with `variant: 'core' | 'scattered' | 'buried'` (three visual tiers: destructive for scattered checks, amber for buried cross-field rule, zinc for core logic)
- Static intro showing annotated controller code with callout
- Reward showing thin controller (green) + contract zone with 3 composed schemas (green) + cross-field rule (amber)
- "Problems Solved" checklist: all errors at once, reusable in any endpoint, cross-field rules testable

Phase changed from `'observe'` to `'intro'`. Build phase kept intact (4 steps: terminal + 3 OptionCard).

Build passes cleanly for both levels.

## Session 2: Audit L20 Error Handling (Request Lifecycle Level)

Audited L20 against the three-phase sequential flow standard. L20 is a **request lifecycle level** (not a refactoring level) because the problem is inconsistent error responses at runtime, which requires firing requests to discover.

### Audit Result: ALL PASS, No Changes Needed

| Check | Result |
|-------|--------|
| Phase 1 (Observe) | PASS: Interactive PipelineFlow with probes, discovery gating (4 defs, 3 required), StageInspector overlays |
| Phase 2 (Build) | PASS: 3 OptionCard steps covering strategy, status mapping, response shape. No gem needed. |
| Phase 3 (Reward) | PASS: Same PipelineFlow returns with StressTestPanel (6 scenarios, 2 allowed, 4 blocked) |
| Cross-phase consistency | PASS: Same visual language (PipelineFlow) in observe and reward |
| Correct answer positions | PASS: All correct answers at index 2 |
| Option colors | PASS: All violet |
| Feedback quality | PASS: No answers revealed |
| Code preview empty states | PASS: Every furthestStep value produces non-empty code |
| Content.ts sync | PASS: Component matches content.ts descriptions |
| Em dashes | PASS: None found |

Key observations:
- L20 correctly uses Type 4 (PipelineFlow) observe phase since error handling inconsistency is a runtime problem
- The linear pipeline layout (Request -> Router -> Controller -> Error Handler -> Response) is appropriate here since there are no satellite nodes
- StressTestPanel is appropriate since the fix changes how error responses are returned
- Reward loop closes well: observe shows three inconsistent error formats, reward lets player fire the same request types and see consistent JSON responses

## Session 3: Audit L22 Background Jobs

Audited Level 22 (Background Jobs) as a request lifecycle level (Type 4 PipelineFlow).

### Audit Result: PASS (all phases)

L22 is well-structured and follows the three-phase flow correctly:
- **Phase 1 (Observe):** Interactive PipelineFlow with 4 inspectable stages, 3 probes, discovery gating (3 of 4 required). Pipeline nodes react to probes (mailer shows dynamic sublabel/badge). PASS.
- **Phase 2 (Build):** 4 steps covering complete workflow (Solid Queue setup, job creation, idempotency, async switch). No empty code preview states. Correct answer never first. All options same color. Feedback never reveals answers. PASS.
- **Phase 3 (Reward):** PipelineFlow returns with Solid Queue node added. StressTestPanel with 5 scenarios (4 allowed, 1 blocked). Pipeline reacts dynamically. Left panel has legend + counters. PASS.
- **Cross-phase consistency:** Same visual language (PipelineFlow) in both observe and reward. Reward closes the loop on all observe problems. PASS.

### Minor Issues (not fixed, low severity)

1. No-op ternary at line 682: `(probeDisplay ? 'danger' : 'danger')` always evaluates to `'danger'`. Cosmetic.
2. Correct answer is always the last option (3rd of 3) in every build step. Predictable but not a rule violation.
3. Content.ts mentions "newsletter API" while component uses "external profile sync". Minor terminology drift.
4. Content.ts hint text references pipeline-builder interaction ("Add a Background Job node") not matching the interactive component.

No code changes needed.

## Session 4: Audit L21 Action Mailer

Audited Level 21 (Action Mailer) as a request lifecycle level (Type 4 PipelineFlow).

### Audit Result: PASS (all phases), one fix applied

| Check | Result |
|-------|--------|
| Phase 1 (Observe) | PASS: Interactive PipelineFlow with 5 inspectable stages, 3 probes, discovery gating (3 of 4 required) |
| Phase 2 (Build) | PASS: 4 steps covering complete workflow (generates_token_for, mailer generator, email method, controller) |
| Phase 3 (Reward) | PASS: PipelineFlow returns with Mailer + Response nodes added. StressTestPanel with 5 scenarios (3 allowed, 2 blocked) |
| Cross-phase consistency | PASS: Same visual language (PipelineFlow) in observe and reward |
| Code preview empty states | PASS: Every furthestStep value produces non-empty code |
| Content.ts sync | PASS: Component matches content.ts descriptions |
| Em dashes | PASS: None found |

### Issue Fixed: Correct answer always last (predictable pattern)

All 4 build steps had the correct answer as the last option (3rd of 3). Reordered options to vary positions:
- Step 0 (TOKEN_OPTIONS): correct moved to position 2 of 3
- Step 1 (MAILER_TERMINAL_COMMANDS): correct moved to position 2 of 3
- Step 2 (EMAIL_OPTIONS): kept at position 3 of 3
- Step 3 (CONTROLLER_OPTIONS): correct moved to position 2 of 3

**File changed:** `frontend/src/features/act3-clean-architecture/components/Level21ActionMailer.tsx`

### Minor Observations (not fixed, low severity)

1. `STAGE_DISCOVERY_MAP` maps clicking `router` to `no-mailer` discovery. Conceptually, the router stage is about missing routes, not missing mailers. However, it works functionally since the player can discover all 4 items.
2. Pipeline uses linear layout rather than hub-and-spoke. Acceptable here because the level adds Mailer and Response nodes in the reward phase that don't fit the hub-spoke model.

Build passes cleanly.

## Session 5: Redesign L19 Query Objects (Type 2 Static Intro)

Converted Level 19 from Type 3 (interactive observe with discovery gating, scenario cards, stage inspector, stress test) to Type 2 (static intro) following the same pattern as L16, L17, and L18.

### Changes to `frontend/src/features/act3-clean-architecture/components/Level19QueryObjects.tsx`:

**Removed:**
- `useDiscoveryGating`, `DiscoveryChecklist`, `DISCOVERY_DEFS`
- `ScenarioCards`, `SCENARIOS`, `SCENARIO_DISCOVERY_MAP`, `OBSERVE_FLOW`
- `StageInspector`, `STAGE_INSPECTOR_MAP`, `STAGE_DISCOVERY_MAP`
- `StressTestPanel`, `useStressTest`, `STRESS_SCENARIOS`, `REWARD_FLOW`
- `FlowConnector` import
- `QueryLegend` component
- All flow animation state (flowPhase, flowMessages, rewardFlowPhase, etc.)
- `useCallback`, `useRef` imports
- `CONSUMERS` interactive button zones

**Added:**
- `ADMIN_SECTIONS`, `API_SECTIONS`, `CSV_SECTIONS` with `variant: 'core' | 'duplicated'`
- `AnnotatedCodeBlock` component with destructive/success colored left borders
- Static intro showing 3-column grid of annotated consumer code + callout
- Reward mirroring intro layout: 3-column grid of clean consumers (green borders, `PostQuery.new...results` delegation) + PostQuery zone with filter/aggregate/ordering categories + "Problems Solved" checklist

**Kept unchanged:**
- All 3 build phase OptionCard steps (pattern, filter method, wiring)
- All option positions and feedback text
- `getCodeFiles` code preview logic (all furthestStep values produce non-empty code)

Phase changed from `'observe'` to `'intro'`. File went from ~1247 to ~615 lines. Build passes cleanly.

## Session 6: Deep Narrative Consistency Audit + Fixes

Performed a deep narrative consistency audit of Act 3, checking that every level's code examples reflect patterns established by earlier levels. This was prompted by the observation that L16 introduces service objects but L18's component still showed `RegistrationController` code.

### Skill Update: audit-act Step 7

Added "Check Cumulative Narrative Consistency" as Step 7 in `.claude/skills/audit-act/skill.md`. Includes:
- Detailed procedure for tracing cumulative patterns across levels
- Two case studies: L18 ignoring L16's service objects (BAD/GOOD), L20 with simple CRUD (acceptable)
- Checklist for code examples, "before" code, "after" code, component previews, and terminology
- New issue categories: "Narrative regression" and "Terminology drift"

### Findings and Fixes

**L18 Validation Contracts (narrative regression):**
- `content.ts`: Rewrote trigger, observation, rootCause, codeExample, and railsCodeExample to reference `RegistrationService` instead of `RegistrationController`
- `Level18ValidationContracts.tsx`: Updated INTRO_SECTIONS (controller-style `params[:email]` + `render json:` changed to service-style `@params[:email]` + `Result.new`), getCodeFiles intro phase (now shows `app/services/registration_service.rb`), left panel text, intro header, callout text, reward phase code display, option labels, and feedback messages

**L20 Error Handling (minor):**
- `content.ts`: Added comment in railsCodeExample acknowledging service objects for multi-step workflows

**L22 Background Jobs (terminology drift):**
- `content.ts`: Changed "newsletter API call" to "external profile sync" in trigger, observation, rootCause, codeExample comment, and test comment

Build passes cleanly.

## Session 7: Fix Act 3 Naming + Code Continuity

Fixed two cross-level consistency issues found by audit-act Steps 7a/7b:

### 1. Naming inconsistency: `RegistrationService` -> `UserRegistration`

L16 creates `UserRegistration` in `app/services/user_registration.rb`. L18 and L22 both incorrectly called it `RegistrationService` in `app/services/registration_service.rb`. Unified to `UserRegistration` everywhere.

**Files changed:**
- `content.ts`: L18 section (codeExample, railsCodeExample, controller example)
- `Level18ValidationContracts.tsx`: all code strings, filenames, intro subtitle
- `Level22BackgroundJobs.tsx`: all code strings, filenames, stage inspector, pipeline labels

### 2. Code continuity: Inline validation checks in L16

L18 shows `UserRegistration#call` with scattered inline validation checks as its "before" problem. But L16's `UserRegistration#call` had zero inline checks (just `user.save`). The checks appeared from nowhere.

**Fix:** Added inline validation checks (email, password, display_name) to L16's fat controller and carried them into the service.

**Files changed:**
- `content.ts`: L16 section (codeExample with checks in fat controller, railsCodeExample with checks + side effects in service)
- `Level16ServiceObjects.tsx`:
  - `ANNOTATED_SECTIONS`: added `checks` section (variant: 'core') with 3 inline checks
  - `getCodeFiles`: intro, step 0-4 code previews all include inline checks
  - Reward phase: "Inline Checks + Core Logic" badge in service annotation
  - Updated callout: "5 responsibilities" (was 4)

### Verification
- `grep -r "RegistrationService" frontend/src/features/act3-clean-architecture/` returns 0 results
- `bun run build` passes cleanly

## Act 3 Audit Summary

| Level | Type | Action | Result |
|-------|------|--------|--------|
| L16 Service Objects | Refactoring | Redesigned (session 2026-03-06) | Type 2 static intro |
| L17 Concerns | Refactoring | Redesigned | Type 2 static intro |
| L18 Validation Contracts | Refactoring | Redesigned | Type 2 static intro |
| L19 Query Objects | Refactoring | Redesigned | Type 2 static intro |
| L20 Error Handling | Request lifecycle | Audited, no changes | Type 4 PipelineFlow, all pass |
| L21 Action Mailer | Request lifecycle | Audited, minor fix | Type 4 PipelineFlow, reordered answer positions |
| L22 Background Jobs | Request lifecycle | Audited, no changes | Type 4 PipelineFlow, all pass |
