# Session Log: 2026-03-30

## Remove Activate Phase from L23, L24, L25

Removed the `activate` phase from Level 23 (N+1 Problem), Level 24 (Eager Loading), and Level 25 (Narrow Fetching) to match the golden standard three-phase flow (observe -> build -> reward).

### Changes per file
- Removed `'activate'` from the Phase type union
- Removed the `useEffect` that auto-transitioned from build to activate on `stepper.isComplete`
- Removed the activate handler functions (`handleActivateDetection`, `handleActivateReward`, `handleActivateSavings`)
- Removed the entire activate phase JSX block (star rating + "Visualize ___" button)
- Changed the build phase "Next Step" button: on the last step, it now calls `setPhase('reward')` + `stressTest.reset()` directly
- Updated left panel conditions from `phase === 'build' || phase === 'activate'` to just `phase === 'build'`
- Updated right panel condition in L24 from `phase === 'activate' || phase === 'reward'` to `phase === 'reward'`
- Removed unused `Star` and `Play` icon imports
- Updated file header comments to reflect the 3-phase flow

### Verification
- `tsc --noEmit`: zero errors in changed files
- `bun run build`: success
- `bun test`: all 1502 tests pass

---

## Act 7: Scale - Complete Redesign (All 6 Levels)

Designed and implemented all 6 levels in Act 7 (Levels 48-53) following the three-phase sequential flow standard (observe -> build -> reward). All existing implementations were replaced: the old levels lacked proper observe/reward phases, had too few build steps (L51 and L53 had only 1), and did not follow the golden standard patterns.

### Design decisions
- All 6 levels use Type 3 (custom visualization) since Act 7 concepts are architectural/spatial
- Each level has 4 probes (3 required) and 6 build steps (2 terminal + 4 option) for consistency
- Every visualization is unique: traffic routing, state diagrams, tenant tables, log viewers, dependency graphs, service topologies
- No activate phase: build flows directly to reward

---

## Level 48: Multi-Database - Full Rewrite

### What was done
- Rewrote from a single-screen sandbox (add replica buttons, toggle connects_to, run simulator) to full three-phase flow
- Custom FlowDiagram: 2 nodes in observe (Rails App, Primary DB with overload indicators), 3 in reward (add Read Replica)
- Animation frames show latency spikes, pool exhaustion, and routing decisions per probe/scenario
- 5 build steps: add replica entry, set replica:true, connects_to declaration, database_selector config, replication delay
- Reward: routing visualization (GET -> replica, POST -> primary), with delay behavior demonstration

## Level 49: State Machines - Full Rewrite

### What was done
- Rewrote from interactive SVG drawing approach to standard three-phase flow
- Custom state diagram: 5 circular nodes (pending, confirmed, shipped, delivered, cancelled) with transition edges
- Observe shows chaotic bidirectional red dashed arrows (any state can reach any state)
- Reward shows only valid forward transitions as solid green arrows with guard badges
- 6 build steps: add AASM, define states, define transitions, add guards, add PaperTrail, wire audit

## Level 51: Observability - Full Rewrite

### What was done
- Rewrote from a single-step flame graph to full three-phase flow
- Custom 4-node visualization: unstructured-log (active/red), structured-log (grayed), trace-timeline (grayed), health-endpoint (grayed)
- Observe probes show debugging failures: can't search logs, can't correlate errors, no tracing, no health check
- Reward: all 4 nodes turn green, scenarios demonstrate diagnostic capabilities
- 6 build steps: add Lograge, configure JSON formatter, add custom fields, add OpenTelemetry, configure tracing, add health endpoint

## Level 52: Modular Monolith - Full Rewrite

### What was done
- Rewrote Level 52 (Modular Monolith) from scratch following the exact three-phase sequential flow pattern from Level 48 (Multi-Database)
- Replaced the old 4-step OptionCard-only implementation with a full observe -> build -> reward flow

### Phase 1 (Observe)
- Custom 2x2 grid visualization using FlowDiagram with 4 PackageNode instances: Billing (top-left), Notifications (top-right), Orders (bottom-left), Inventory (bottom-right)
- 5 edges showing coupling: Billing->Notifications, Orders->Inventory, Inventory->Orders (circular), Billing->Orders, Notifications->Inventory
- Red dashed arrows show undeclared coupling in observe; green solid arrows in reward
- 4 probes (3 required): cross-domain coupling, circular dependency, no ownership, privacy violation
- Stage inspector on each domain node with code examples

### Phase 2 (Build - 6 steps)
- Step 0 (Terminal): `bundle add packwerk`
- Step 1 (Terminal): `bin/packwerk init`
- Step 2 (OptionCard): Configure enforce_dependencies + enforce_privacy
- Step 3 (OptionCard): Define public API in app/public/
- Step 4 (OptionCard): Declare explicit dependencies
- Step 5 (OptionCard): Set up CODEOWNERS by domain team

### Phase 3 (Reward)
- Same 2x2 grid with green borders, "Public API" labels, solid green arrows for valid deps
- 7 stress scenarios: 3 allowed (valid dep call, public API, CI check) + 4 blocked (private access, undeclared dep, circular, no privacy bypass)
- Allowed/Violations counters in left panel

### Checks
- TypeScript: zero errors
- Biome: formatted (1 file fixed)
- Build: successful
- Tests: 1502 pass, 0 fail

## Level 50: Multi-Tenancy - Full Rewrite

### What was done
- Rewrote Level 50 (Multi-Tenancy) from scratch following the exact three-phase sequential flow pattern from Level 48 (Multi-Database) and Level 49 (State Machines)
- Replaced the old strategy-picker/toggle-models implementation with a full observe -> build -> reward flow

### Phase 1 (Observe)
- Custom visualization using FlowDiagram with 3 nodes: Acme Corp (blue, top-left), Globex Inc (orange, top-right), and a database table node (center-bottom)
- Two edges: Acme -> Table (blue dots), Globex -> Table (orange dots)
- 4 probes (3 required): cross-tenant product read, cross-tenant order read, orphan record creation, direct ID access bypass
- Stage inspector on all 3 nodes with code examples showing unscoped queries

### Phase 2 (Build - 6 steps)
- Step 0 (Terminal): `bundle add acts_as_tenant`
- Step 1 (Terminal): `rails generate migration AddCompanyToProducts company:references`
- Step 2 (OptionCard): Set current tenant via subdomain with set_current_tenant_through_filter
- Step 3 (OptionCard): Add acts_as_tenant :company to Product model
- Step 4 (OptionCard): Add acts_as_tenant :company to Order model
- Step 5 (OptionCard): Add tenant-scoped unique index on [:company_id, :sku]

### Phase 3 (Reward)
- Same 3-node viz with green borders and "WHERE company_id = ?" filter indicator
- 7 stress scenarios: 4 allowed (list products/orders per tenant, create with auto-assign, same SKU across tenants) + 3 blocked (cross-tenant read, cross-tenant order, no-tenant-set)
- "Isolated" (green) / "Blocked" (red) counters in left panel

### Checks
- TypeScript: zero errors
- Biome: formatted (1 file fixed)
- Build: successful
- Tests: 1502 pass, 0 fail

## Level 53: Domain Events & Decoupling - Full Rewrite

### What was done
- Rewrote Level 53 (Domain Events) from scratch following the exact three-phase sequential flow pattern from Level 48 (Multi-Database)
- Replaced the old toggle-based event bus demo with a full observe -> build -> reward flow

### Phase 1 (Observe)
- Custom chain visualization using FlowDiagram: CheckoutService at top, sequential arrows down through EmailService -> InventoryService -> AnalyticsService -> ShippingService
- 4 chain edges showing direct method calls in waterfall order
- 4 probes (3 required): email-slow (sequential bottleneck), email-down (cascade failure), add-loyalty (tight coupling), partial-failure-retry (no isolation)
- Stage inspector on all 5 service nodes with code examples

### Phase 2 (Build - 6 steps)
- Step 0 (Terminal): `bundle add wisper`
- Step 1 (OptionCard): Define OrderCompleted event class with Wisper::Publisher
- Step 2 (OptionCard): Replace direct calls in CheckoutService with broadcast
- Step 3 (OptionCard): Subscribe EmailService as listener (EmailListener + Wisper.subscribe)
- Step 4 (OptionCard): Subscribe remaining services (Inventory, Analytics, Shipping listeners)
- Step 5 (OptionCard): Add async processing (delegate to background jobs via Solid Queue)

### Phase 3 (Reward)
- Fan-out visualization: CheckoutService -> Event Bus (green) -> 4 service nodes horizontally
- 7 stress scenarios (all "allowed" since events always succeed at publish): normal checkout, email slow, email down, analytics fail, add loyalty, retry email, cascade attempt
- "Processed" (green) / "Retrying" (amber) counters in left panel
- Animation shows parallel processing, independent failures, granular retries

### Checks
- TypeScript: zero errors
- Biome: formatted (1 file fixed)
- Build: successful
- Tests: 1502 pass, 0 fail

## Level 48: Multi-Database - Audit Fix (Probe-to-Scenario ID Mismatch)

### What was done
- Fixed probe-to-scenario ID mismatch: observe probes `peak-traffic` and `check-pool` had no matching reward stress scenario IDs
- Added two new stress scenarios: `peak-traffic` (100 concurrent reads routed to replica, 0 timeouts) and `check-pool` (check pool stats, healthy distribution)
- Added `REWARD_POOL_FRAMES` animation for `check-pool` showing healthy pool distribution across primary (5/25 busy, writes only) and replica (18/25 busy, reads here)
- Added both new scenario IDs to `REWARD_PROBE_FRAMES` map (`peak-traffic` reuses `REWARD_PEAK_FRAMES`, `check-pool` uses new `REWARD_POOL_FRAMES`)
- All checks pass: zero TS errors, biome clean, build successful

## Level 50: Multi-Tenancy - Audit Fix: Cumulative Code Patterns

### What was done
- Updated observe phase code preview to show cumulative patterns from earlier levels:
  - Controller now delegates to `Products::ListService.call(params:)` (Service Objects from L16+)
  - Added service file showing the unscoped `Product.all` query
  - Controller renders via `ProductSerializer` (Serializers from L7+)
- Updated build phase code preview (step 3+) to show:
  - Service file with comments explaining acts_as_tenant auto-scopes `Product.all`
  - Controller file unchanged, demonstrating that tenant scoping is transparent to service/controller layer
- All checks pass: zero TS errors, biome clean, build successful

## Level 51: Observability - Audit Fixes

### What was done
- **CRITICAL: Probe-to-scenario ID mismatch** - Renamed stress scenario IDs `trace-bottleneck` to `find-bottleneck` and `health-check` to `check-health` to match probe IDs. Updated `REWARD_PROBE_FRAMES` keys to match.
- **HIGH: Solution leakage in left panel** - Rewrote scenario text to describe only the problem (no mention of Lograge, OpenTelemetry, or health check endpoint). Now describes what is broken and what the team cannot do.
- **HIGH: L41 contradiction** - Acknowledged L41's RequestLogger middleware in scenario text ("Your RequestLogger middleware from L41 logs basic request/response data, but it is not enough"). Updated observe code preview to show the existing RequestLogger as the "before" state with comments noting its limitations.
- **MEDIUM: Missing service object in health endpoint** - Updated health endpoint option and code preview to delegate to `HealthCheckService.call` instead of inline DB/Redis/disk checks.
- **LOW: LogNode badge always bg-destructive** - Made badge color conditional: green flash uses `bg-success/20 text-success`, red flash uses `bg-destructive/20 text-destructive`, default uses `bg-muted text-muted-foreground`.
- **LOW: No stage-discovery for no-correlation** - Added `'structured-log': 'no-correlation'` to `STAGE_DISCOVERY_MAP`.
- All checks pass: zero TS errors, biome clean, build successful, 1502 tests pass

## Level 49: State Machines - Audit Fixes

### What was done
- **HIGH: PaperTrail already installed at L43** - Replaced step 4 (`bundle add paper_trail`) with `rails generate paper_trail:install && rails db:migrate`. Updated STEP_DEFS title to "Set Up PaperTrail", terminal commands (wrong options: `rails generate migration CreateVersions` and `rails generate model Version`), output lines showing migration creation, and SHELL_STEP_MAP. Updated Gemfile code preview to always show `gem "paper_trail"` with a comment noting it was installed in L43.
- **HIGH: Code preview missing Service Objects (L16+)** - Added service object file to observe phase showing `Orders::ConfirmService` with direct `order.update!(status: "confirmed")` (the broken "before" state). Added updated service file from step 2 onward showing `order.confirm!` with `AASM::InvalidTransition` rescue (the "after" state using state machine events).
- **MEDIUM: Probes `set-invalid-status` and `check-audit` have no matching reward scenarios** - Added two new stress scenarios: `set-invalid-status` (blocked, AASM rejects arbitrary status strings) and `check-audit` (allowed, PaperTrail shows version history). Added matching `REWARD_BLOCKED_INVALID_STATUS_FRAMES` and `REWARD_AUDIT_TRAIL_FRAMES` animation arrays, wired into `REWARD_PROBE_FRAMES`.
- **LOW: `text-[10px]` arbitrary Tailwind value** - Replaced all `text-[10px]` with `text-xs` in StateNode badge and TransitionEdge label.
- All checks pass: zero TS errors, biome clean, build successful, 1502 tests pass

## Level 52: Modular Monolith - Audit Fixes

### What was done
- **HIGH: StressTestPanel missing `disabled={vizAnimating}`** - Added `disabled={vizAnimating}` prop to prevent firing scenarios during animation playback.
- **HIGH: InvoiceSender lacks ApplicationService inheritance (L16+)** - Updated observe code preview to show `class Billing::InvoiceSender < ApplicationService` with `Result = Data.define(:invoice, :success, :error)` pattern.
- **LOW: Edge label `text-[10px]`** - Replaced with `text-xs`.
- All checks pass: zero TS errors, biome clean, build successful.

## Level 53: Domain Events - Audit Fixes

### What was done
- **HIGH: StressTestPanel missing `disabled={vizAnimating}`** - Added `disabled={vizAnimating}` prop.
- **HIGH: CheckoutService lacks ApplicationService inheritance (L16+)** - Updated ALL code previews (observe, stage inspector, build step 2 options, build code preview) to show `class CheckoutService < ApplicationService`. Added `Result = Data.define(:order, :success, :error)` in observe code.
- **MEDIUM: Probe `partial-failure-retry` ID mismatch** - Renamed stress scenario ID from `retry-email` to `partial-failure-retry` and updated the matching key in `REWARD_PROBE_FRAMES`.
- **MEDIUM: Badge styling always bg-destructive in reward** - Made badge color conditional on flash state in both CheckoutNode and ServiceNode: green -> `bg-success/20 text-success`, red -> `bg-destructive/20 text-destructive`, amber -> `bg-warning/20 text-warning`, default -> `bg-muted text-muted-foreground`.
- **LOW: Edge label `text-[10px]`** - Replaced with `text-xs`.
- All checks pass: zero TS errors, biome clean, build successful.

---

## Act 8: Mastery - Complete Design and Implementation (Levels 54-56)

Designed and implemented all 3 levels in Act 8 following the three-phase sequential flow standard. All existing implementations were replaced. Each level has a unique visualization, 4 probes with discovery gating, 6-7 build steps, and stress test reward phase.

### Design decisions
- L54 (API Gateway): Client -> services fan-out visualization, showing 6 sequential calls collapsing to 1 aggregated request
- L55 (Sharding): 3 tenants converging on 1 DB transforms to 3 tenants routed through ShardResolver to 3 shard DBs
- L56 (Capstone): Tangled monolith star topology transforms to: Gateway -> Billing Service -> Event Bus -> async subscribers
- All cumulative patterns honored: ApplicationService, Faraday/Stoplight, acts_as_tenant, connects_to, AASM, Wisper, OpenTelemetry

## Level 54: API Gateway - Full Rewrite

### What was done
- Custom FlowDiagram: Mobile Client + 3 service groups (Users/Orders, Inventory/Notifications, Billing/Analytics)
- Observe: 6 sequential calls with different auth schemes, probes show latency compounding, auth chaos, cascade failure, topology leak
- Build (6 steps): Generate gateway controller, edge authentication (JWT), request routing (Faraday), response aggregation (parallel fetch), circuit breakers (Stoplight), rate limiting
- Reward: Client -> Gateway -> parallel fan-out. 7 scenarios (5 allowed, 2 blocked)
- Cumulative: ApplicationService, /api/v1/ namespace, Faraday + Stoplight, Rails 8 rate_limit

## Level 55: Database Sharding - Full Rewrite

### What was done
- Rewrote Level 55 (Database Sharding) from scratch following the exact three-phase sequential flow pattern from Level 48 (Multi-Database)
- Replaced the old single-screen sandbox (enable sharding button, hot shard rebalancing, load bars) with a full observe -> build -> reward flow

### Phase 1 (Observe)
- Custom FlowDiagram: 3 tenant nodes (Acme Corp, Globex Inc, Initech) at top, 1 overloaded Primary DB at bottom
- 3 edges converging all tenants onto single DB, showing write contention visually
- 4 probes (3 required): write-latency (INSERT 214ms from lock contention), index-rebuild (4h+ on 2B rows), backup-fails (pg_dump out of disk after 6h), vacuum-behind (84M dead tuples, TX wraparound risk)
- Stage inspector on all 4 nodes with descriptions and code example on Primary DB

### Phase 2 (Build - 6 steps)
- Step 0 (Terminal): Add shard_one, shard_two, shard_three to database.yml
- Step 1 (OptionCard): Create ShardRecord abstract class with connects_to shards
- Step 2 (OptionCard): Configure shard key selection (company_id % 3)
- Step 3 (OptionCard): Build ShardResolver middleware reading from ActsAsTenant context
- Step 4 (OptionCard): Move Order to inherit from ShardRecord
- Step 5 (OptionCard): Add cross-shard aggregation service (RevenueReport iterates all shards)

### Phase 3 (Reward)
- Topology changes: 3 tenants -> ShardResolver middleware -> 3 shard DB nodes
- 7 stress scenarios: 6 allowed (tenant writes to correct shard at 5ms, cross-shard aggregation, write latency check, migration on all shards) + 1 blocked (query wrong shard returns empty)
- Animation frames show per-tenant routing, cross-shard fan-out, and shard key mismatch
- Routed/Blocked counters in left panel with sharding legend

### Cumulative patterns
- ShardResolver uses ActsAsTenant.current_tenant (L50+)
- connects_to shards extends multi-database pattern (L48+)
- RevenueReport inherits from ApplicationService (L16+)

### Checks
- TypeScript: zero errors
- Biome: formatted (1 file fixed)
- Build: successful

## Level 56: The Architect (Capstone) - Full Rewrite

### What was done
- Replaced old toggle-components sandbox with full three-phase flow
- Observe: Star topology (Order#charge! -> Stripe, Email, Inventory, Analytics, Loyalty). Probes show: sync blocking 4.5s, no state guards (double charge), deploy coupling (2hr deploy), cascade failure (email kills payment)
- Build (7 steps): Packwerk init, AASM state machine, Wisper domain events, billing DB (connects_to), API gateway proxy, OpenTelemetry observability, Flipper feature flags
- Reward: Extracted architecture: Gateway -> Billing Service (AASM + own DB) -> Event Bus -> 4 async subscribers. 8 scenarios (5 allowed, 3 blocked)
- Cumulative: ApplicationService, AASM (L49), Wisper (L53), connects_to (L48), OpenTelemetry (L51), acts_as_tenant (L50), Packwerk (L52)

### Checks
- TypeScript: zero errors
- Biome: clean
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Level 55: Database Sharding - Audit Fixes

### What was done
- Added 3 missing stress scenarios (`index-rebuild`, `backup-fails`, `vacuum-behind`) to STRESS_SCENARIOS so every observe probe has a matching reward scenario
- Added corresponding REWARD_PROBE_FRAMES for each: index rebuild showing 15min per shard vs 4h monolith, backup showing 200GB success vs 2TB failure, vacuum showing autovacuum keeping up on smaller tables
- Moved correct answer in RESOLVER_OPTIONS from position 0 to position 1 (convention: correct answer must never be first in raw array)
- Replaced `text-[10px]` with `text-xs` (no arbitrary Tailwind values)

### Checks
- TypeScript: zero errors (grep Level55 returned nothing)
- Biome: clean, no fixes needed
- Build: successful

---

## Level 56: The Architect (Capstone) - Audit Fixes

### What was done
- Fixed code preview sequencing bug: Payment model was showing `class Payment < BillingRecord` at completedStep=2 (Wisper step done), but BillingRecord class definition only appears at completedStep>=3 (billing database step). Now uses three-way split: step 1 shows AASM only, step 2 adds Wisper::Publisher (still ApplicationRecord), step 3+ switches to BillingRecord.
- Replaced `text-[10px]` with `text-xs` (arbitrary value violation).
- Added Solid Queue reference in Wisper subscriber code preview: shows `ReceiptSubscriber` calling `SendReceiptJob.perform_later` to connect domain events back to Solid Queue (L22+).

### Checks
- TypeScript: zero errors (grep for Level56 found nothing)
- Biome: clean, no fixes applied
- Build: successful

---

## Level 54: API Gateway - Audit Fixes

### What was done
- **Probe-to-scenario ID mismatch (HIGH):** Added two new stress scenarios (`auth-inconsistency` and `topology-exposed`) with matching REWARD_PROBE_FRAMES. `auth-inconsistency` shows unified JWT at gateway edge (allowed). `topology-exposed` shows direct access rejected, internal hosts hidden (blocked). Kept existing `auth-at-edge` and `direct-access` scenarios as reward-only.
- **Arbitrary value `text-[10px]` (HIGH):** Replaced with `text-xs` in edge label renderer.
- **STAGE_DISCOVERY_MAP sparse (MEDIUM):** Added `client` -> `topology-leak` and `svcC` -> `auth-chaos` mappings so clicking those nodes triggers discoveries.
- **ServiceNode badge color always destructive (LOW):** Made badge color conditional on flash state: green -> success, red -> destructive, default -> muted.

### Checks
- TypeScript: zero errors
- Biome: clean
- Build: successful

---

## Levels 29, 30, 31: Remove Activate Phase

### What was done
Removed the `activate` phase from Level 29 (Search), Level 30 (Caching), and Level 31 (HTTP Caching). The golden standard says there should be no activate phase: the build phase's last step transitions directly to the reward phase.

For each file:
- Changed Phase type from `'observe' | 'build' | 'activate' | 'reward'` to `'observe' | 'build' | 'reward'`
- Removed the `useEffect` that auto-advanced from build to activate when `stepper.isComplete`
- Removed the `handleActivate*` function, replaced with `handleStartReward`
- Changed the OptionCard "Next Step" button to show on the last step too, with `onClick={hasNextStep ? stepper.nextStep : handleStartReward}`
- Removed the entire activate phase JSX block (star rating + "Visualize ___" button)
- Removed unused `Star` and `Play` icon imports (and `useEffect` from Level 31)
- Updated comments throughout (phase flow, code preview sections, step progress conditions)

### Checks
- TypeScript: zero errors (grep Level29/30/31 returned nothing)
- Biome: clean (3 files formatted)
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Levels 23, 24, 25: Remove Activate Phase

### What was done
Removed the `activate` phase from Level 23 (N+1 Problem), Level 24 (Eager Loading), and Level 25 (Narrow Fetching).

For each file:
- Changed Phase type to `'observe' | 'build' | 'reward'`
- Removed useEffect auto-advancing build -> activate
- Removed activate handler and JSX block (star rating + "Visualize" button)
- Changed last step "Next Step" to call setPhase('reward') + stressTest.reset()
- Removed unused Star/Play imports, updated comments

### Checks
- TypeScript: zero errors
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Levels 26, 27, 28: Remove Activate Phase

### What was done
Removed the `activate` phase from Level 26 (Indexing), Level 27 (Counter Caches), and Level 28 (Pagination). The golden standard says there should be no activate phase: the build phase's last step transitions directly to the reward phase.

For each file:
- Changed Phase type from `'observe' | 'build' | 'activate' | 'reward'` to `'observe' | 'build' | 'reward'`
- Removed the `useEffect` that auto-advanced from build to activate when `stepper.isComplete`
- Renamed `handleActivateIndexes`/`handleActivateReward`/`handleActivatePagination` to `handleStartReward`
- Changed the build phase's last step "Next Step" button to show on all completed steps (not just when `hasNextStep`), with `onClick={hasNextStep ? stepper.nextStep : handleStartReward}`
- For L26 step 4 (terminal type, last step): set `hasNext` to always true and `onNext` to `handleStartReward`
- Removed the entire activate phase JSX block (star rating + "Visualize ___" button)
- Removed unused `Star` and `Play` icon imports
- Updated left panel step progress condition from `phase === 'build' || phase === 'activate'` to `phase === 'build'`
- Updated file header comments to reflect the 3-phase flow

### Checks
- TypeScript: zero errors (grep Level26/27/28 returned nothing)
- Biome: clean (3 files formatted)
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Level 26: Audit Fix (toggleAutoFire bug)

### What was done
- Fixed `onToggleAutoFire={stressTest.toggleAutoFire}` to `onToggleAutoFire={() => stressTest.toggleAutoFire(handleFireScenario)}`. Without the wrapper, auto-fire would not receive the fire handler callback and would silently fail.

---

## Levels 34, 35: Remove Activate Phase

### What was done
Removed the `activate` phase from Level 34 (Locking) and Level 35 (Active Storage).

For each file:
- Changed Phase type from `'observe' | 'build' | 'activate' | 'reward'` to `'observe' | 'build' | 'reward'`
- Removed the `useEffect` that auto-advanced from build to activate when `stepper.isComplete`
- Renamed `handleActivateReward` to `handleStartReward` (L34) / created `handleStartReward` (L35), both call `stressTest.reset()`
- Changed the build phase's last step "Next Step" button to show on all completed steps, with `onClick={hasNextStep ? stepper.nextStep : handleStartReward}`
- Removed the entire activate phase JSX block (star rating + "Visualize ___" button)
- Removed unused `Star` and `Play` icon imports
- Updated left panel step progress condition from `phase === 'build' || phase === 'activate'` to `phase === 'build'` (L34)
- Removed activate left panel block (L35)
- Updated file header comments to reflect the 3-phase flow
- Fixed pre-existing bug in L34: added missing `discoveredCount` prop to `DiscoveryChecklist`

### Checks
- TypeScript: zero errors (grep Level3[45] returned nothing)
- Biome: clean (2 files formatted)
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Levels 32, 33: Remove Activate Phase

### What was done
Removed the `activate` phase from Level 32 (Polymorphic Associations) and Level 33 (Transactions).

For each file:
- Changed Phase type to `'intro' | 'build' | 'reward'` (L32) or `'observe' | 'build' | 'reward'` (L33)
- Removed the `useEffect` that auto-advanced from build to activate when `stepper.isComplete`
- Removed the activate handler function (`handleActivateReward`)
- Changed the build phase's last step "Next Step" button to show on all completed steps, with `onClick={hasNextStep ? stepper.nextStep : () => setPhase('reward')}` (L32) or `handleStartReward` (L33)
- Removed the entire activate phase JSX block (star rating + "Visualize ___" button)
- Removed unused `Star` and `Play` icon imports; removed unused `useEffect` import from L32
- Updated left panel step progress condition from `phase === 'build' || phase === 'activate'` to `phase === 'build'`
- Updated file header comments to reflect the 3-phase flow
- Fixed pre-existing TS error in L33: added missing `discoveredCount` prop to `DiscoveryChecklist`

### Checks
- TypeScript: zero errors (grep Level3[23] returned nothing)
- Biome: clean (2 files formatted)
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Levels 38, 39, 40: Audit Fixes

### L38 (External APIs)
- Added `shuffleOptions` import and applied it to both terminal command arrays and OptionCard option arrays in `currentStepConfig` useMemo
- Added 3 new stress scenarios (`slow-stripe`, `stripe-503`, `stripe-down`) matching observe probe IDs so every probe has a corresponding reward scenario
- Added `stressTest.reset()` before `setPhase('reward')` on last build step transition
- Replaced all arbitrary text sizes (`text-[10px]`, `text-[9px]`, `text-[8px]`) with `text-xs`

### L39 (Webhooks)
- Added `stressTest.reset()` before `setPhase('reward')` on last build step transition
- Replaced all arbitrary text sizes (`text-[10px]`, `text-[9px]`) with `text-xs`

### L40 (API Versioning)
- Added `stressTest.reset()` before `setPhase('reward')` on last build step transition
- Replaced all arbitrary text sizes (`text-[10px]`, `text-[9px]`) with `text-xs`

### Checks
- TypeScript: zero errors in all 3 files
- Biome: clean (3 files formatted)
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Levels 32, 33, 34: Audit Fixes (shuffleOptions, code preview, probe-scenario mismatches)

### L32 (Polymorphic Associations)
- Added `shuffleOptions` import and `useMemo` for shuffled options in OptionCard rendering
- Fixed code preview index: replaced `stepper.furthestStep` with `stepper.isCurrentStepCompleted ? stepper.currentStep : stepper.currentStep - 1`

### L33 (Transactions)
- Added `shuffleOptions` import and `useMemo` for shuffled options in OptionCard rendering
- Fixed code preview index: same pattern as L32
- Added 2 new stress scenarios (`boost-fail`, `log-fail`) matching observe probe IDs so every probe has a corresponding reward scenario
- Added scenario ID handling in reward animation: `boost-fail` reuses `boost-creation-fails` animation, `log-fail` reuses `log-fails-rollback` animation

### L34 (Locking)
- Added `shuffleOptions` import and `useMemo` for shuffled options in OptionCard rendering
- Fixed code preview index: same pattern as L32
- Removed leftover "Activate + reward" comment at line 1303, changed to "Reward: complete solution"
- Removed dead 4th instruction branch (`['Review your star rating and visualize the solution']`) from the instructions ternary
- Added new stress scenario `concurrent-checkout` matching observe probe ID, with response lines showing serialized locking behavior
- Added `concurrent-checkout` to SCENARIO_PARAMS map and showRequestB condition
- Fixed `transfer` scenario response text: replaced "debit sender -> credit receiver" funds transfer language with "deduct 5 -> create order -> audit log" stock deduction language

### Checks
- TypeScript: zero errors in all 3 files
- Biome: clean (2 files formatted)
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Levels 35, 36, 37: Audit Fixes

### L35 (Active Storage)
- **CRITICAL: Fixed initial phase** from `useState<Phase>('reward')` to `useState<Phase>('observe')`
- Added `shuffleOptions` import and applied to OptionCard rendering

### L36 (Encryption)
- **CRITICAL: Fixed initial phase** from `useState<Phase>('reward')` to `useState<Phase>('observe')`
- Added `stressTest.reset()` before `setPhase('reward')` on last build step transition
- Fixed `lastScenarioBlocked` never being set: `handleFireScenario` now reads `result.result` from `stressTest.fireRequest()` and calls `setLastScenarioBlocked(result.result === 'blocked')`
- Fixed hardcoded star rating: changed `onComplete({ stars: 3 })` to `onComplete({ stars: stepper.starRating })`
- Added 3 new stress scenarios (`sql-injection`, `backup-leak`, `inspect-config`) matching observe probe IDs, with corresponding entries in `REWARD_VIZ` config map
- Added `shuffleOptions` import and applied to OptionCard rendering

### L37 (Real-Time)
- Added `stressTest.reset()` before `setPhase('reward')` on last build step transition
- Added 3 new stress scenarios (`trigger-event`, `check-polling`, `check-cpu`) matching observe probe IDs
- Added `shuffleOptions` import and applied to OptionCard rendering

### L35/36/37 Checks
- TypeScript: zero errors in all 3 files
- Biome: clean (2 files formatted)
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Levels 16, 17, 18: Remove Activate Phase

### What was done
Removed the `activate` phase from Level 16 (Service Objects), Level 17 (Concerns & Modules), and Level 18 (Validation Contracts).

For each file:
- Changed Phase type from `'intro' | 'build' | 'activate' | 'reward'` to `'intro' | 'build' | 'reward'`
- Removed the `useEffect` that auto-advanced from build to activate when `stepper.isComplete`
- Removed the activate handler function (`handleActivateService`/`handleActivateConcern`/`handleActivateContract`)
- Changed the build phase's last step "Next Step" button to show on all completed steps, with `onClick={hasNextStep ? stepper.nextStep : () => { setPhase('reward'); }}`
- Removed the entire activate phase JSX block (star rating + "Visualize ___" button)
- Removed unused `Star` and `Play` icon imports; removed `useEffect` import from L17 and L18
- Updated left panel step progress condition from `phase === 'build' || phase === 'activate'` to `phase === 'build'`
- Updated file header comments to reflect the 3-phase flow

### Checks
- TypeScript: zero errors (grep Level1[678] returned nothing)
- Biome: clean (3 files formatted)
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Levels 19, 20, 21, 22: Remove Activate Phase

### What was done
Removed the `activate` phase from Level 19 (Query Objects), Level 20 (Error Handling), Level 21 (Action Mailer), and Level 22 (Background Jobs).

For each file:
- Changed Phase type to remove `'activate'` (L19: `'intro' | 'build' | 'reward'`, L20-22: `'observe' | 'build' | 'reward'`)
- Removed the `useEffect` that auto-advanced from build to activate when `stepper.isComplete`
- Removed the activate handler function, replaced with `handleStartReward`
- Changed the build phase's last step "Next Step" button to show on all completed steps, with `onClick={hasNextStep ? stepper.nextStep : handleStartReward}`
- Removed the entire activate phase JSX block (star rating + "Visualize ___" button)
- Removed unused `Star` and `Play` icon imports; removed `useEffect` import from L19
- Updated left panel step progress condition from `phase === 'build' || phase === 'activate'` to `phase === 'build'`
- Updated file header comments to reflect the 3-phase flow

### Checks
- TypeScript: zero errors
- Biome: clean (4 files formatted)
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Levels 16, 17, 18: Audit Fixes (shuffleOptions, code preview index, Check icon)

### Changes

**All 3 levels (L16, L17, L18):**
1. Added `shuffleOptions` import from `@/lib/shuffleOptions` and `useMemo` from React
2. Added `shuffledOptions` useMemo that wraps `currentOptionConfig.options` with `shuffleOptions(options, stepper.currentStep)`
3. Replaced all `currentOptionConfig.options.map(...)` calls with `shuffledOptions.map(...)` for both completed and active OptionCard views
4. Fixed code preview index: replaced `stepper.furthestStep` with `stepper.isCurrentStepCompleted ? stepper.currentStep : stepper.currentStep - 1` for build phase and `STEP_DEFS.length` for reward phase

**L16 only:**
5. Replaced 3 HTML entity checkmarks (`&#10003;` in `<span>`) with `<Check className="w-4 h-4 text-success shrink-0 mt-0.5" />` from lucide-react

### Files Modified
- `frontend/src/features/act3-clean-architecture/components/Level16ServiceObjects.tsx`
- `frontend/src/features/act3-clean-architecture/components/Level17Concerns.tsx`
- `frontend/src/features/act3-clean-architecture/components/Level18ValidationContracts.tsx`

### Checks
- TypeScript: zero errors in changed files
- Biome: clean (no fixes needed)
- Build: successful
- Tests: 1502 pass, 0 fail

## Fix HIGH Audit Issues in Act 3 Levels (L19-L22)

Applied three fixes across all four Act 3 levels:

### Fix 1: Add shuffleOptions to all OptionCard steps
- Imported `shuffleOptions` from `@/lib/shuffleOptions` in all 4 levels
- Wrapped both completed-state and active-state OptionCard `.map()` calls with `shuffleOptions(options, stepper.currentStep)`
- Files: L19, L20, L21, L22

### Fix 2: Fix code preview index
- Replaced `stepper.furthestStep` with `stepper.isCurrentStepCompleted ? stepper.currentStep : stepper.currentStep - 1`
- Reward phase uses `STEP_DEFS.length - 1` (max step index)
- Files: L19, L20, L21, L22

### Fix 3: Add probe-matching stress scenarios
- **L20 (Error Handling):** Added `missing-post`, `bad-params`, `missing-user` scenarios matching probe IDs. Removed `not-found` (replaced by `missing-post`).
- **L21 (Action Mailer):** Added `post-password-reset`, `check-support`, `inspect-user` scenarios matching probe IDs.
- **L22 (Background Jobs):** Replaced `register-user-1`, `register-user-2`, `register-smtp-down` with `register-alice`, `register-bob`, `register-fail` matching probe IDs.

### Checks
- TypeScript: zero errors in changed files
- Biome: clean after auto-format
- Build: successful
- Tests: 1502 pass, 0 fail

## Remove Activate Phase from L9, L10, L11

Removed the `activate` phase from Level 9 (Authentication), Level 10 (Validations), and Level 11 (Callbacks & Normalizations) to match the golden standard three-phase flow (observe -> build -> reward).

### Changes per file

**Level9Authentication.tsx:**
- Phase type: `'observe' | 'build' | 'reward'` (removed `'activate'`)
- Removed `useEffect` auto-advancing build -> activate
- Removed `handleActivateAuth` function
- Last build step (step 4, Protect Endpoint) "Next Step" now calls `setPhase('reward')` + `stressTest.reset()`
- Removed activate phase JSX (star rating + "Visualize Authentication" button)
- Removed unused imports: `Star`, `Play`, `useEffect`
- Left panel condition: `phase === 'build'` instead of `phase === 'build' || phase === 'activate'`

**Level10Validations.tsx:**
- Phase type: `'observe' | 'build' | 'reward'` (removed `'activate'`)
- Removed `useEffect` auto-advancing build -> activate
- Removed `handleActivateValidations` function
- Last build step (step 3, TerminalChoiceStep) `onNext` now transitions to reward, `hasNext` changed to `true`
- Removed activate phase JSX (star rating + "Visualize Validations" button)
- Removed unused imports: `Star`, `Play`
- Left panel condition updated

**Level11Callbacks.tsx:**
- Phase type: `'observe' | 'build' | 'reward'` (removed `'activate'`)
- Removed `useEffect` auto-advancing build -> activate
- Removed `handleActivateLifecycle` function
- Added "Next Step" button for last step (step 3) that calls `setPhase('reward')` + `stressTest.reset()`
- Removed activate phase JSX (star rating + "Visualize Lifecycle" button)
- Removed unused imports: `Star`, `Play`
- Left panel condition updated

### Checks
- TypeScript: zero errors in changed files
- Biome: clean after auto-format
- Build: successful
- Tests: 1502 pass, 0 fail

## Remove Activate Phase from L12, L13, L14, L15

Removed the `activate` phase from Level 12 (Authorization), Level 13 (Testing), Level 14 (Strong Params), and Level 15 (CORS) to match the golden standard three-phase flow (observe -> build -> reward). L12 is the canonical reference implementation cited in CLAUDE.md.

### Changes per file
- Removed `'activate'` from the Phase type union
- Removed the `useEffect` that auto-transitioned from build to activate on `stepper.isComplete`
- Removed activate handler functions, replaced with `handleStartReward`
- Removed activate phase JSX block (star rating + "Visualize" button)
- Removed unused imports (`Star`, `Play`; also `useEffect` from L12/L13/L14)
- Updated left panel condition from `phase === 'build' || phase === 'activate'` to `phase === 'build'`
- Added "Next Step" button on last build step that calls `handleStartReward` (transitions directly to reward)
- Updated docblock comments to reflect three-phase flow

### Files changed
- `frontend/src/features/act2-users-security/components/Level12Authorization.tsx`
- `frontend/src/features/act2-users-security/components/Level13Testing.tsx`
- `frontend/src/features/act2-users-security/components/Level14StrongParams.tsx`
- `frontend/src/features/act2-users-security/components/Level15CORS.tsx`

### Checks
- TypeScript: zero errors in changed files
- Biome: clean after auto-format
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Audit Fixes: Act 2 Levels 12-15

Applied four fixes across Level 12 (Authorization), Level 13 (Testing), Level 14 (Strong Params), and Level 15 (CORS).

### Fix 1: Add shuffleOptions (all 4 levels)
- Imported `shuffleOptions` from `@/lib/shuffleOptions`
- Added `useMemo`-wrapped `shuffledOptions` variable in each component
- Replaced `currentOptionConfig.options.map(...)` with `shuffledOptions.map(...)` in both completed and active OptionCard renders
- L15 also needed `useMemo` added to the React import

### Fix 2: Fix code preview index (all 4 levels)
- Replaced `stepper.furthestStep` in `<CodePreviewPanel>` with:
  - Reward phase: `STEP_DEFS.length - 1` (max step index, shows final code)
  - Build phase: `stepper.isCurrentStepCompleted ? stepper.currentStep : stepper.currentStep - 1`

### Fix 3: L13 probe-matching stress scenarios
- Replaced stress scenario IDs to match probe IDs: `broken-login`, `untested-endpoint`, `bad-migration`
- Labels match the probe labels exactly
- All three probe-matching scenarios are `blocked`, kept three `allowed` scenarios for clean commits

### Fix 4: L15 specific fixes
- Replaced all 20 instances of `text-[10px]` with `text-xs`
- Added `useMemo` to React import (needed for shuffledOptions)

### Files changed
- `frontend/src/features/act2-users-security/components/Level12Authorization.tsx`
- `frontend/src/features/act2-users-security/components/Level13Testing.tsx`
- `frontend/src/features/act2-users-security/components/Level14StrongParams.tsx`
- `frontend/src/features/act2-users-security/components/Level15CORS.tsx`

### Checks
- TypeScript: zero errors in changed files
- Biome: clean (fixed 2 files with auto-format)
- Build: successful
- Tests: 1502 pass, 0 fail

---

## Audit Fixes for Act 2 Levels (L9, L10, L11)

Applied three fixes across Level 9 (Authentication), Level 10 (Validations), and Level 11 (Callbacks).

### Fix 1: Add shuffleOptions to OptionCard arrays
- All three levels were missing `shuffleOptions` for their OptionCard option arrays
- L9: Added `shuffledPasswordOptions` and `shuffledProtectOptions` via `useMemo`
- L10, L11: Added `shuffledOptions` via `useMemo` wrapping `currentOptionConfig.options`
- Imported `shuffleOptions` from `@/lib/shuffleOptions` and added `useMemo` to react imports where missing

### Fix 2: Fix code preview index
- All three levels used `stepper.furthestStep` for `getCodeFiles`, which reveals code too early
- Replaced with `stepper.isCurrentStepCompleted ? stepper.currentStep : stepper.currentStep - 1` for build phase
- Reward phase uses `STEP_DEFS.length` (max step index) to show complete code

### Fix 3: Add probe-matching stress scenarios
- L9: Replaced `no-token-delete` with `delete-no-token`, added `create-no-token` and `check-identity` scenarios (matching probe IDs exactly)
- L10: Added `empty-post` and `bad-email` scenarios, updated `duplicate-email` label to match probe label
- L11: Renamed `messy-signup` to `signup-messy`, `clean-lookup` to `lookup-clean`, `welcome-email` to `check-mailer` (matching probe IDs). Updated all inline references to old IDs in the reward visualization

### Checks
- TypeScript: zero errors in changed files
- Biome: clean (fixed 3 files with auto-format)
- Build: successful
- Tests: 1502 pass, 0 fail
