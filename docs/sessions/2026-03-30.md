# Session Log: 2026-03-30

## Act 7: Scale - Complete Redesign (All 6 Levels)

Designed and implemented all 6 levels in Act 7 (Levels 48-53) following the three-phase sequential flow standard (observe -> build -> reward). All existing implementations were replaced: the old levels lacked proper observe/reward phases, had too few build steps (L51 and L53 had only 1), and did not follow the golden standard patterns.

### Design decisions
- All 6 levels use Type 3 (custom visualization) since Act 7 concepts are architectural/spatial
- Each level has 4 probes (3 required) and 6 build steps (2 terminal + 4 option) for consistency
- Every visualization is unique: traffic routing, state diagrams, tenant tables, log viewers, dependency graphs, service topologies
- No activate phase: build flows directly to reward

---

## Level 48: Multi-Database - Full Rewrite

### What was done
- Rewrote from a single-screen sandbox (add replica buttons, toggle connects_to, run simulator) to full three-phase flow
- Custom FlowDiagram: 2 nodes in observe (Rails App, Primary DB with overload indicators), 3 in reward (add Read Replica)
- Animation frames show latency spikes, pool exhaustion, and routing decisions per probe/scenario
- 5 build steps: add replica entry, set replica:true, connects_to declaration, database_selector config, replication delay
- Reward: routing visualization (GET -> replica, POST -> primary), with delay behavior demonstration

## Level 49: State Machines - Full Rewrite

### What was done
- Rewrote from interactive SVG drawing approach to standard three-phase flow
- Custom state diagram: 5 circular nodes (pending, confirmed, shipped, delivered, cancelled) with transition edges
- Observe shows chaotic bidirectional red dashed arrows (any state can reach any state)
- Reward shows only valid forward transitions as solid green arrows with guard badges
- 6 build steps: add AASM, define states, define transitions, add guards, add PaperTrail, wire audit

## Level 51: Observability - Full Rewrite

### What was done
- Rewrote from a single-step flame graph to full three-phase flow
- Custom 4-node visualization: unstructured-log (active/red), structured-log (grayed), trace-timeline (grayed), health-endpoint (grayed)
- Observe probes show debugging failures: can't search logs, can't correlate errors, no tracing, no health check
- Reward: all 4 nodes turn green, scenarios demonstrate diagnostic capabilities
- 6 build steps: add Lograge, configure JSON formatter, add custom fields, add OpenTelemetry, configure tracing, add health endpoint

## Level 52: Modular Monolith - Full Rewrite

### What was done
- Rewrote Level 52 (Modular Monolith) from scratch following the exact three-phase sequential flow pattern from Level 48 (Multi-Database)
- Replaced the old 4-step OptionCard-only implementation with a full observe -> build -> reward flow

### Phase 1 (Observe)
- Custom 2x2 grid visualization using FlowDiagram with 4 PackageNode instances: Billing (top-left), Notifications (top-right), Orders (bottom-left), Inventory (bottom-right)
- 5 edges showing coupling: Billing->Notifications, Orders->Inventory, Inventory->Orders (circular), Billing->Orders, Notifications->Inventory
- Red dashed arrows show undeclared coupling in observe; green solid arrows in reward
- 4 probes (3 required): cross-domain coupling, circular dependency, no ownership, privacy violation
- Stage inspector on each domain node with code examples

### Phase 2 (Build - 6 steps)
- Step 0 (Terminal): `bundle add packwerk`
- Step 1 (Terminal): `bin/packwerk init`
- Step 2 (OptionCard): Configure enforce_dependencies + enforce_privacy
- Step 3 (OptionCard): Define public API in app/public/
- Step 4 (OptionCard): Declare explicit dependencies
- Step 5 (OptionCard): Set up CODEOWNERS by domain team

### Phase 3 (Reward)
- Same 2x2 grid with green borders, "Public API" labels, solid green arrows for valid deps
- 7 stress scenarios: 3 allowed (valid dep call, public API, CI check) + 4 blocked (private access, undeclared dep, circular, no privacy bypass)
- Allowed/Violations counters in left panel

### Checks
- TypeScript: zero errors
- Biome: formatted (1 file fixed)
- Build: successful
- Tests: 1502 pass, 0 fail

## Level 50: Multi-Tenancy - Full Rewrite

### What was done
- Rewrote Level 50 (Multi-Tenancy) from scratch following the exact three-phase sequential flow pattern from Level 48 (Multi-Database) and Level 49 (State Machines)
- Replaced the old strategy-picker/toggle-models implementation with a full observe -> build -> reward flow

### Phase 1 (Observe)
- Custom visualization using FlowDiagram with 3 nodes: Acme Corp (blue, top-left), Globex Inc (orange, top-right), and a database table node (center-bottom)
- Two edges: Acme -> Table (blue dots), Globex -> Table (orange dots)
- 4 probes (3 required): cross-tenant product read, cross-tenant order read, orphan record creation, direct ID access bypass
- Stage inspector on all 3 nodes with code examples showing unscoped queries

### Phase 2 (Build - 6 steps)
- Step 0 (Terminal): `bundle add acts_as_tenant`
- Step 1 (Terminal): `rails generate migration AddCompanyToProducts company:references`
- Step 2 (OptionCard): Set current tenant via subdomain with set_current_tenant_through_filter
- Step 3 (OptionCard): Add acts_as_tenant :company to Product model
- Step 4 (OptionCard): Add acts_as_tenant :company to Order model
- Step 5 (OptionCard): Add tenant-scoped unique index on [:company_id, :sku]

### Phase 3 (Reward)
- Same 3-node viz with green borders and "WHERE company_id = ?" filter indicator
- 7 stress scenarios: 4 allowed (list products/orders per tenant, create with auto-assign, same SKU across tenants) + 3 blocked (cross-tenant read, cross-tenant order, no-tenant-set)
- "Isolated" (green) / "Blocked" (red) counters in left panel

### Checks
- TypeScript: zero errors
- Biome: formatted (1 file fixed)
- Build: successful
- Tests: 1502 pass, 0 fail

## Level 53: Domain Events & Decoupling - Full Rewrite

### What was done
- Rewrote Level 53 (Domain Events) from scratch following the exact three-phase sequential flow pattern from Level 48 (Multi-Database)
- Replaced the old toggle-based event bus demo with a full observe -> build -> reward flow

### Phase 1 (Observe)
- Custom chain visualization using FlowDiagram: CheckoutService at top, sequential arrows down through EmailService -> InventoryService -> AnalyticsService -> ShippingService
- 4 chain edges showing direct method calls in waterfall order
- 4 probes (3 required): email-slow (sequential bottleneck), email-down (cascade failure), add-loyalty (tight coupling), partial-failure-retry (no isolation)
- Stage inspector on all 5 service nodes with code examples

### Phase 2 (Build - 6 steps)
- Step 0 (Terminal): `bundle add wisper`
- Step 1 (OptionCard): Define OrderCompleted event class with Wisper::Publisher
- Step 2 (OptionCard): Replace direct calls in CheckoutService with broadcast
- Step 3 (OptionCard): Subscribe EmailService as listener (EmailListener + Wisper.subscribe)
- Step 4 (OptionCard): Subscribe remaining services (Inventory, Analytics, Shipping listeners)
- Step 5 (OptionCard): Add async processing (delegate to background jobs via Solid Queue)

### Phase 3 (Reward)
- Fan-out visualization: CheckoutService -> Event Bus (green) -> 4 service nodes horizontally
- 7 stress scenarios (all "allowed" since events always succeed at publish): normal checkout, email slow, email down, analytics fail, add loyalty, retry email, cascade attempt
- "Processed" (green) / "Retrying" (amber) counters in left panel
- Animation shows parallel processing, independent failures, granular retries

### Checks
- TypeScript: zero errors
- Biome: formatted (1 file fixed)
- Build: successful
- Tests: 1502 pass, 0 fail

## Level 48: Multi-Database - Audit Fix (Probe-to-Scenario ID Mismatch)

### What was done
- Fixed probe-to-scenario ID mismatch: observe probes `peak-traffic` and `check-pool` had no matching reward stress scenario IDs
- Added two new stress scenarios: `peak-traffic` (100 concurrent reads routed to replica, 0 timeouts) and `check-pool` (check pool stats, healthy distribution)
- Added `REWARD_POOL_FRAMES` animation for `check-pool` showing healthy pool distribution across primary (5/25 busy, writes only) and replica (18/25 busy, reads here)
- Added both new scenario IDs to `REWARD_PROBE_FRAMES` map (`peak-traffic` reuses `REWARD_PEAK_FRAMES`, `check-pool` uses new `REWARD_POOL_FRAMES`)
- All checks pass: zero TS errors, biome clean, build successful

## Level 50: Multi-Tenancy - Audit Fix: Cumulative Code Patterns

### What was done
- Updated observe phase code preview to show cumulative patterns from earlier levels:
  - Controller now delegates to `Products::ListService.call(params:)` (Service Objects from L16+)
  - Added service file showing the unscoped `Product.all` query
  - Controller renders via `ProductSerializer` (Serializers from L7+)
- Updated build phase code preview (step 3+) to show:
  - Service file with comments explaining acts_as_tenant auto-scopes `Product.all`
  - Controller file unchanged, demonstrating that tenant scoping is transparent to service/controller layer
- All checks pass: zero TS errors, biome clean, build successful

## Level 51: Observability - Audit Fixes

### What was done
- **CRITICAL: Probe-to-scenario ID mismatch** - Renamed stress scenario IDs `trace-bottleneck` to `find-bottleneck` and `health-check` to `check-health` to match probe IDs. Updated `REWARD_PROBE_FRAMES` keys to match.
- **HIGH: Solution leakage in left panel** - Rewrote scenario text to describe only the problem (no mention of Lograge, OpenTelemetry, or health check endpoint). Now describes what is broken and what the team cannot do.
- **HIGH: L41 contradiction** - Acknowledged L41's RequestLogger middleware in scenario text ("Your RequestLogger middleware from L41 logs basic request/response data, but it is not enough"). Updated observe code preview to show the existing RequestLogger as the "before" state with comments noting its limitations.
- **MEDIUM: Missing service object in health endpoint** - Updated health endpoint option and code preview to delegate to `HealthCheckService.call` instead of inline DB/Redis/disk checks.
- **LOW: LogNode badge always bg-destructive** - Made badge color conditional: green flash uses `bg-success/20 text-success`, red flash uses `bg-destructive/20 text-destructive`, default uses `bg-muted text-muted-foreground`.
- **LOW: No stage-discovery for no-correlation** - Added `'structured-log': 'no-correlation'` to `STAGE_DISCOVERY_MAP`.
- All checks pass: zero TS errors, biome clean, build successful, 1502 tests pass

## Level 49: State Machines - Audit Fixes

### What was done
- **HIGH: PaperTrail already installed at L43** - Replaced step 4 (`bundle add paper_trail`) with `rails generate paper_trail:install && rails db:migrate`. Updated STEP_DEFS title to "Set Up PaperTrail", terminal commands (wrong options: `rails generate migration CreateVersions` and `rails generate model Version`), output lines showing migration creation, and SHELL_STEP_MAP. Updated Gemfile code preview to always show `gem "paper_trail"` with a comment noting it was installed in L43.
- **HIGH: Code preview missing Service Objects (L16+)** - Added service object file to observe phase showing `Orders::ConfirmService` with direct `order.update!(status: "confirmed")` (the broken "before" state). Added updated service file from step 2 onward showing `order.confirm!` with `AASM::InvalidTransition` rescue (the "after" state using state machine events).
- **MEDIUM: Probes `set-invalid-status` and `check-audit` have no matching reward scenarios** - Added two new stress scenarios: `set-invalid-status` (blocked, AASM rejects arbitrary status strings) and `check-audit` (allowed, PaperTrail shows version history). Added matching `REWARD_BLOCKED_INVALID_STATUS_FRAMES` and `REWARD_AUDIT_TRAIL_FRAMES` animation arrays, wired into `REWARD_PROBE_FRAMES`.
- **LOW: `text-[10px]` arbitrary Tailwind value** - Replaced all `text-[10px]` with `text-xs` in StateNode badge and TransitionEdge label.
- All checks pass: zero TS errors, biome clean, build successful, 1502 tests pass

## Level 52: Modular Monolith - Audit Fixes

### What was done
- **HIGH: StressTestPanel missing `disabled={vizAnimating}`** - Added `disabled={vizAnimating}` prop to prevent firing scenarios during animation playback.
- **HIGH: InvoiceSender lacks ApplicationService inheritance (L16+)** - Updated observe code preview to show `class Billing::InvoiceSender < ApplicationService` with `Result = Data.define(:invoice, :success, :error)` pattern.
- **LOW: Edge label `text-[10px]`** - Replaced with `text-xs`.
- All checks pass: zero TS errors, biome clean, build successful.

## Level 53: Domain Events - Audit Fixes

### What was done
- **HIGH: StressTestPanel missing `disabled={vizAnimating}`** - Added `disabled={vizAnimating}` prop.
- **HIGH: CheckoutService lacks ApplicationService inheritance (L16+)** - Updated ALL code previews (observe, stage inspector, build step 2 options, build code preview) to show `class CheckoutService < ApplicationService`. Added `Result = Data.define(:order, :success, :error)` in observe code.
- **MEDIUM: Probe `partial-failure-retry` ID mismatch** - Renamed stress scenario ID from `retry-email` to `partial-failure-retry` and updated the matching key in `REWARD_PROBE_FRAMES`.
- **MEDIUM: Badge styling always bg-destructive in reward** - Made badge color conditional on flash state in both CheckoutNode and ServiceNode: green -> `bg-success/20 text-success`, red -> `bg-destructive/20 text-destructive`, amber -> `bg-warning/20 text-warning`, default -> `bg-muted text-muted-foreground`.
- **LOW: Edge label `text-[10px]`** - Replaced with `text-xs`.
- All checks pass: zero TS errors, biome clean, build successful.
