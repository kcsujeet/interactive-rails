# Session: 2026-04-17

## L1 Environment: switch from asdf to mise

Replaced asdf with mise as the version manager taught in Level 1 (The
Environment). In 2026 mise is the recommended tool: Rust-based (faster), core
Ruby plugin built in (no `asdf plugin add ruby`), single-line shell activation,
and it still reads the standard `.tool-versions` format so the rest of the
level's mental model is unchanged.

### What changed

- `src/features/act1-foundation/content/level-1-environment.ts`
  - Trigger/observation/goal no longer mention `asdf`, now describe a generic
    "version manager" so the tool name isn't leaked in player-facing copy.
  - `conceptExplanation` rewritten to highlight mise's Rust speed, built-in
    Ruby support, and shell activation model.
  - `railsCodeExample` now uses `brew install mise`, `mise activate zsh`,
    and `mise install`.
  - `furtherReading` link swapped for https://mise.jdx.dev/.

- `src/features/act1-foundation/components/level-1-environment/Level1Environment.tsx`
  - Step 1 "Install mise": correct answer `brew install mise`, wrong options
    `apt-get` and `npm install -g`.
  - Step 2 "Activate mise in .zshrc": correct answer
    `eval "$(mise activate zsh)"`. Wrong options cover PATH-only, a
    non-existent `mise.sh` source, and alias-only, each with feedback
    explaining why they miss the shell hook mise relies on for auto-switching.
  - Step 3 `.tool-versions` format unchanged (mise reads the same file).
  - Step 4 "Install Ruby": correct answer `mise install`.
  - Step 5 "Install Rails": unchanged (`gem install rails`).
  - `getCodeFiles` ~/.zshrc preview now shows `eval "$(mise activate zsh)"`.
  - File-header comment updated to reflect the new step sequence.

- `docs/spec.md`: Level 1 row in the concept table now says "mise, Ruby, Rails
  install" instead of "asdf, Ruby, Rails install".

### Checks (all green)

- `bunx biome check --write` on touched files: 1 file auto-formatted, 0
  remaining diagnostics.
- `bunx tsc --noEmit`: no errors.
- `bun run build`: completed successfully (standard Vite font warnings only).
- `bun test`: 1502 / 1502 pass across 46 files.

## L1 Environment: switch `.tool-versions` -> `.mise.toml`

Follow-up on the mise migration: the config step now teaches the mise-native
TOML format instead of the asdf-compat `.tool-versions`. Reasoning: if we're
already teaching mise, teach the format mise's own docs recommend. `.mise.toml`
is also what later levels would extend with env/tasks, so anchoring on it in
L1 makes the progression coherent.

### What changed

- `Level1Environment.tsx`
  - Step 3 renamed from `tool-versions` -> `mise-toml`, title "Configure
    .mise.toml".
  - Wrong options exercise three realistic TOML/config mistakes:
    - YAML colon syntax (`ruby: "3.3.6"`)
    - asdf-style space-separated (`ruby 3.3.6`) inside a `.toml` file
    - bare TOML key with no section (`ruby = "3.3.6"`)
  - Correct option uses section syntax. Label uses `>` as an inline separator
    (`[tools] > ruby = "3.3.6"`) because button labels render single-line;
    the `command` uses `printf '[tools]\nruby = "3.3.6"\n'` to produce the
    real multi-line file contents.
  - Step 4 description and `brew install ruby` feedback now reference
    `.mise.toml` instead of `.tool-versions`.
  - `getCodeFiles` step-3 preview switched to a `toml` file showing
    `[tools]\nruby = "3.3.6"`.

- `level-1-environment.ts`
  - `trigger.description` generalised ("project config file") so the filename
    isn't spoiled before the player hits step 3.
  - `codeExample` comment block no longer names `.tool-versions`.
  - `conceptExplanation` "The .mise.toml file" section replaces the old
    `.tool-versions` section; adds a note that the same file can hold env
    vars and tasks.
  - `railsCodeExample` uses a heredoc to write `[tools]\nruby = "3.3.6"` into
    `.mise.toml`.
  - `commonMistakes[1]` updated to the real TOML pitfall ("bare key without
    section header") instead of the old hyphen/colon pitfall.
  - `whenToUse` and `hint.text` updated to reference `.mise.toml`.

### Checks

- `bunx biome check --write`: no diagnostics.
- `bunx tsc --noEmit`: no errors.
- `bun run build`: completed.
- `bun test`: 1502 / 1502 pass.

## LevelHeader: two-step Submit -> Complete flow

The Submit button had three label states (`Submit`, `Complete Level`,
`Completing...`) but `handleSubmit` validated *and* completed in the same
click, so the green `Complete Level` state was unreachable and a successful
validation jumped straight to the redirect. Players never got a chance to see
the success banner or confirm.

### What changed

- `src/components/levels/LevelHeader.tsx`
  - `handleSubmit` now short-circuits to `onComplete()` when `lastResult?.valid`
    is already true (i.e. the button is already in the `Complete Level`
    state). Otherwise it just validates and surfaces feedback.
  - Net effect: first click validates, second click completes. No config/UI
    changes elsewhere were needed; the button label already reflected this
    intended flow.
  - Cleaned up pre-existing warnings in the same file while touching it
    (etiquette rule: own every error in files you touch):
    - Removed unused `useMemo` import.
    - Removed unused `buildUnifiedProjectAtStep` import.
    - Removed unused `completedLevels` and `currentLevelId` props (no caller
      was passing them).

### Checks

- `bunx biome check src/components/levels/LevelHeader.tsx`: no diagnostics.
- `bunx tsc --noEmit`: no errors.
- `bun run build`: completed.
- `bun test`: 1502 / 1502 pass.

## Kamal deployment level: phase 1 (rename + renumber)

Prep for adding a Kamal deploy level. This phase is mechanical only: act
renames, Act 6 directory rename, Act 6 L42-L47 shifted +1 so a new L42
Deployment slot is free, and the cascading Act 7/8 shift to avoid level-number
collisions. No new content written yet.

### Scope decisions

- Act 5: "Production Features" -> **"Advanced Features"** (act names describe
  Rails concepts, not the e-commerce narrative).
- Act 6: "Reliability" -> **"Operations"** (broader scope now that deploy,
  middleware, rate limiting, jobs, data lifecycle, and error tracking all
  live here).
- Deploy level goes at Act 6 L42 (the pivot). Act 6 L42-L47 shifted to L43-L48.
- Cascade: Act 7 L48-L53 -> L49-L54, Act 8 L54-L56 -> L55-L57. Total level
  numbering now tops out at 57 (was 56); the new L42 Deployment slot is
  currently empty pending phase 2.

### File moves

- `src/features/act6-reliability/` -> `src/features/act6-operations/` (git mv).
- Act 6: 6 content files, 6 component dirs, 6 component .tsx, 6 test files
  all renamed +1 (L42-L47 -> L43-L48).
- Act 7: 6 content files, 6 component dirs, 6 .tsx renamed +1 (L48-L53 ->
  L49-L54).
- Act 8: 3 content files, 3 component dirs, 3 .tsx renamed +1 (L54-L56 ->
  L55-L57).

### In-file updates

- Every shifted level's content file: `levelNumber`, `id` string, and the
  `export const levelNX...` identifier bumped to match new number.
- Every shifted level's component .tsx: `registerLevelCode('act6-levelN...')`
  id string updated; exported function + default export renamed to
  `LevelNX...` with the new number.
- Test files: matching identifier renames.
- Act 5 `content/act.ts`: name, tagline, description rewritten for "Advanced
  Features".
- Act 6 `content/act.ts`: name -> Operations, tagline/description rewritten,
  imports updated, exported vars renamed to match new numbers.
- Act 7/8 `content/act.ts`: imports updated to new paths and new identifier
  names. Also fixed a pre-existing naming quirk in Act 8 (variables were
  named `level49APIGateway`, `level50Sharding`, `level51Architect` — off by
  several — now correctly `level55/56/57`).
- `src/lib/levels-registry.ts`: complete rewrite of Act 6/7/8 sections
  (imports, curriculum comment, `LEVEL_COMPONENTS` map). L42 left with a
  placeholder comment.
- `src/lib/acts-registry.ts`: `actSix` import path updated.
- `scripts/validate-levels.ts`: `expectedCount` bumped 50 -> 56 (matches the
  real current count; will bump to 57 when L42 Deployment is implemented).
- `docs/spec.md`: Act 5/6/7/8 headers and tables rewritten for new names and
  numbers. L42 Deployment placeholder row added. Rails 8 Features Integration
  table updated (rate_limit at L43, Solid Queue at L46, Kamal 2 at L42).
  Stats line bumped 56 -> 57.

### Checks

- `bunx biome check --write` on touched files: 1 file auto-formatted.
- `bunx tsc --noEmit`: no errors.
- `bun run build`: completed.
- `bun test`: 1502 / 1502 pass.
- `bun run scripts/validate-levels.ts`: 7 pre-existing `missing
  successConditions` errors remain (in Act 1, 2, 5 level content files I did
  not touch). No new errors introduced by this phase. The count mismatch
  error is gone.

### Not done (intentional, phase 2 territory)

- **No L42 Deployment content yet.** The ID `act6-level42-...` currently has
  no registered component; `levels-registry.ts` has a commented placeholder.
  The registry map simply skips L42. Players would hit the generic pipeline
  fallback if they somehow navigated there, but no route links to it yet.
- `src/server/services/content.ts` `unlockRequirement: level: 45` left
  unchanged. That's a player-progression threshold for a "db-mastery" realm,
  not a content anchor; shifting it would change gameplay semantics, not
  just naming.

## Kamal deployment level: phase 2 (L42 content)

Wrote the new L42 Deployment level. Three-phase flow mirroring L41's
pattern, using the shared `PipelineFlow` visualization rather than custom
React Flow nodes to keep the component under 1,100 lines.

### Files created

- `src/features/act6-operations/content/level-42-deployment.ts` — Level
  content (trigger, problem, learningContent, hint). `successConditions`
  uses the existing `'zero_downtime_configured'` type.
- `src/features/act6-operations/components/level-42-deployment/Level42Deployment.tsx` —
  component with the three phases.
- `src/features/act6-operations/__tests__/Level42Deployment.test.ts` — 42
  tests (discoveries, probes, probe-to-discovery mapping, build step quality
  per option set, stress scenarios, phase coverage).

### Files updated

- `src/features/act6-operations/content/act.ts` — imports
  `level42Deployment`, inserts it into `levels[]` after L41 Middleware.
- `src/lib/levels-registry.ts` — imports `Level42Deployment`, adds
  `'act6-level42-deployment'` to `LEVEL_COMPONENTS`, removes the "TBD"
  placeholder comment.
- `scripts/validate-levels.ts` — `expectedCount` bumped 56 -> 57.

### Three-phase shape

- **Observe**: 5-stage horizontal `PipelineFlow` (Dev Laptop -> Registry ->
  Proxy -> Container -> Server). Registry/Proxy/Container start `inactive`
  (dashed, dimmed). Server starts `danger` with a "502!" badge. 5 probes
  show five different manual-deploy failure modes (scp+restart downtime,
  git-pull drift, no-health-gate, rollback redeploy, two-server split-brain).
  Unlock at 3+ discoveries.
- **Build**: 5 steps with shuffled options:
  1. `bundle add kamal` (terminal)
  2. `kamal init` (terminal)
  3. Pick the valid `config/deploy.yml` (option card, 4 choices: missing
     proxy, inlined password, correct, missing servers)
  4. Pick the valid `.kamal/secrets` (option card, 4 choices: literal
     secrets, Ruby ENV, correct, empty-file dodge)
  5. `kamal setup` vs `kamal deploy` / `kamal push` (terminal)
- **Reward**: Same 5-stage pipeline, now active. `StressTestPanel` with 5
  scenarios: healthy deploy, broken-health gate, rollback, staggered fleet
  deploy, broken image push. Node sublabels react to the last-fired scenario
  (proxy "gate held", container "rotation aborted", etc.).

### API matching pass

Several component APIs differ from my first draft; corrected them:
- `DiscoveryChecklist` expects `discoveries={gating.discoveries}` +
  `discoveredCount` (not a mapped array with a `discovered` field).
- `OptionCard` uses `name`, not `label`.
- `ProbeConfig.responseLines[].color` union is
  `'default' | 'green' | 'yellow' | 'red' | 'cyan' | 'muted'` (no `'amber'`).
- `StressScenario.story` is `string[]`, not `string` — dropped the `story`
  field from all probes/scenarios since it's optional.
- Imported `StressScenario` type from `@/hooks/useStressTest` instead of
  declaring a local one.

### Checks

- `bunx tsc --noEmit`: clean.
- `bunx biome check --write` on changed files: 2 files auto-formatted, 0
  diagnostics.
- `bun test`: 1544 / 1544 pass (42 new tests for L42).
- `bun run build`: completed.
- `bun run scripts/validate-levels.ts`: 7 pre-existing `missing
  successConditions` errors carried over from phase 1; L42 itself is clean
  (not in the failure list).

### What is intentionally MVP

- Reward visualization is static-per-scenario (sublabel changes, no animated
  container rotation). L41 uses custom React Flow nodes for richer
  animations; I picked `PipelineFlow` for scope control.
- `story: string[]` bullet-point info modals are dropped. Can be added
  later if we want the info modal on probes/scenarios.
- No UI shown in a browser yet; need a manual smoke-test before sign-off.

## L42 bulletproof-react refactor + codified rule

User flagged that my L42 single-file mega-component was an
bulletproof-react violation. Refactored L42 and codified the rule.

### New non-negotiable rule in CLAUDE.md

Added "Per-Level Feature Structure (non-negotiable)" under Project
Architecture. Spells out the required layout:

```
src/features/actN-<name>/
  content/
    level-<N>-<name>.ts
  components/
    level-<N>-<name>/
      Level<N><Name>.tsx      # thin orchestrator (<=150 lines)
      ObservePhase.tsx
      BuildPhase.tsx
      RewardPhase.tsx
      data/
        discoveries.ts
        probes.ts
        build-steps.ts
        stress-scenarios.ts
        pipeline-stages.ts
        code-files.ts
      types.ts
  __tests__/
    Level<N><Name>.test.ts    # imports from data/ directly
```

Rules: phase components are separate files (no inline `renderObserve`/etc
helpers), module-scope data lives in `data/<topic>.ts`, no file in a level
dir exceeds ~300 lines, `getCodeFiles` + `registerLevelCode` live in
`data/code-files.ts`, tests import data directly so renames fail at
type-check time.

Legacy levels stay single-file for now; the rule applies to every new level
and any level touched substantively.

### L42 split

Previous: single `Level42Deployment.tsx` at 1,156 lines.

New layout (all under 270 lines each):

- `Level42Deployment.tsx` — 96 lines. Holds phase state + hooks; renders
  one of three phase components.
- `ObservePhase.tsx` — 105 lines.
- `BuildPhase.tsx` — 208 lines (contains inner `OptionStep` sub-component).
- `RewardPhase.tsx` — 103 lines.
- `data/discoveries.ts` — 7 lines.
- `data/probes.ts` — 96 lines.
- `data/build-steps.ts` — 264 lines.
- `data/stress-scenarios.ts` — 85 lines.
- `data/pipeline-stages.ts` — 91 lines.
- `data/code-files.ts` — 171 lines. Owns `registerLevelCode(...)` call.

Tests rewritten to import from the `data/` files directly (was: mirrored
inline). Now 45 tests, all passing.

### Checks

- `bunx tsc --noEmit`: clean.
- `bunx biome check --write`: 8 files auto-formatted.
- `bun run build`: completed.
- `bun test`: 1547 / 1547 pass.

## L42 colocation pass

User: "everything related to that component/level lives near it." Moved
the content file and test file into the level directory and nested the
content under `data/` (it is static level metadata, same category as
discoveries/probes).

### File moves

- `src/features/act6-operations/content/level-42-deployment.ts`
  -> `src/features/act6-operations/components/level-42-deployment/data/content.ts`
- `src/features/act6-operations/__tests__/Level42Deployment.test.ts`
  -> `src/features/act6-operations/components/level-42-deployment/__tests__/Level42Deployment.test.ts`

### Consequences

- `act6-operations/content/act.ts` now imports L42's content via a relative
  path into the level's `data/` dir: `../components/level-42-deployment/data/content`.
- Test file's data imports shortened to `../data/*` since tests are now a
  sibling of the `data/` dir.
- L42 is fully self-contained: everything the level owns lives under
  `src/features/act6-operations/components/level-42-deployment/`.

### CLAUDE.md rule updated

The per-level structure now specifies `data/content.ts` (not a top-level
`content.ts` inside the level dir, not a feature-level `content/`
directory). The feature's `content/act.ts` imports each level's content
via a relative path into its level dir.

### Checks

- `bunx tsc --noEmit`: clean.
- `bun test`: 1547 / 1547 pass.
- `bun run build`: clean.

### Notes / leftover

- CLAUDE.md still contains two `asdf` example snippets under the
  "correct-answer position" rule. Those are generic illustrations of the rule
  itself, not Level 1 content, so left as-is.
- `docs/rails-scales-book/...` and older session logs still mention asdf;
  those are historical artifacts and not player-facing.
- Level 1 does not yet have a dedicated test file (the
  `Level1StackChoice.test.ts` in `__tests__/` is a legacy file for a
  different level). Adding a test for L1 Environment is a follow-up.
