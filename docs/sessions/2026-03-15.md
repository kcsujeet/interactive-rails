# Session Log: 2026-03-15

## Act 5 Sequential Rewrite (Continued)

Continuing the sequential rewrite of Act 5 levels (L32-L39). L32-L37 were completed in the previous session. This session continues with L38 and L39.

### Level 38: Webhooks & Idempotency (Rewrite Complete)

**Replaced:** 546-line legacy toggle-based webhook simulator (WebhookConfig toggles, setTimeout processing, no three-phase flow)

**New component:** ~840 lines with full three-phase flow

#### Visualization: "Webhook Event Queue" (Type 3 custom, vertical top-to-bottom)
- Incoming Stripe Webhook at top -> Signature Gate -> Idempotency Gate -> Processing Gate -> Response at bottom
- Vertically stacked gates with FlowConnectors between each
- Observe: all gates show "absent"/"sync" (gray), events flow through unprotected
- Reward: gates show active states, rejected events blocked at signature gate, duplicates caught at idempotency gate, valid events enqueued async
- Visually distinct from L36 (side-by-side lanes), L37 (horizontal gate pipeline)

#### Discoveries (4):
- no-signature, duplicate-credit, sync-timeout, no-dedup

#### Probes (3):
- Forged webhook (no signature), duplicate event_id, slow synchronous handler

#### Build Steps (6):
1. Generate webhook_events migration (terminal)
2. Run migration (terminal) - follows migration generation immediately
3. Configure signature verification (option) - Stripe::Webhook.construct_event
4. Configure idempotency check (option) - atomic find_or_create_by with unique index
5. Configure async processing (option) - background job via Solid Queue
6. Build webhook handler service (option) - IngestStripeWebhook < ApplicationService

#### Stress Scenarios (6):
- 4 allowed: valid payment, valid subscription, duplicate (deduped gracefully), valid refund
- 2 blocked: forged event (bad signature), malformed JSON

#### Cumulative Patterns:
- IngestStripeWebhook < ApplicationService with Result = Data.define(:success?, :webhook_event, :errors)
- Controller delegates to service: IngestStripeWebhook.call(payload:, signature:)
- Background job: ProcessStripeWebhookJob with retry_on and polynomially_longer backoff
- Wrong "controller logic" option flagged for service object pattern violation

#### Content.ts Updates:
- Renamed variable: level35Webhooks -> level38Webhooks
- Fixed section comment: "Level 35" -> "Level 38"
- Added requiresTests: true

#### Tests: 43 tests, 184 assertions
- Discovery defs, probes, build step quality, stress scenarios, cross-phase consistency, cumulative compliance, data consistency
- Fixed: wrong service option feedback revealed "find_or_create_by!" (correct answer API)

#### Biome Fixes:
- Removed unused imports: ListChecks, Lock, MailWarning, ShieldAlert
- Removed unused state: lastProbeId
- Changed onComplete to _props for unused parameter

### Level 39: API Versioning (Rewrite Complete)

**Replaced:** 769-line legacy toggle/radio-based version picker (VersioningConfig, strategy selector, side-by-side endpoint comparison, no three-phase flow)

**New component:** ~1350 lines with full three-phase flow

#### Visualization: "Version Router" (Type 3 custom, fork/split layout)
- Two client boxes (v1, v2) on left -> central Router node -> two branching controller paths (v1 + v2)
- Observe: single controller path, no versioning. Router shows "No Split" in red
- Reward: Router splits correctly. v1 path shows "Deprecated + Sunset" (amber), v2 shows object format (green)
- Unknown versions (v3, unversioned) show both paths dimmed with 404
- Visually distinct from L36 (lanes), L37 (horizontal gates), L38 (vertical gates)

#### Discoveries (4):
- single-controller, breaking-change, no-deprecation, no-migration-path

#### Probes (3):
- v1 client expects cents, v2 client wants object, breaking deploy

#### Build Steps (6):
1. Add v2 namespace to routes (terminal)
2. Generate v2 controller (terminal)
3. Create v2 serializer with new response shape (option)
4. Add deprecation headers to v1 (option)
5. Add Sunset header with future date (option)
6. Wire v1 controller to use service delegation + versioned serializer (option)

#### Stress Scenarios (6):
- 4 allowed: v1 cents, v2 object, v1 deprecated, v2 with line_items
- 2 blocked: v3 not found, unversioned 404

#### Cumulative Patterns:
- FetchOrder.call(id:) service delegation in both v1 and v2 controllers
- Versioned serializers: Api::V1::OrderSerializer and Api::V2::OrderSerializer
- Error shape: { error: { code: "NOT_FOUND", message: ... } }
- Wrong "direct render" option flagged for bypassing service layer

#### Content.ts Updates:
- Renamed variable: level36APIVersioning -> level39APIVersioning
- Fixed section comment: "Level 36" -> "Level 39"
- Added requiresTests: true

#### Tests: 40 tests, 162 assertions
- Discovery defs, probes, build step quality, stress scenarios, cross-phase consistency, cumulative compliance, data consistency

## Act 5 Rewrite Summary

All 8 levels (L32-L39) now have full three-phase flow implementations:

| Level | Visualization | Tests | Assertions |
|-------|--------------|-------|------------|
| L32 Polymorphic | Schema Split (3 tables -> 1) | Yes | Yes |
| L33 Transactions | Race Timeline (concurrent writes) | Yes | Yes |
| L34 Active Storage | Upload Pipeline (file -> variants) | Yes | Yes |
| L35 Encryption | Field Encryption (plaintext -> cipher) | Yes | Yes |
| L36 Real-Time | Connection Comparison (polling vs WebSocket) | Yes | Yes |
| L37 External APIs | Resilience Pipeline (timeout/retry/circuit gates) | Yes | Yes |
| L38 Webhooks | Webhook Event Queue (vertical gates) | Yes | Yes |
| L39 API Versioning | Version Router (fork/split layout) | Yes | Yes |

**Total Act 5 tests: 381 tests, 1442 assertions across 8 files. All passing.**

## Bug Fix: L32/L33 Crash (standardPipeline missing parentheses)

**Problem:** L32 crashed with `TypeError: Cannot read properties of undefined (reading 'map')` at `level.startingPipeline.nodes.map()`. Root cause: `startingPipeline: standardPipeline,` passed the function reference instead of calling it.

**Fixed:**
- L32 (content.ts line 28): `standardPipeline` -> `standardPipeline()`
- L33 (content.ts line 193): `standardPipeline` -> `standardPipeline()` (same bug)
- Verified no other occurrences across the codebase
- Build passes successfully

## Fix: Terminal Edge-to-Edge Inconsistency (All Act 5 Levels)

**Problem:** In Act 4 levels, ProbeTerminal and StressTestPanel are wrapped in `<div className="px-6 pb-2">` giving them padding from panel edges. Act 5 levels had terminals as direct flex children with no wrapper, causing them to stretch edge-to-edge.

**Fixed all 8 Act 5 level components (L32-L39):**
- Wrapped both ProbeTerminal and StressTestPanel in `<div className="px-6 pb-2">` wrapper
- Removed `className="shrink-0"` from L32, L33 terminals (not needed with wrapper)
- Replaced `<div className="flex-1 flex flex-col min-h-0">` wrapper + `className="flex-1 flex flex-col"` in L34, L35 with `<div className="px-6 pb-2">`
- L36-L39: added `<div className="px-6 pb-2">` wrapper around terminals that were direct children
- All 381 tests pass, build succeeds

## L32 Polymorphic: Convert to Type 2 Static Intro

**Problem:** L32's observe phase had probes, discovery gating, stage inspectors, and animations for a code-structure problem (duplicate schemas). Polymorphic associations are a schema duplication issue that's self-evident from reading the code, not a runtime behavior. This is a Type 2 level (like L16 Service Objects), not Type 3.

**Converted from Type 3 (interactive observe) to Type 2 (static intro):**
- Phase type: `'observe'` -> `'intro'`
- Removed all observe-phase infrastructure:
  - Removed imports: DiscoveryChecklist, ProbeTerminal, StageInspector, useDiscoveryGating, Search icon
  - Removed data maps: DISCOVERY_DEFS, PROBES, PROBE_DISCOVERY_MAP, STAGE_DISCOVERY_MAP, STAGE_INSPECTOR_MAP
  - Removed state: discoveryGating hook, animatingProbe, animStep, columnsHighlighted, queryErrorShown, showArticleTable, inspectorData
  - Removed handlers: handleProbe, handleStageClick
  - Removed render functions: renderTableCard, renderObserveVisualization
- Replaced with static annotated schema display:
  - 3 database table cards showing column chips (shared columns in red, unique FK in table color)
  - Problem banner ("3 Separate Comment Tables")
  - Duplication callout explaining 4 of 5 columns are identical
  - "Build the Fix" button always visible (no gating)
- Simplified data: COMMENT_TABLES (with rows) -> DUPLICATE_TABLES (name + fkColumn + rows)
- Simplified handleStartBuild: removed references to deleted state variables
- Updated left panel: replaced DiscoveryChecklist with explanatory text, updated instructions
- Build passes, Biome clean

**Intro visualization iterations:**
- Replaced column chips with proper database table grids (column headers + sample data rows)
- Added `created_at` column to match "4 of 5 columns identical" callout
- Added `overflow-x-auto` on each table card for horizontal scrolling on small screens
- Added `min-h-0` on flex container to fix center panel scrollability in build phase

**Reward phase: removed StressTestPanel, converted to static before/after:**
- Type 2 levels use static before/after, not interactive stress testing
- Removed: STRESS_SCENARIOS, useStressTest, StressTestPanel, handleFireScenario, handleToggleAutoFire, vizAnimating, rewardHighlight, animTimerRef, ANIMATION_DURATION_MS
- Reward now shows "Before: 3 Separate Tables" (compact, dimmed) with arrow pointing to "After: 1 Unified Table" (full grid with polymorphic columns highlighted in green)
- Unified table includes Article type (4th row) to show extensibility
- Simplified validateSolution (no stress test count check)
- Updated left panel reward section: explanatory text instead of counters
- Tests updated: replaced stress scenario tests with reward table consistency tests
- 26 tests, 128 assertions. Build passes, Biome clean
