# Session Log: 2026-03-15

## Act 5 Sequential Rewrite (Continued)

Continuing the sequential rewrite of Act 5 levels (L32-L39). L32-L37 were completed in the previous session. This session continues with L38 and L39.

### Level 38: Webhooks & Idempotency (Rewrite Complete)

**Replaced:** 546-line legacy toggle-based webhook simulator (WebhookConfig toggles, setTimeout processing, no three-phase flow)

**New component:** ~840 lines with full three-phase flow

#### Visualization: "Webhook Event Queue" (Type 3 custom, vertical top-to-bottom)
- Incoming Stripe Webhook at top -> Signature Gate -> Idempotency Gate -> Processing Gate -> Response at bottom
- Vertically stacked gates with FlowConnectors between each
- Observe: all gates show "absent"/"sync" (gray), events flow through unprotected
- Reward: gates show active states, rejected events blocked at signature gate, duplicates caught at idempotency gate, valid events enqueued async
- Visually distinct from L36 (side-by-side lanes), L37 (horizontal gate pipeline)

#### Discoveries (4):
- no-signature, duplicate-credit, sync-timeout, no-dedup

#### Probes (3):
- Forged webhook (no signature), duplicate event_id, slow synchronous handler

#### Build Steps (6):
1. Generate webhook_events migration (terminal)
2. Run migration (terminal) - follows migration generation immediately
3. Configure signature verification (option) - Stripe::Webhook.construct_event
4. Configure idempotency check (option) - atomic find_or_create_by with unique index
5. Configure async processing (option) - background job via Solid Queue
6. Build webhook handler service (option) - IngestStripeWebhook < ApplicationService

#### Stress Scenarios (6):
- 4 allowed: valid payment, valid subscription, duplicate (deduped gracefully), valid refund
- 2 blocked: forged event (bad signature), malformed JSON

#### Cumulative Patterns:
- IngestStripeWebhook < ApplicationService with Result = Data.define(:success?, :webhook_event, :errors)
- Controller delegates to service: IngestStripeWebhook.call(payload:, signature:)
- Background job: ProcessStripeWebhookJob with retry_on and polynomially_longer backoff
- Wrong "controller logic" option flagged for service object pattern violation

#### Content.ts Updates:
- Renamed variable: level35Webhooks -> level38Webhooks
- Fixed section comment: "Level 35" -> "Level 38"
- Added requiresTests: true

#### Tests: 43 tests, 184 assertions
- Discovery defs, probes, build step quality, stress scenarios, cross-phase consistency, cumulative compliance, data consistency
- Fixed: wrong service option feedback revealed "find_or_create_by!" (correct answer API)

#### Biome Fixes:
- Removed unused imports: ListChecks, Lock, MailWarning, ShieldAlert
- Removed unused state: lastProbeId
- Changed onComplete to _props for unused parameter

### Level 39: API Versioning (Rewrite Complete)

**Replaced:** 769-line legacy toggle/radio-based version picker (VersioningConfig, strategy selector, side-by-side endpoint comparison, no three-phase flow)

**New component:** ~1350 lines with full three-phase flow

#### Visualization: "Version Router" (Type 3 custom, fork/split layout)
- Two client boxes (v1, v2) on left -> central Router node -> two branching controller paths (v1 + v2)
- Observe: single controller path, no versioning. Router shows "No Split" in red
- Reward: Router splits correctly. v1 path shows "Deprecated + Sunset" (amber), v2 shows object format (green)
- Unknown versions (v3, unversioned) show both paths dimmed with 404
- Visually distinct from L36 (lanes), L37 (horizontal gates), L38 (vertical gates)

#### Discoveries (4):
- single-controller, breaking-change, no-deprecation, no-migration-path

#### Probes (3):
- v1 client expects cents, v2 client wants object, breaking deploy

#### Build Steps (6):
1. Add v2 namespace to routes (terminal)
2. Generate v2 controller (terminal)
3. Create v2 serializer with new response shape (option)
4. Add deprecation headers to v1 (option)
5. Add Sunset header with future date (option)
6. Wire v1 controller to use service delegation + versioned serializer (option)

#### Stress Scenarios (6):
- 4 allowed: v1 cents, v2 object, v1 deprecated, v2 with line_items
- 2 blocked: v3 not found, unversioned 404

#### Cumulative Patterns:
- FetchOrder.call(id:) service delegation in both v1 and v2 controllers
- Versioned serializers: Api::V1::OrderSerializer and Api::V2::OrderSerializer
- Error shape: { error: { code: "NOT_FOUND", message: ... } }
- Wrong "direct render" option flagged for bypassing service layer

#### Content.ts Updates:
- Renamed variable: level36APIVersioning -> level39APIVersioning
- Fixed section comment: "Level 36" -> "Level 39"
- Added requiresTests: true

#### Tests: 40 tests, 162 assertions
- Discovery defs, probes, build step quality, stress scenarios, cross-phase consistency, cumulative compliance, data consistency

## Act 5 Rewrite Summary

All 8 levels (L32-L39) now have full three-phase flow implementations:

| Level | Visualization | Tests | Assertions |
|-------|--------------|-------|------------|
| L32 Polymorphic | Schema Split (3 tables -> 1) | Yes | Yes |
| L33 Transactions | Race Timeline (concurrent writes) | Yes | Yes |
| L34 Active Storage | Upload Pipeline (file -> variants) | Yes | Yes |
| L35 Encryption | Field Encryption (plaintext -> cipher) | Yes | Yes |
| L36 Real-Time | Connection Comparison (polling vs WebSocket) | Yes | Yes |
| L37 External APIs | Resilience Pipeline (timeout/retry/circuit gates) | Yes | Yes |
| L38 Webhooks | Webhook Event Queue (vertical gates) | Yes | Yes |
| L39 API Versioning | Version Router (fork/split layout) | Yes | Yes |

**Total Act 5 tests: 381 tests, 1442 assertions across 8 files. All passing.**

## Bug Fix: L32/L33 Crash (standardPipeline missing parentheses)

**Problem:** L32 crashed with `TypeError: Cannot read properties of undefined (reading 'map')` at `level.startingPipeline.nodes.map()`. Root cause: `startingPipeline: standardPipeline,` passed the function reference instead of calling it.

**Fixed:**
- L32 (content.ts line 28): `standardPipeline` -> `standardPipeline()`
- L33 (content.ts line 193): `standardPipeline` -> `standardPipeline()` (same bug)
- Verified no other occurrences across the codebase
- Build passes successfully

## Fix: Terminal Edge-to-Edge Inconsistency (All Act 5 Levels)

**Problem:** In Act 4 levels, ProbeTerminal and StressTestPanel are wrapped in `<div className="px-6 pb-2">` giving them padding from panel edges. Act 5 levels had terminals as direct flex children with no wrapper, causing them to stretch edge-to-edge.

**Fixed all 8 Act 5 level components (L32-L39):**
- Wrapped both ProbeTerminal and StressTestPanel in `<div className="px-6 pb-2">` wrapper
- Removed `className="shrink-0"` from L32, L33 terminals (not needed with wrapper)
- Replaced `<div className="flex-1 flex flex-col min-h-0">` wrapper + `className="flex-1 flex flex-col"` in L34, L35 with `<div className="px-6 pb-2">`
- L36-L39: added `<div className="px-6 pb-2">` wrapper around terminals that were direct children
- All 381 tests pass, build succeeds

## L32 Polymorphic: Convert to Type 2 Static Intro

**Problem:** L32's observe phase had probes, discovery gating, stage inspectors, and animations for a code-structure problem (duplicate schemas). Polymorphic associations are a schema duplication issue that's self-evident from reading the code, not a runtime behavior. This is a Type 2 level (like L16 Service Objects), not Type 3.

**Converted from Type 3 (interactive observe) to Type 2 (static intro):**
- Phase type: `'observe'` -> `'intro'`
- Removed all observe-phase infrastructure:
  - Removed imports: DiscoveryChecklist, ProbeTerminal, StageInspector, useDiscoveryGating, Search icon
  - Removed data maps: DISCOVERY_DEFS, PROBES, PROBE_DISCOVERY_MAP, STAGE_DISCOVERY_MAP, STAGE_INSPECTOR_MAP
  - Removed state: discoveryGating hook, animatingProbe, animStep, columnsHighlighted, queryErrorShown, showArticleTable, inspectorData
  - Removed handlers: handleProbe, handleStageClick
  - Removed render functions: renderTableCard, renderObserveVisualization
- Replaced with static annotated schema display:
  - 3 database table cards showing column chips (shared columns in red, unique FK in table color)
  - Problem banner ("3 Separate Comment Tables")
  - Duplication callout explaining 4 of 5 columns are identical
  - "Build the Fix" button always visible (no gating)
- Simplified data: COMMENT_TABLES (with rows) -> DUPLICATE_TABLES (name + fkColumn + rows)
- Simplified handleStartBuild: removed references to deleted state variables
- Updated left panel: replaced DiscoveryChecklist with explanatory text, updated instructions
- Build passes, Biome clean

**Intro visualization iterations:**
- Replaced column chips with proper database table grids (column headers + sample data rows)
- Added `created_at` column to match "4 of 5 columns identical" callout
- Added `overflow-x-auto` on each table card for horizontal scrolling on small screens
- Added `min-h-0` on flex container to fix center panel scrollability in build phase

**Reward phase: removed StressTestPanel, converted to static before/after:**
- Type 2 levels use static before/after, not interactive stress testing
- Removed: STRESS_SCENARIOS, useStressTest, StressTestPanel, handleFireScenario, handleToggleAutoFire, vizAnimating, rewardHighlight, animTimerRef, ANIMATION_DURATION_MS
- Reward now shows "Before: 3 Separate Tables" (compact, dimmed) with arrow pointing to "After: 1 Unified Table" (full grid with polymorphic columns highlighted in green)
- Unified table includes Article type (4th row) to show extensibility
- Simplified validateSolution (no stress test count check)
- Updated left panel reward section: explanatory text instead of counters
- Tests updated: replaced stress scenario tests with reward table consistency tests
- 26 tests, 128 assertions. Build passes, Biome clean

## Audit: Level 35 Encryption (Pass)

Audited L35 against the updated audit-level skill with Type 2 litmus test and cross-phase consistency checks.

**Type justification:** Encryption transforms plaintext to ciphertext at the database storage layer, which is a runtime data transformation visible when probes fire (SQL injection dumps plaintext, backup leaks show plaintext). This is genuine runtime behavior, not code structure.

**Type 2 litmus test result:** All three questions answer "no":
1. Can the problem be understood from code side-by-side? No, the impact (what attackers see) requires demonstrating runtime behavior.
2. Is it about code organization/duplication? No, it's about data security at rest.
3. Would probes feel forced? No, SQL injection and backup leak probes naturally reveal the vulnerability.

**Verdict: Type 3 is correct.**

**Cross-phase consistency: Pass.** Observe uses `renderDatabaseTable(false)` (plaintext, red) + ProbeTerminal. Reward uses `renderDatabaseTable(true)` (encrypted, green) + StressTestPanel. Same visual component, different state. Correct for Type 3.

**Build step quality: Pass.**
- Correct answers never first in any step (all at index 1)
- Feedback never reveals correct answers
- Each step has exactly one correct answer
- All wrong options have feedback

**Other checks: Pass.**
- No em dashes
- No Biome/lint issues
- Visualization unique (database table grid, distinct from L34 upload pipeline and L36 connection comparison)
- ANIMATION_DURATION_MS imported from shared lib
- ProbeTerminal and StressTestPanel have `disabled={flowPhase !== -1}`
- Code previews use cumulative patterns (ApplicationService, Data.define, dry-validation contracts)
- All 42 tests pass (127 assertions)

**No changes needed.**

## Audit: Level 39 API Versioning (Pass)

Audited L39 against the updated audit-level skill (Type 2 litmus test, mandatory type justification, cross-phase consistency).

**Type justification:** API versioning has clear runtime behavior to animate: requests being routed to v1 vs v2 controllers, deprecation/Sunset headers being added, unknown version paths returning 404. This is Type 3 (custom visualization), correctly implemented.

**Type 2 litmus test:**
1. Can the problem be understood from code side-by-side? Not fully: the breaking impact requires seeing requests from different clients hitting the same endpoint and receiving incompatible responses.
2. Is it about code organization? No, it's about request routing behavior and response contract compatibility.
3. Would probes feel forced? No: probing v1/v2/broken-deploy requests is the natural way to demonstrate the problem.

**Cross-phase consistency:** Pass. Both observe and reward use the same `renderVersionRouter()` function (same visual component, different state). Observe shows "No Split" in red, reward shows "Router" in green with split paths. Same visual language throughout.

**Build step quality:** All correct answers are never first. Feedback never reveals answers. All wrong options have distinct teaching feedback.

**No changes needed.** Level passes all audit checks.

## Audit-Level Skill Update: Type 2 vs Type 3 Forcing Function

**Problem:** When building L32, the Type 3 pattern (probes, discoveries, animations, StressTestPanel) was mechanically applied because adjacent Act 5 levels used it, without first asking whether the concept had runtime behavior to animate. The decision flowchart existed but lacked a forcing function.

**Changes to `.claude/skills/audit-level/SKILL.md`:**

1. **Added mandatory type justification step** after the decision flowchart: requires a one-sentence answer to "What runtime behavior does this level need to animate?" before proceeding. If the answer is "none," it's Type 2.

2. **Added "pattern matching" trap warning**: explicitly warns against choosing Type 3 just because adjacent levels use it.

3. **Added Type 2 litmus test**: three yes/no questions that, if any answer "yes," confirm the level is Type 2 (can problem be understood from code side-by-side? is it about code organization? would probes feel forced?).

4. **Added L32 case study**: concrete example of a level that was incorrectly built as Type 3 and why Type 2 is correct.

5. **Restructured Phase 3 (Reward)**: split into Type 2 (static before/after) and Types 3/4 (interactive StressTestPanel). Previously the section said "reward MUST be interactive" with no Type 2 exception, making it easy to mechanically add StressTestPanel to every level.

6. **Added L32 reward case study**: shows what a Type 2 reward looks like (before: 3 tables dimmed, after: 1 unified table with green highlights).

7. **Strengthened cross-phase consistency check**: the Type 2 reward check now explicitly calls out "If a Type 2 level has StressTestPanel, that is a bug."

### Skill Update Round 2: Cross-Phase Consistency for Type Conversions

**Problem:** When L32 was converted from Type 3 to Type 2, only the intro phase was updated (static tables). The reward phase was left with StressTestPanel from the old Type 3 implementation, creating a visual mismatch: static tables in intro, terminal in reward.

**Changes:**

1. **SKILL.md cross-phase checklist** (line 433): Expanded "Visual language is consistent across phases" to explicitly state that mixing styles (static tables in intro, terminal in reward) is a cross-phase inconsistency.

2. **SKILL.md Step Quality** (line 429): Changed blanket "The reward phase is interactive" to "The reward phase matches the level type" with type-specific guidance.

3. **cross-phase-consistency.md**: Added L32 case study documenting the mismatched intro/reward problem, explaining why it happened (only half the conversion was done), and providing a checklist for type conversions that ensures both phases are updated together, including checking for leftover imports from the previous type.

## Audit: Level 37 External APIs (One Fix)

Audited L37 against the updated audit-level skill with Type 2 litmus test, cross-phase consistency, and build step quality checks.

**Type justification:** External API resilience involves animating runtime behavior: requests hanging without timeouts, retries being attempted with backoff, circuit breakers tripping open after repeated failures. These are network-level runtime mechanisms.

**Type 2 litmus test result:** All three questions answer "no":
1. Can the problem be understood from code side-by-side? No, the impact (thread exhaustion, cascade failure) is runtime.
2. Is it about code organization/duplication? No, it's about runtime resilience patterns.
3. Would probes feel forced? No, firing requests against slow/failing APIs naturally reveals the problem.

**Verdict: Type 3 is correct.**

**Cross-phase consistency: Pass.** Observe uses `renderResiliencePipeline(false)` + ProbeTerminal. Reward uses `renderResiliencePipeline(true)` + StressTestPanel. Same visual component, different state (inactive gates vs active gates). Probe and scenario labels use the same format (e.g., "POST charge (Stripe slow)" / "POST charge (slow, timeout)").

**Build step quality: Pass.**
- Correct answers never first in any of the 6 steps
- Feedback never reveals correct answers (checked: no "faraday", "stoplight", "open_timeout" in feedback)
- Each step has exactly one correct answer
- All wrong options have meaningful, distinct feedback
- Step labels generic ("Install HTTP Client", not "Install Faraday")

**Bug found and fixed:**
- `onComplete` was never called. The player could complete all phases but had no way to finish the level. Added a Submit button to the reward left panel that calls `onComplete({ stars: stepper.starRating })`.

**Other checks: Pass.**
- No em dashes
- No Biome/lint issues
- Animation locking: ProbeTerminal disabled during flowPhase, StressTestPanel disabled during rewardFlowPhase
- Code previews use cumulative patterns (ApplicationService, Data.define, PaymentContract)
- All 57 tests pass (229 assertions)

**File changed:** `frontend/src/features/act5-production/components/Level37ExternalAPIs.tsx` (added Submit button in reward left panel)

## Audit: Level 38 Webhooks & Idempotency (Two Fixes)

Audited L38 against the updated audit-level skill with Type 2 litmus test, mandatory type justification, cross-phase consistency, and build step quality checks.

**Type justification:** Webhooks involve incoming HTTP events flowing through gates (signature verification, idempotency check, async dispatch). Forged events get rejected at the signature gate, duplicates caught at the idempotency gate, valid events enqueued asynchronously. This is genuine runtime behavior.

**Type 2 litmus test result:** All three questions answer "no":
1. Can the problem be understood from code side-by-side? No, the impact (duplicate credits, forged events) requires runtime demonstration.
2. Is it about code organization/duplication? No, it's about missing security/reliability gates.
3. Would probes feel forced? No, firing forged/duplicate/slow webhooks naturally reveals each vulnerability.

**Verdict: Type 3 is correct.**

**Cross-phase consistency: Pass.** Both observe and reward use the same `renderWebhookQueue()` function. Observe passes `isReward=false` (red, gates absent), reward passes `isReward=true` (green, gates active). Same visual component, different state.

**Build step quality: Two fixes needed.**
1. `wrong-find-by` feedback in CONFIGURE_IDEMPOTENCY_OPTIONS revealed the correct answer: "Use find_or_create_by with a unique index instead." Changed to: "You need an atomic operation backed by a database constraint."
2. `wrong-controller-logic` feedback in BUILD_SERVICE_OPTIONS revealed the answer: "Extract verification, dedup, and dispatch into a service." Changed to: "Complex multi-step operations belong in a dedicated object."

**Test updates:**
- Mirrored feedback strings updated to match component
- Added `find_or_create_by` (without bang) to feedback-reveals-answer checks
- Added `Extract verification` to forbidden feedback strings
- Updated cumulative compliance test: `service object` -> `dedicated object`
- All 43 tests pass (200 assertions)

**Other checks: Pass.**
- Correct answers never first in any step
- No em dashes
- No Biome/lint issues
- Animation locking: ProbeTerminal disabled during flowPhase, StressTestPanel disabled during rewardFlowPhase
- Code previews use cumulative patterns (ApplicationService, Data.define)
- ANIMATION_DURATION_MS imported from shared lib

**Files changed:**
- `frontend/src/features/act5-production/components/Level38Webhooks.tsx` (2 feedback fixes)
- `frontend/src/features/act5-production/__tests__/Level38Webhooks.test.ts` (mirrored data + stronger answer-leak checks)

## Audit: Level 36 Real-Time (Three Fixes)

Audited L36 against the updated audit-level skill with Type 2 litmus test, mandatory type justification, cross-phase consistency, and build step quality checks.

**Type justification:** Real-time/WebSockets involves animating runtime behavior: HTTP polling creating 25K wasteful request/response cycles per second vs WebSocket persistent connections with instant server-push. The visualization shows two lanes side-by-side with animated request arrows (polling waste) vs push messages.

**Type 2 litmus test result:** All three questions answer "no":
1. Can the problem be understood from code side-by-side? No, the waste (25K empty responses/sec, CPU at 95%) is runtime.
2. Is it about code organization/duplication? No, it's about replacing a network pattern (polling vs push).
3. Would probes feel forced? No, firing probes to see empty poll responses and missing push mechanisms is natural.

**Verdict: Type 3 is correct.**

**Cross-phase consistency: Pass.** Both observe and reward use the same `renderConnectionComparison()` function with isReward flag. Observe shows HTTP Polling lane active (red) + WebSocket lane inactive (gray). Reward shows HTTP Polling lane labeled "Before: Replaced" + WebSocket lane active (green). Same visual component, different state. Probe and scenario labels are consistent format.

**Build step quality: Pass.**
- Correct answers never first in any of the 6 steps
- Feedback never reveals correct answers (checked: no "solid_cable", "encrypted cookies", "after_create_commit" in feedback)
- Each step has exactly one correct answer
- All wrong options have meaningful, distinct feedback
- Step labels generic ("Install Cable Adapter", not "Install Solid Cable")

**Bugs found and fixed (3):**
1. **Missing completion mechanism (critical):** No `LevelHeader`, no `handleComplete`, no `handleValidate`, no Submit button. The level could never be completed. Added `LevelHeader` to CenterPanel with proper `onComplete`, `onValidate`, and `onReset` handlers.
2. **Dead state variable:** `_lastProbeId` state was declared and set but never read. Removed the state entirely (kept the setter calls removed too).
3. **Biome lint error (pre-existing):** `Array.from({ length: 6 }).map((_, i) => ...)` used array index as key. Changed to string array `['req-1', ..., 'req-6'].map(id => ...)` with stable string keys.

**Other checks: Pass.**
- No em dashes
- Animation locking: ProbeTerminal disabled during flowPhase, StressTestPanel disabled during rewardFlowPhase
- Code previews use cumulative patterns (ApplicationService, Data.define, dry-validation contracts)
- ANIMATION_DURATION_MS imported from shared lib
- All 64 tests pass (243 assertions)

**File changed:** `frontend/src/features/act5-production/components/Level36RealTime.tsx`

## Audit: Level 33 Transactions & Locking (Three Fixes)

Audited L33 against the updated audit-level skill with Type 2 litmus test, mandatory type justification, cross-phase consistency, and build step quality checks.

**Type justification:** Transactions involve race conditions between concurrent database writes: two users reading the same stale balance, writing back conflicting values, causing lost updates. This is runtime behavior (data races, timing-dependent outcomes) that cannot be understood from code alone.

**Type 2 litmus test result:** All three questions answer "no":
1. Can the problem be understood from code side-by-side? No, the code looks fine in isolation; the bug manifests only with concurrent execution.
2. Is it about code organization/duplication? No, it's about what happens at runtime with concurrent access.
3. Would probes feel forced? No, firing concurrent deductions is exactly how you expose the bug.

**Verdict: Type 3 is correct.**

**Cross-phase consistency: Pass.** Both observe and reward use the same 3-column grid layout (User A, Account, User B) with FlowConnectors and color-coded states. Observe shows red/danger states, reward shows green/success states. Same visual language throughout.

**Bugs found and fixed (3):**

1. **Feedback reveals correct answer (LOCK_VERSION_COMMANDS):** Both wrong options' feedback mentioned "lock_version column" and "integer" by name, which is exactly the correct migration command. Changed:
   - wrong-boolean: "Rails uses an integer lock_version column..." -> "Optimistic locking needs a column that tracks how many times a record has been saved."
   - wrong-timestamp: "Rails optimistic locking uses an integer lock_version column..." -> "Rails needs an auto-incrementing counter to detect exact version mismatches."

2. **Missing `rails db:migrate` step:** The audit skill requires that every migration generation is immediately followed by a `rails db:migrate` step. Step 0 generated the migration but Step 1 jumped straight to wrapping in a transaction. Added new Step 1 "Run Migration" with MIGRATE_COMMANDS (3 options: wrong db:seed, wrong db:reset, correct db:migrate) and MIGRATE_OUTPUT. Shifted all subsequent step indices (option steps now at indices 2-5 instead of 1-4). Updated code preview function, OPTION_STEP_CONFIG, TERMINAL_STEP_MAP, isTerminalStep check, build instructions, and file header comment.

3. **Hint text reveals answer (content.ts):** Hint said "Use Account.transaction with Account.lock.find to prevent lost updates on balances," revealing the exact API calls for build steps 2-3. Changed to: "Think about what happens when two processes read the same row. What mechanism can serialize their writes?"

**Test updates:**
- Mirrored STEP_DEFS updated to 6 steps (added run-migration)
- Added MIGRATE_COMMANDS to test data
- Updated step count test: 5 -> 6
- Updated step order test: added run-migration before wrap-transaction
- Updated step position test: terminal steps 0-1, option steps 2-5
- Added "correct migrate command is never first" test
- Added MIGRATE_COMMANDS to all-step-options test arrays
- Updated feedback-reveals-answer check to also forbid "lock_version column" and "rails db:migrate"
- Mirrored feedback text updated to match component
- All 46 tests pass (199 assertions)

**Files changed:**
- `frontend/src/features/act5-production/components/Level33Transactions.tsx` (3 fixes)
- `frontend/src/features/act5-production/content.ts` (hint text + goal text)
- `frontend/src/features/act5-production/__tests__/Level33Transactions.test.ts` (mirrored data + new tests)
