# Session 2026-07-14

## Docs sync to fully-static, client-side app

The app was pivoted from an Astro + Cloudflare Workers (Hono API, better-auth, D1) stack to a fully static, MIT open-source site. Code changes were already done; this session updated `docs/*.md` to match.

### Changes

- **Deleted** `docs/api-reference.md` (`git rm`): documented the removed REST/Hono API. No API exists anymore.
- **Deleted** `docs/database-schema.md` (`git rm`): documented the removed D1/SQLite schema. No database exists.
- **Rewrote** `docs/deployment-guide.md`: replaced the Cloudflare Workers / wrangler / D1 deploy content with a short static-hosting guide (`bun run build` -> `dist/` -> GitHub Pages / Netlify / Cloudflare Pages / `bunx serve dist`). No env vars, secrets, or database. Progress is client-side `localStorage`.
- **Rewrote** `docs/architecture.md`: removed all server/API/D1/auth sections. Describes the static Astro site, React islands, static routing via `getStaticPaths()` / `getActLevelStaticPaths()`, the registries (acts/levels/codebase), Zustand stores, `localStorage` progress (key `interactive_rails_progress_v1`), and the sandbox simulation engine.
- **Rewrote** `docs/development-setup.md`: dropped wrangler/D1/better-auth/env-var steps. Setup is now install Bun, `bun install`, `bun run dev`. Kept real script names from `package.json` and the check suite (`tsc --noEmit`, `biome check .`, `bun test`, `test:e2e`, `check-types`).
- **Rewrote** `docs/troubleshooting.md`: removed backend/D1/wrangler/auth/CORS/JWT entries. Kept/added front-end ones: port in use, hot reload, hydration, TS errors, React Flow blank canvas, `localStorage` progress, level-not-found (static routing), build failures.
- **Updated** `docs/README.md`: dropped the deleted files from the index, fixed the tech-stack table (removed Cloudflare/Hono/D1/better-auth rows), fixed key directories (removed `src/server/`), and updated Quick Start and Important Notes.

### Verification

- `grep -rln "Cloudflare\|Hono\|D1\|wrangler\|better-auth\|/api/pipeline\|/api/auth"` over `docs/` returns only: `deployment-guide.md` and `architecture.md` (both mention Cloudflare **Pages** only as a static file host, explicitly stating no Worker/D1/wrangler), plus the historical session log `2026-07-09.md`. No current-feature backend references remain.
- No em dashes in any touched file.
- Verified against the live codebase: `astro.config.mjs` is `output: 'static'` with no adapter; `src/server` and `src/pages/api` do not exist; deps confirmed removed (zod/tanstack/nanostores/hono/better-auth/cloudflare/wrangler absent from `package.json`); stores are `game.ts`, `pipeline.ts`, `ui.ts` (no simulation store); progress key is `interactive_rails_progress_v1`.

### Learned

- Cloudflare Pages is still a valid deploy target as a pure static host, so keeping that mention is correct as long as it is clear there is no Worker/D1/wrangler backing it.
- Pruned the oldest session log (`2026-05-01.md`) to keep the max-10 cap when creating today's log.

## Open-source pivot: fully client-side, no backend (the full change)

The owner decided to open-source the project (MIT) as a free, client-side-only app: all progress in localStorage, no server, no account.

### Backend removed
Deleted `src/server/` (Hono API, repositories, better-auth, D1 schema), `src/pages/api/`, `wrangler.jsonc`, the auth pages/stores/client (`login.astro`, `signup.astro`, `authStore.ts`, `auth-client.ts`, `components/auth/`), the empty `battle/` page, and the unused `IncidentFeed` component. IncidentFeed also held the pure incident-generator helpers the sandbox simulation uses; those were extracted to a React-free `src/lib/incidents.ts` and the import in `usePipelineSimulation.ts` was repointed.

### Progress went local-first (TDD)
`src/lib/progress.ts` rewritten: a pure `mergeCompletion` over an injectable `ProgressStorage`, keep-max stars/score, corrupted-JSON recovery, the legacy storage key kept (`interactive_rails_progress_v1`) so existing players keep progress. 10 new tests written first (red), then the implementation. Call sites rewired: `useLevelCompletion`, `LevelPlayApp` (dropped the server-only `timeToComplete`/`finalMetrics`), `ActsListApp` (removed the guest/account distinction and banner), `DashboardCodebaseButton`. The dashboard was rewritten to render real local data (completed count, stars, chosen database) client-side, dropping the server-only XP/level/title/playtime gamification. The header lost its auth block and now links to Progress.

### Static build
`astro.config.mjs` -> `output: 'static'`, no adapter. The `[actId]/[levelId]` routes gained `getStaticPaths()` (new `getActLevelStaticPaths()` in the registry). The complete page reads its `?stars=N` client-side (static prerender cannot see runtime query strings). `bun run build` now emits 178 static pages to `dist/`, hostable anywhere.

### Deps + config pruned
Removed `@astrojs/cloudflare`, `hono`, `@hono/zod-validator`, `better-auth`, `nanostores`, `@nanostores/react`, `@tanstack/react-query`, `zod` (all unused after backend removal), and `@cloudflare/workers-types`. Dropped the `deploy`/`db:migrate` scripts and the miniflare override. `tsconfig.json` no longer pins workers-types. Removed tracked backend env files (`.env.example` documenting JWT/D1/Workers vars, `.env.development`) and local Cloudflare artifacts; confirmed `.env` was never tracked, so no real secret ever leaked.

### Docs + wording
README rewritten for the open-source, static, free framing with the real repo URL (github.com/kcsujeet/interactive-rails). MIT LICENSE added (copyright Sujeet Kc). AGENTS.md (CLAUDE.md symlink) Bun Reference section rewritten to "no backend; localStorage only." Docs agent deleted `api-reference.md` and `database-schema.md` (documented the gone backend), rewrote `deployment-guide.md` (static hosting), `architecture.md`, `development-setup.md`, `troubleshooting.md`, and the docs index.

### Copyrighted book
`docs/rails-scales-book/` (a full copyrighted PDF + derivative summaries) removed from the working tree. It is ALREADY on the public GitHub remote in three commits (3c25bad, c243e0a, f814e11), so a history purge + force-push is required to scrub it (pending owner approval of the public-history rewrite).

### Checks
tsc clean, biome clean (333 src+scripts files), 1827 unit tests pass (10 new), 178-page static build, 59 e2e pass. No em dashes.

## History purge executed (owner approved)

Committed the pivot (3fd021f). Backed up all refs to /tmp/ir-prepurge.bundle first. Ran git-filter-repo (standalone script, no global install): `git filter-repo --path docs/rails-scales-book/ --invert-paths --force`. Verified 0 commits reference the book path and no book objects are reachable. Re-added origin and force-pushed the rewritten main (1850d95 -> 9214a7c). Remote tip confirmed 9214a7c; build (178 pages) and 1827 unit tests green after the rewrite.

REMAINING MANUAL STEP FOR THE OWNER: GitHub caches unreachable commits, so the PDF may still be reachable on github.com by its old commit SHA until GitHub garbage-collects. To force that, open a GitHub Support request asking them to purge the cached/unreachable objects for kcsujeet/interactive-rails. Also: anyone who cloned/forked before the rewrite still has the old history; force-pushed history means their next pull will diverge.

## Follow-ups: CI fix, dead-code sweep, fallow gate

- CI was red since 2026-07-05 (independent of the pivot): the "Run tests" step ran bare `bun test`, which swept in the Playwright e2e specs and errored. Changed it to `bun run test` (scoped `bun test src scripts`). Commit 977afa6.
- Removed the root `shared/` dir (orphaned client/worker types).
- Ran fallow (fallow-rs, TS/JS dead-code tool) and removed a verified dead cluster (16 files, ~3.5k lines): the old `src/components/pipeline/` implementation (superseded by `src/components/PipelineCanvas.tsx`), the entire Zustand `src/stores/` layer (no live importer; `usePipelineState` is plain useState), RPG leftovers (HealthBar, XPBar, ScenarioCards), the duplicate `pages/SandboxApp.tsx`, and `lib/test-setup.ts`. Each verified by hand against real importers including `.astro` pages, the dynamic level registries, and test config (fallow follows `.astro` component imports but NOT CSS imports, which is its only blind spot here). Commit d93c72c.
- Removing `stores/` orphaned `zustand` and `immer`; fallow's dependency check caught it, so both were removed from package.json.
- Added a fallow CI gate: `fallow-rs/fallow@v3` with `command: audit` (changed-file gate that fails only on newly INTRODUCED dead code, not the legacy unused-export backlog). Checkout bumped to `fetch-depth: 0` for the base-ref diff. `.fallowrc.json` ignores the CSS-import false positives (`src/styles/**`) and the two intentional standalone scripts (`dump-level-ids.ts`, `fix-imports.ts`). Local `fallow audit` verdict: pass (exit 0, "No issues in 20 changed files").

Known non-blocking fallow findings left as-is: 2 font packages flagged unused (imported via CSS `@import` in global.css, a fallow blind spot) and `shadcn` (a CLI tool used via `bunx shadcn@latest`). All excluded from the changed-file gate. Also ~150 unused exports (barrel re-exports, shadcn UI sub-exports, registry helpers) left as backlog; the gate prevents them from growing.

NOTE: pushing is still blocked on the `gh` token missing the `workflow` scope, since these commits modify `.github/workflows/ci.yml`. Run `gh auth refresh -h github.com -s workflow`, then push.
