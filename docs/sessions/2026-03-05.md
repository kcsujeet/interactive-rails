# Session Log: 2026-03-05

## Session 1: Redesign L15 CORS Visualization

### What Changed

**File:** `frontend/src/features/act2-users-security/components/Level15CORS.tsx`

Redesigned the L15 CORS visualization from a broken layout to an accurate 3-zone horizontal flow.

**Before (problems):**
- Layout: Browser (left) | Origin Boundary (narrow w-20 divider) | Server Tower (right, 3 stacked boxes: Preflight OPTIONS, CORS Middleware, Rails API)
- Preflight was a separate clickable server-side box (it's actually a browser-sent request type)
- CORS Middleware sat between Preflight and Rails API as a middle layer (it's actually the first thing requests hit)
- No flow between the 3 server boxes (stacked vertically, disconnected)
- Single FlowConnector only spanned the narrow origin boundary divider

**After (3-zone horizontal flow):**
```
[Browser/Client] --FC1--> [CORS Middleware Gate] --FC2--> [Rails API]
```

- 3 horizontal zones with 2 FlowConnectors (matches L10 Validations pattern)
- 5 flow phases: Zone 0 highlight -> FC1 animate -> Zone 1 highlight -> FC2 animate -> Zone 2 highlight
- Preflight removed as a zone, communicated through flow messages instead (accurate: it's browser behavior)
- CORS Middleware is now the center gate zone (accurate: first thing requests hit)
- curl probe: Zone 0 switches from browser chrome to terminal style, CORS gate shows "(bypassed)", API shows "200 OK"
- Browser probes: red flow, CORS gate shows blocking, API shows "not reached" with opacity-50
- Stage inspectors reduced from 4 to 3 (browser, cors, api; removed preflight)
- Browser StageInspector updated to explain preflight as part of browser behavior
- Data maps updated: `PROBE_PIPELINE_MAP` uses `gateSublabel`/`gateBadge` instead of `corsSublabel`/`corsBadge`
- `OBSERVE_FLOW` and `REWARD_FLOW` messages updated to match 3-zone semantics

**What stayed the same:**
- 3 probes, 4 discoveries, minRequired: 3
- 6 stress test scenarios
- Build phase (3 steps: terminal + 2 OptionCard)
- Left panel, right panel, code preview
- Flow state machine (runFlow/clearFlow/flowPhase)

### Verification
- `bun run build` passes cleanly

---

## Session 1b: Update audit-level Skill with Visualization Accuracy Section

### What Changed

**File:** `.claude/skills/audit-level/SKILL.md`

1. **Updated L15 reference description** (line 22): Changed from "Browser-Server Handshake with side-by-side towers" to "3-zone horizontal flow (Client -> CORS Middleware Gate -> Rails API) with 2 FlowConnectors"

2. **Added "Visualization accuracy" subsection** under Phase 1's visualization approach section. Includes:
   - Principle: "The visualization must not lie." If a player memorized the diagram, would they have an accurate mental model?
   - **Full case study of L15 CORS redesign (bad -> good)** showing:
     - The original layout with annotated problems (preflight as server box, middleware in wrong position, no flow connectors between stacked boxes)
     - The redesigned layout with annotated fixes (preflight as flow messages, middleware as center gate, curl bypass visually distinct, 2 FlowConnectors)
   - **New checklist items** for visualization accuracy:
     - Each zone represents a real architectural component (not request types)
     - Zone order matches real processing order
     - Connectors exist between every adjacent zone
     - Bypass/skip scenarios are visually distinct
     - "Not reached" state is shown for blocked requests

---

## Session 1c: Fix FlowConnector Line Stretching Across Full Width

### What Changed

**File:** `frontend/src/features/act2-users-security/components/Level15CORS.tsx`

All 4 FlowConnector instances had `className="h-4 w-full"` but were missing `relative`. The FlowConnector's static line and dot track use `absolute` positioning, so without `relative` on the container, they positioned relative to the nearest positioned ancestor and stretched across the entire visualization area.

**Fix:** Changed all 4 instances to `className="relative h-4 w-full"`.

---

## Session 1d: Gate Zone Content Behind Flow Phase

### What Changed

**File:** `frontend/src/features/act2-users-security/components/Level15CORS.tsx`

Zone 1 (CORS gate) and Zone 2 (Rails API) were showing their result text ("No headers sent", "BLOCKED", "not reached", "200 OK") immediately when a probe fired, before the flow animation reached them. This is because `probeState` was derived from `lastProbeId` which updates instantly.

**Fix:** Added two derived flags that gate content display behind the flow reaching each zone:
- `gateRevealed = probeState && (flowPhase >= 2 || flowPhase === -1)` for Zone 1
- `apiRevealed = probeState && (flowPhase >= 4 || flowPhase === -1)` for Zone 2

Replaced all `probeState` references in Zone 1's styling/content with `gateRevealed`, and in Zone 2 with `apiRevealed`. Now zones only show their result state after the animation reaches them (or after it completes).

---

## Session 1e: Defer Discovery Until Animation Ends

### What Changed

**File:** `frontend/src/features/act2-users-security/components/Level15CORS.tsx`

The "Build the Fix" button was appearing mid-animation because `discoveryGating.discover()` was called immediately in `handleProbe`, which could unlock the button before the flow finished.

**Fix:** Deferred `discoveryGating.discover(discoveryId)` into a `setTimeout` matching the animation end time (`delay * (totalPhases + 2)`). The timeout is pushed into `flowTimeoutsRef` so it gets cleaned up with `clearFlow()`. Now discoveries register only after the animation completes.

---

## Session 1f: Reduce Animation End Delay + Update Skill

### L15 Animation Timing
Changed `delay * (totalPhases + 2)` to `delay * (totalPhases + 1)` for both the flow reset and discovery defer. Animation now resets 1.5s after the last zone lights up instead of 3s.

### Audit-Level Skill: Not Every Level Needs Visualization
Added "Step 0: Does this level need a visualization at all?" section before the visualization approach section. Key points:
- Setup levels (L1 installing Rails, L4 generating scaffold) have no problem to visualize
- Three questions to ask before designing a visualization
- List of level types that need/skip visualization
- Expanded the visualization approach section with a decision checklist and stronger guidance that the choice requires careful thought

---

## Session 2: Full Act 1 Audit (L1-L8)

### What was done
Spawned 8 parallel Claude Code agents to audit every level in Act 1 against the three-phase sequential flow standard.

### Key Findings

**No level in Act 1 implements the three-phase flow.** All 8 are build-phase-only with static completion screens. No phase state machines exist.

**Tier 1 (Setup levels, acceptable as-is):** L1 Environment, L2 First Boot, L3 The Model, L4 CRUD. Pure setup levels where skipping observe/reward is justified. Minor fixes only.

**Tier 2 (Need redesign):** L5 Routes, L6 Controller, L7 Serializers, L8 Associations. Architecture/concept levels that would benefit from observe and reward phases.

**Critical factual errors found:**
- L7: Uses `bundle install` instead of `bundle add jsonapi-serializer`
- L7: Missing BaseSerializer creation step with `include JSONAPI::Serializer`
- L8: Missing `rails db:migrate` step after generating Comment model
- L8: Missing serializer inclusion step (spec requires "nested JSON")

**Cross-cutting issues:**
- L4 and L8 have all correct answers at the same position (index 1)
- L5, L6, L8 have wrong level numbers in file header comments
- L5-L8 use hardcoded `bg-zinc-900` instead of semantic tokens

### Fixes Applied

**Fix 1 (L7):** Changed `bundle install` to `bundle add jsonapi-serializer`. Updated step description, output lines, and moved correct answer to last position.

**Fix 2 (L7):** Added new "Base Serializer" step (step 3) teaching `include JSONAPI::Serializer` pattern. Added 3 options (ActiveModel::Serializer wrong, ApplicationSerializer wrong, JSONAPI include correct). Updated all step indices and code preview.

**Fix 3 (L8):** Added `rails db:migrate` step after Generate Comment. Added migrate commands with 3 options. Fixed header comment (Level 7 -> Level 8). Varied answer positions: Relationship correct at index 2, Dependent at index 3 (last), Test at index 2 (last). Fixed `bg-zinc-900` to `bg-card`.

**Fix 4 (L4):** Varied correct answer positions from all-index-1 to: Create=2, Read=1, Update=2, Destroy=1, Verify=2.

**Fix 5 (L5, L6):** Fixed header comments: "Level 4" -> "Level 5", "Level 5" -> "Level 6".

**Fix 6 (L2):** Made OptionCard colors uniform (both `blue` instead of `cyan` vs `blue`).

All changes verified with `bun run build` (passes cleanly).

### Remaining work (not fixed this session)
- L1 hint text reveals answers (minor)
- L5-L7 hardcoded `bg-zinc-900` replacements (minor)

---

## Session 3: Three-Phase Redesign of L5-L8

### What Changed

Four levels in Act 1 were completely rewritten from build-phase-only to the mandatory three-phase sequential flow (`observe -> build -> activate -> reward`), following the Level 12 Authorization canonical pattern.

#### Level 5: Routes (completed in Session 2b)
**File:** `frontend/src/features/act1-foundation/components/Level5Routes.tsx` (~1107 lines)
- Observe: 5-stage pipeline (Request -> Router [inactive] -> Controller -> Model -> Response), 3 probes showing 404s, 4 discoveries (minRequired: 3)
- Build: 4 steps (Define Resource, Add Namespace, View Routes terminal, Trace Request)
- Reward: StressTestPanel with 5 scenarios for all RESTful routes

#### Level 6: Controller (completed in Session 3)
**File:** `frontend/src/features/act1-foundation/components/Level6Controller.tsx`
- Observe: 5-stage pipeline. Router is active (green, routes work from L5), Controller is inactive/dashed ("Missing!"). 3 probes show routes matching but crashing with NameError at the controller. 4 discoveries (minRequired: 3).
- Build: 4 steps preserved from original: Generate Controller (terminal), Add 5 RESTful Actions (click-to-select with distractors), Strong Params assembly (`params.expect`), Test Endpoint with curl (terminal).
- Reward: All stages active/green. StressTestPanel with 5 scenarios (GET 200, POST 201, GET show 200, PATCH 200, DELETE 204).

#### Level 7: Serializers (completed in Session 3)
**File:** `frontend/src/features/act1-foundation/components/Level7Serializers.tsx` (~1339 lines)
- Observe: 5-stage pipeline with Serializer stage inactive. 3 probes show raw JSON dump (all columns exposed, no JSON:API structure). 4 discoveries (minRequired: 3).
- Build: 5 steps: Choose Gem (OptionCard), Install Gem (`bundle add jsonapi-serializer`, terminal), Create Base Serializer (OptionCard), Define Attributes (click-to-select grid), Update Controller (OptionCard).
- Reward: Serializer stage active (green). StressTestPanel with 5 scenarios showing clean JSON:API responses.

#### Level 8: Associations (completed in Session 3)
**File:** `frontend/src/features/act1-foundation/components/Level8Associations.tsx`
- Observe: 6-stage pipeline with Association stage inactive. 3 probes show isolated posts (no comments, no nested routes). 4 discoveries (minRequired: 3).
- Build: 6 steps: Generate Comment with `post:references` (terminal), Run Migration (terminal), Choose `has_many` Relationship (OptionCard), Auto `belongs_to` explanation (info step), Set `dependent: :destroy` (OptionCard), Test via `post.comments.create` (terminal with irb> prompt).
- Reward: Association stage active (green). StressTestPanel with 5 scenarios including cascade delete and FK constraint test.

### Verification
- `bun run build` passes cleanly for all four levels.
- All four files have `type Phase = 'observe' | 'build' | 'activate' | 'reward'` state machine.
- All use `useDiscoveryGating`, `PipelineFlow`, `ProbeTerminal`, `StageInspector`, `DiscoveryChecklist`, `StressTestPanel`, `useStressTest`.

---

## Session 4: Pipeline Consistency Fix (L7, L8)

### Problem
Pipelines were inconsistent across levels:
- L7 dropped the Model stage (Controller -> Serializer -> Response), making players wonder "where did the Model go?"
- L8 invented a fake "Association" stage that is not a real request lifecycle component (associations are a Model-layer concern)

### Fix
Standardized pipelines across all 4 levels:
- **L5-L6**: Request -> Router -> Controller -> Model -> Response
- **L7-L8**: Request -> Router -> Controller -> Model -> Serializer -> Response

The pipeline grows as the app grows: Serializer is introduced in L7 and carried forward to L8.

**L7 changes:**
- Added Model stage between Controller and Serializer (observe + reward)
- Added Model stage inspector ("Post Model from Level 3, works correctly, problem is how controller renders it")
- Added Model to connections (controller->model, model->serializer)

**L8 changes:**
- Removed fake "Association" stage entirely
- Model is now the focus stage (inactive with "No associations" sublabel, goes danger on probes)
- Added Serializer stage (active, working from L7)
- Updated PROBE_PIPELINE_MAP to target Model (modelSublabel instead of assocSublabel)
- Updated STAGE_INSPECTOR_MAP: merged association info into Model, added Serializer
- Updated STAGE_DISCOVERY_MAP: model triggers 'no-comment-model' instead of association
- Updated connections: model->serializer->response instead of model->association->response
- Reward pipeline: Model stage shows "has_many :comments" or "404 Not Found" (was on Association)

### Verification
- `bun run build` passes cleanly
- All 4 levels have consistent pipeline stage progression

### Remaining work
- L1 hint text reveals answers (minor)
- L5-L7 hardcoded `bg-zinc-900` replacements (minor)

---

## Session 5: Hub-and-Spoke Pipeline Layout for L5-L8

### What Changed

Replaced the linear pipeline layout (Request -> Router -> Controller -> Model -> Response) with an accurate hub-and-spoke layout where Controller is the central orchestrator and Model/Database/Serializer are its bidirectional dependencies.

**Layout:**
```
                 Serializer (L7+)
                    ^  |
                    |  v
Request -> Router -> Controller -> Response
                    ^  |
                    |  v
                   Model (L5+)
                    ^  |
                    |  v
                  Database (L5+)
```

#### PipelineFlow.tsx changes:
- Added `position?: { x: number; y: number }` to `PipelineStage` for explicit node positioning
- Added `sourceHandle?`, `targetHandle?`, `bidirectional?` to `PipelineConnection` for multi-directional edges
- Added `activeConnections?: string[]` prop for sequential edge animation control (undefined = idle, [] = dormant, [...ids] = active)
- Replaced 2 generic handles with 8 named handles (4 sides x 2 types: source + target) on every node
- Updated `buildNodes` to use explicit positions while auto-positioning unpositioned stages horizontally
- Updated `buildEdges` to expand bidirectional connections into forward + return edges, with handle ID resolution
- Return edges render with smaller dots (r=6 vs r=8) and lower opacity (0.6), no base line (shares forward edge's line)
- Edge animation modes: idle (repeatCount indefinite), active (repeatCount 1, fast dur), dormant (no dots)
- Added `fitView({ padding: 0.15 })` to prevent satellite nodes from being clipped

#### Level updates (L5, L6, L7, L8):
- All four levels now use hub layout with `HUB_POS` constants
- Main chain (Request, Router, Controller, Response) is auto-positioned horizontally
- Model at `{ x: 500, y: 180 }` below Controller, bidirectional
- Database at `{ x: 500, y: 360 }` below Model, bidirectional
- Serializer at `{ x: 500, y: -180 }` above Controller (L7+), bidirectional
- Progressive disclosure: L5-L6 show Model + Database, L7-L8 add Serializer
- Both observe and reward stages updated with positions

#### audit-level SKILL.md:
- Added "Hub-and-spoke layout for MVC architecture levels" subsection with layout diagram, position constants, and checklist
- Added "Sequential edge animation" subsection documenting the `activeConnections` prop API and flow sequence pattern

#### Follow-up fixes

**Symmetric bidirectional edges:**
Forward and return edges overlapped on the exact same path. Fixed with symmetric offset: forward edge shifts -14px left, return edge shifts +14px right (via `LANE_OFFSET`). Added `isBidirectional` flag to forward edges so only bidirectional pairs get offset (non-bidirectional horizontal edges stay centered). Return edge renders with dashed line (`strokeDasharray="4 4"`). Both directions use same dot size (r=8).

**Satellite node state:**
Database node was rendering with bare `default` variant (zinc), looking identical to Request. Fixed across all levels:
- L5/L6 observe: Database shows `inactive` + "unreachable" when probe fires (upstream is broken)
- L7/L8 observe: Database shows `active` (problem is elsewhere)
- All reward phases: Database shows `active`

**audit-level SKILL.md:**
Added bidirectional edge rendering rules, satellite node state rules with per-level table, and expanded checklist items for hub layout auditing.

### Verification
- `bun run build` passes cleanly
- Backward compatible: L9, L12, L13, L14 unaffected (no `position` or handle props, default to horizontal layout with right-source/left-target handles)

---

## Session 6: Act 2 Audit + Audit Skill Improvement

### Act 2 Audit Findings

Audited all 7 levels (L9-L15) in Act 2 "Guards & Gates" against the audit-act skill procedure.

**Overall:** Well-structured act with excellent transitions and clear narrative arc. All 7 levels implement the three-phase flow. No structural redesign needed.

**Issues found:**
1. **Concept overlap (L6/L14):** Both levels teach `params.expect()`. L6 introduces it as part of controller building, L14 re-teaches it with a "security audit" framing. The spec's Rails 8 Features table lists it at both L6 and L14. This is the same API taught twice under different themes.
2. **Schema ghosts:** L10 content references `Post.status` and `User.username` columns that no prior level creates. L14 references `Post.admin` column.
3. **Enum mention in L10:** `learningContent.conceptExplanation` mentions Rails 8 enums with a detailed example, but enums are an Act 3+ concept.
4. **Variable naming in content.ts:** Variables like `level10Callbacks` (actually L11), `level11Authorization` (actually L12), `level13StrongParams` (actually L14) are misnamed. Comment headers also wrong.
5. **Hardcoded bg-zinc-900 in L15 component.**
6. **Missing startingPipeline for L12 and L14** (inconsistent with other levels).

### Audit-Act Skill Update

**File:** `.claude/skills/audit-act/SKILL.md`

Rewrote Step 6 ("Check Concept Ownership") to prevent missing concept overlaps like L6/L14:

- **Added Step 6a:** "Extract the Rails API surface per level." Forces building an explicit table of Rails APIs/keywords/methods each level teaches. Makes same-API overlaps visible.
- **Added Step 6b:** Restructured checklist. New item: "No Rails API or keyword is the primary concept of two levels." If `params.expect` appears as the core teaching of both L6 and L14, one must be redesigned or merged.
- **Added spec cross-check:** "Check the spec's Rails 8 Features Integration table. If a feature appears at two level numbers, flag it as a concept ownership conflict."
- **Added "same API, different framing" trap warning:** Explains that repackaging the same Rails method under a new theme still counts as overlap. The test: if you removed one level, would the player still learn the API from the other?
- **Added Step 6c: "Resolve overlap by finding the right home."** When overlap is found, determine which level should own the concept by considering: (1) which act the concept thematically belongs to (params.expect is security, belongs in Act 2 not Act 1), (2) whether the earlier level can work without the concept, (3) which level's identity IS the concept (L14 "Strong Params" owns params.expect by name), (4) relocate teaching rather than just removing it. Includes full L6/L14 case study.

---

## Session 7: Remove Strong Params from L6 Controller

### What Changed

**File:** `frontend/src/features/act1-foundation/components/Level6Controller.tsx`

Removed the `params.expect()` / Strong Params step from Level 6 to resolve the concept overlap with Level 14 (Strong Params). The controller level now focuses purely on controller generation, RESTful actions, and endpoint testing.

**Changes:**
1. Removed Step 2 ("Strong Params") from `STEP_DEFS`. Steps are now: Generate Controller -> Add Actions -> Test Endpoint (3 steps, down from 4).
2. Removed `ParamsPiece` interface, `PARAMS_PIECES` array, and `CORRECT_PARAMS_ORDER` array.
3. Updated `SHELL_STEP_MAP` to remove the `null` entry for the old Strong Params step.
4. Removed `getParamsErrorFeedback` function.
5. Removed `assembledPieces` state and `handleAddPiece` callback.
6. Updated `getActionBody` to use raw params (`params[:title]`, `params[:body]`) instead of `post_params` in create/update actions.
7. Updated `getCodeFiles` to remove the `private post_params` method from the code preview.
8. Removed the entire Step 2 Strong Params UI block (assembly area, piece buttons, reset button).
9. Renumbered Step 3 (Test Endpoint) to Step 2 in both code and comments.
10. Updated left panel scenario text: "wire up strong params" -> "test the endpoint".
11. Updated file header comment: 3 steps instead of 4, removed `params.expect` from teaches line.
12. Replaced `bg-zinc-900 dark:bg-zinc-950` with `bg-card` semantic token.

### Verification
- `bun run build` passes cleanly.

---

## Session 8: Rewrite L14 Strong Params to Introduce params.expect from Scratch

### What Changed

**File:** `frontend/src/features/act2-users-security/components/Level14StrongParams.tsx`

Previously L14 assumed the player already knew `params.expect()` from L6 and framed the problem as "too-broad whitelist." Now that L6 no longer teaches `params.expect`, L14 introduces the concept from scratch.

**Observe phase (problem reframing):**
- Problem changed from "too-broad params.expect whitelist" to "controller passes raw request params directly to the model with no filtering"
- Discovery defs updated: `broad-whitelist` -> `no-filtering`, `unprotected-model` -> `raw-params`
- Probe response text updated to say "No parameter filtering" instead of "The whitelist includes..."
- Pipeline filter stage: label changed from `params.expect` to `Params Filter`, sublabel from `(too broad)` to `(missing!)`, default variant from `default` to `inactive`
- Pipeline probe display: sublabels changed from "user_id: 42 PASSED" to "NO FILTER"
- Stage inspector: filter stage shows "Parameter Filtering (Missing!)" with raw params code, controller shows `Post.create!` with raw `params[:key]` access
- Left panel scenario text rewritten to describe raw params problem
- Code preview shows controller using `params[:title]`, `params[:body]`, `params[:user_id]`, `params[:admin]` directly

**Build phase (new step progression):**
- Step 0: "Add Parameter Filtering" (choose filtering method: manual if/else, params.permit!, or params.expect)
- Step 1: "Define the Whitelist" (choose which fields: all 4, 3 with user_id, or just title+body)
- Step 2: "Set Ownership Safely" (unchanged, current_user.posts.create!)
- Code preview evolves: raw params -> params.expect(post: [...]) -> params.expect(post: [:title, :body]) -> full solution with current_user

**Reward phase:**
- Filter stage label stays `params.expect` (it now exists after the build)
- Sublabel updated from `[:title, :body, :status]` to `[:title, :body]` (no status column)
- Stress test scenario descriptions updated to remove `:status` references

**All references to `:status` column removed** since Post has no status column in the schema.

### Verification
- `bun run build` passes cleanly.

---

## Session 9: Apply Remaining Act 2 Audit Fixes

### What Changed

**File:** `frontend/src/features/act2-users-security/content.ts`

1. **Removed enum paragraph from L10 conceptExplanation.** Rails 8 enums are not an Act 2 concept.
2. **Removed schema ghosts from L10 railsCodeExample:**
   - Removed `validates :status, inclusion: { in: %w[draft published archived] }` (Post has no status column)
   - Removed `validates :username` block (User has no username column)
3. **Renamed mismatched variables:** `level10Callbacks` -> `level11Callbacks`, `level11Authorization` -> `level12Authorization`, `level13StrongParams` -> `level14StrongParams`. Fixed comment headers to match.
4. **Rewrote L14 trigger/problem/learningContent** to introduce `params.expect()` from scratch (no prior knowledge assumed). Problem now describes raw params with no filtering. Code example shows before/after.

**File:** `docs/spec.md`

5. **Updated Rails 8 Features table:** Changed `params.expect()` from "L6, L14" to "L14".

**File:** `frontend/src/features/act2-users-security/components/Level15CORS.tsx`

6. **Fixed hardcoded `bg-zinc-900 dark:bg-zinc-950`** to `bg-card` in curl probe header.

### Verification
- `bun run build` passes cleanly.

---

## Session 10: Fix Pipeline Node State Colors (L6, L8)

### Problem
Nodes in observe phase stayed `default` (black) when they should reflect their state during probes:
- **L6 Controller:** Model node stayed black on probe, should be `inactive` + "unreachable" (controller is broken, model is downstream and unreachable)
- **L8 Associations:** Response node stayed black on probe showing "404", should be `danger` (error state)

### Fix
- **L6** (`Level6Controller.tsx`): Added `sublabel: probeDisplay ? 'unreachable' : undefined` and `variant: probeDisplay ? 'inactive' : 'default'` to Model node in observe stages. Now matches the Database node pattern already in the file.
- **L8** (`Level8Associations.tsx`): Added `variant: probeDisplay ? 'danger' : 'default'` to Response node in observe stages. Now matches L5/L7 Response node pattern.

### audit-level SKILL.md
Added "Node colors must match their state" rule under PipelineFlow checks. Every node whose sublabel changes on probe must also change its variant. Includes L6/L8 case study.

### Verification
- `bun run build` passes cleanly.
