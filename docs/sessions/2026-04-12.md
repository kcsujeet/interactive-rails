# Session: 2026-04-12

## Summary

Major UX improvements, consistency fixes, and the new Codebase Viewer feature.

## Changes

### Better Auth Fixes
- Moved auth handler back into Hono following the [official pattern](https://hono.dev/examples/better-auth-on-cloudflare)
- Moved secrets (`BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `JWT_SECRET`) to `.dev.vars` (Cloudflare's recommended local dev secrets file)
- Pinned miniflare to `4.20251217.0` to work around [POST+non-2xx crash bug](https://github.com/cloudflare/workers-sdk/issues/13013) in newer versions
- Fixed D1 foreign key constraint: `player_progress.user_id` now references Better Auth's `"user"` table instead of the old `users` table
- Added user-friendly error messages in LoginForm and SignupForm catch blocks

### Acts Page: Collapsible Accordion
- Removed separate `/acts/[actId]` page
- Made acts expand inline with collapsible level lists (using Radix Collapsible)
- Added smooth 200ms slide animation via Tailwind `@theme` keyframes
- Persists open/closed state in localStorage
- Auto-opens first incomplete act on first visit
- Removed misleading ChevronRight icons from level cards
- Updated dead `/acts/{id}` links in LevelHeader and LevelBreadcrumb to point to `/acts`
- Deleted `ActCard.tsx` and `ActDetailApp.tsx` (no longer needed)

### Probe Info Stories (67 probes across 27 levels)
- Added `story` arrays to all probes missing them (Acts 1-5)
- Converted probe buttons to split-button style: main button fires probe, smaller right section shows info dialog

### Scenario Heading Consistency (37 levels)
- Added `<h3>Scenario</h3>` heading to all three-phase levels that were missing it
- Updated CLAUDE.md, audit-level skill, and design-level skill to enforce this as non-negotiable

### Codebase Viewer Feature
- New feature at `src/features/codebase-viewer/` following bulletproof-react structure
- `components/CodebaseViewerDialog.tsx`: Near-full-screen dialog with Monaco Editor (VS Code's editor)
- `components/FileTree.tsx`: Custom recursive file tree with Lucide folder/file icons
- `utils/build-project-tree.ts`: Converts flat file list to nested tree structure
- `utils/codebase-registry.ts`: Aggregates code from completed levels using lazy getters (zero duplication)
- All 56 levels register with `registerLevelCode('id', () => getCodeFiles('reward', STEP_DEFS.length))`
- Trigger points: Acts page ("View Codebase" button), LevelHeader ("Codebase" button)
- Installed `@monaco-editor/react` and `monaco-editor`

### Vite/Astro Fixes
- Added `better-auth` to `optimizeDeps.include` to prevent mid-request page reloads
- Added explicit `@/` resolve alias in Vite config for consistent client-side hydration
- Cleared Vite/Astro caches to fix intermittent `@id/@/` import resolution errors

### Project Structure
- Enforced bulletproof-react structure in CLAUDE.md, audit-level skill, and design-level skill
- Restructured `codebase-viewer` feature to use `components/` and `utils/` subdirectories

## Architecture Notes

### Codebase Registry: Zero-Duplication Pattern
Each level calls `registerLevelCode('id', () => getCodeFiles('reward', STEP_DEFS.length))` at module level. The lazy getter reuses the existing `getCodeFiles` function (single source of truth). The registry evaluates lazily when the viewer opens, merging files in level order with last-writer-wins for duplicate filenames.

### Miniflare Pin
`miniflare` is pinned to `4.20251217.0` via `overrides` in package.json due to [workers-sdk#13013](https://github.com/cloudflare/workers-sdk/issues/13013). POST requests returning non-2xx status codes crash newer miniflare versions. Remove the pin once the upstream fix ships.

---

## Session 2: Content File Splitting (Acts 1-4)

### Split content.ts into per-level files

Split the monolithic `content.ts` file in Acts 1-4 into individual per-level content files and an act metadata file.

**Pattern for each act:**
- `content/content.ts` (deleted) -> `content/act.ts` + `content/level-N-name.ts` (one per level)
- Each per-level file exports a single `Level` object with its own type import
- `act.ts` imports all level files and exports the act aggregate

**Files created:**
- Act 1 (`act1-foundation`): 8 level files + `act.ts` (levels 1-8)
- Act 2 (`act2-users-security`): 7 level files + `act.ts` (levels 9-15)
- Act 3 (`act3-clean-architecture`): 7 level files + `act.ts` (levels 16-22)
- Act 4 (`act4-performance`): 9 level files + `act.ts` (levels 23-31)

**Import update:** `src/lib/acts-registry.ts` updated to import from `content/act` instead of `content/content` for all 4 acts.

**Note:** Act 3 levels 16, 20, 21 use `standardPipeline` from `@/utils/pipelineTemplates`, which was added as an extra import in their individual level files.

**Verification:** Build passes, all 1502 tests pass, no new TS errors introduced.

---

## Session 3: Content File Splitting (Acts 5-8)

### Split content.ts into per-level files

Continued the content.ts splitting pattern from Session 2, now covering Acts 5-8.

**Files created:**
- Act 5 (`act5-production`): 9 level files + `act.ts` (levels 32-40)
- Act 6 (`act6-reliability`): 7 level files + `act.ts` (levels 41-47)
- Act 7 (`act7-scale`): 6 level files + `act.ts` (levels 48-53)
- Act 8 (`act8-mastery`): 3 level files + `act.ts` (levels 54-56)

**Import update:** `src/lib/acts-registry.ts` updated to import from `content/act` instead of `content/content` for all 4 acts.

**Pipeline template imports:** Act 5 levels using `standardPipeline` and Act 6 levels using `standardPipeline`/`middlewarePipeline` from `@/utils/pipelineTemplates` have the appropriate imports in their individual level files.

**Old files deleted:** `content/content.ts` removed from all 4 act directories.

**Verification:** Build passes, all 1502 tests pass, biome formatted, no new TS errors in changed files.

---

## Session 4: Fix All 16 TypeScript Errors from `astro check`

### Changes

Fixed all 16 TypeScript errors reported by `bun run check-types` (which runs `astro check`):

1. **Button `color` prop removal** (2 files): Removed invalid `color` prop from `<Button>` in `QueryTraceViewer.tsx` (line 204) and `RequestTimeline.tsx` (line 280). The Button component does not accept a `color` prop.

2. **Missing Level type properties** (7 content files): Added `startingPipeline`, `successConditions`, `availableNodes`, `unlockedNodes` with empty defaults to:
   - `level-12-authorization.ts`, `level-14-strong-params.ts`, `level-15-cors.ts` (Act 2)
   - `level-24-eager-loading.ts`, `level-25-narrow-fetching.ts`, `level-26-database-indexing.ts`, `level-27-counter-caches.ts` (Act 4)

3. **Null check for `opt.feedback`** (2 test files): Used `(opt.feedback ?? '').length` instead of `opt.feedback.length` in `Level45RecurringJobs.test.ts` and `Level46DataLifecycle.test.ts` to handle possibly undefined feedback.

4. **Type assertions in `dashboard.astro`**: Added type assertions for `errorData` (from `.catch(() => ({}))`) and `result` (from `response.json()`) to resolve `unknown` type and missing `error` property errors.

5. **Button `size="xl"` to `size="lg"`** (3 instances in `index.astro`): Changed all three `size="xl"` Button uses to `size="lg"` since "xl" is not a valid Button size variant.

**Verification:** `bun run check-types` reports 0 errors, `bun run build` succeeds, all 1502 tests pass.

---

## Session 5: Migrate L35-L40 React Flow Nodes to Shared FlowNode Component

### Changes

Replaced custom React Flow node components in levels 35, 37, 38, 39, and 40 with wrappers around the shared `FlowNode` component (`src/components/levels/FlowNode.tsx`).

**Pattern used for each level:**
- Created a `flashToStatus()` helper to map level-specific `ZoneFlash` states (`idle`/`red`/`green`/`amber`) to `FlowNodeData['status']` (`idle`/`active`/`warning`/`error`)
- Each custom node now constructs a `FlowNodeData` object with `label`, `icon` (2-letter code), `color` (hex), and `status`
- Set `showTarget: false, showSource: false` on FlowNode data to suppress its default handles
- Render `<FlowHandles />` as children to preserve the 8-handle system (with specific IDs like `right-source`, `left-target`, `top-source`, `bottom-source`) that edges reference
- Level-specific content (thread pools, CPU gauges, memory gauges, badges, DB rows, sub-panels) moved into FlowNode's `children` slot

**Files modified:**
- `src/features/act5-production/components/level-35-active-storage/Level35ActiveStorage.tsx`: Replaced `ZoneNode` (3 zone variants: client, app, s3). Removed unused `ZONE_FLASH_STYLES` and `ZONE_LABEL_STYLES`. Removed `Cloud`, `Monitor`, `Server` icon imports.
- `src/features/act5-production/components/level-37-real-time/Level37RealTime.tsx`: Replaced `ClientNode`, `ServerNode`, `ProcessorNode`. Removed `FLASH_BORDER`/`FLASH_BG` maps. Removed `Globe`, `ServerIcon` icon imports.
- `src/features/act5-production/components/level-38-external-apis/Level38ExternalAPIs.tsx`: Replaced `ClientNode`, `AppServerNode`, `StripeNode`. Kept `FLASH_BORDER`/`FLASH_BG` (still used by middleware sub-panels). Removed `Globe`, `Monitor`, `Server` icon imports.
- `src/features/act5-production/components/level-39-webhooks/Level39Webhooks.tsx`: Replaced all 6 nodes (`WebhookCustomerNode`, `WebhookAttackerNode`, `WebhookStripeNode`, `WebhookAppNode`, `WebhookDbNode`, `WebhookQueueNode`). Removed `FLASH_BORDER`/`FLASH_BG` maps. Removed `Database`, `Globe`, `ListTodo`, `Server`, `ShieldAlert`, `User` icon imports.
- `src/features/act5-production/components/level-40-api-versioning/Level40APIVersioning.tsx`: Replaced `PartnerNode`, `VersionAppNode`. Kept `FLASH_BORDER`/`FLASH_BG` (still used by v1/v2 sub-panels). Removed `Server`, `Users` icon imports.

**Verification:** `bunx tsc --noEmit` clean for all modified files, `bunx biome check --write` formatted, `bun run build` succeeds, all 1502 tests pass.

---

## Session 6: Migrate L48-L56 React Flow Nodes to Shared FlowNode Component

### Changes

Replaced custom React Flow node components in levels 48-56 (Acts 7-8) with wrappers around the shared `FlowNode` component (`src/components/levels/FlowNode.tsx`).

**Same pattern as Session 5:**
- `flashToStatus()` helper maps `ZoneFlash` to `FlowNodeData['status']`
- Each custom node constructs `FlowNodeData` with `label`, `icon` (2-letter code), `color` (hex), `status`
- Set `showTarget: false, showSource: false` on FlowNode data; render `<FlowHandles />` as children for multi-directional handles
- Level-specific content (badges, overloaded indicators, filter active, public API, AASM, etc.) moved into FlowNode `children` slot

**Files modified:**
- `Level48MultiDatabase.tsx`: Replaced `AppNode`, `PrimaryDbNode`, `ReplicaDbNode`. Removed flash-class logic; kept `Zap` for overloaded indicator.
- `Level49StateMachines.tsx`: Replaced `StateNode` (was rounded-full pill, now card-style via FlowNode). Initial state uses `selected` prop for ring highlight.
- `Level50MultiTenancy.tsx`: Replaced `CompanyNode` (blue/orange color variants), `TableNode`. Removed `Building2`, `Database` icon imports.
- `Level51Observability.tsx`: Replaced `LogNode`, `TraceNode`, `HealthNode`. Kept `Activity`, `FileText`, `Heart` for legend.
- `Level52ModularMonolith.tsx`: Replaced `PackageNode`. Removed `flashToClass`/`flashToIconClass` helpers and `Package` import. Kept `FileCode` for Public API indicator.
- `Level53DomainEvents.tsx`: Replaced `CheckoutNode`, `ServiceNode` (with icon/color maps by label), `EventBusNode`. Removed `getFlashClass`/`getIconClass` helpers.
- `Level54APIGateway.tsx`: Replaced `ClientNode`, `GatewayNode`, `ServiceNode`. Removed `Server`, `Shield`, `Smartphone` from node rendering (kept `Shield`, `Zap` for legend).
- `Level55Sharding.tsx`: Replaced `TenantNode`, `DbNode`, `ShardDbNode`, `ResolverNode`. Kept `Database`, `GitBranch`, `Zap` for legend/indicators.
- `Level56Architect.tsx`: Replaced `OrderNode`, `ServiceNode` (with SERVICE_ICON_CODE/SERVICE_COLOR maps), `GatewayNode`, `BillingNode`, `EventBusNode`. Removed `flashToClasses` helper. Kept `Database`, `Globe`, `Radio`, `ShieldCheck` for legend/indicators.

**Verification:** `bunx tsc --noEmit` zero errors, `bunx biome check --write` formatted, `bun run build` succeeds, all 1502 tests pass.

---

## Session 7: Migrate L41-L47 React Flow Nodes to Shared FlowNode Component

### Changes

Replaced custom React Flow node components in levels 41-47 (Act 6: Reliability) with wrappers around the shared `FlowNode` component (`src/components/levels/FlowNode.tsx`).

**Same pattern as Sessions 5 and 6:**
- `flashToStatus()` helper maps `ZoneFlash` to `FlowNodeData['status']`
- Each custom node constructs `FlowNodeData` with `label`, `icon` (2-letter code), `color` (hex), `status`
- Set `showTarget: false, showSource: false` on FlowNode data; render `<FlowHandles />` alongside for multi-directional edge handles
- Level-specific content (middleware sub-panels, rate limit badges/counters, DB rows, lock indicators, row counts, error logs, alert statuses) moved into FlowNode `children` slot

**Files modified:**
- `Level41Middleware.tsx`: Replaced `ClientNode`, `MiddlewareNode`, `AppNode`. Middleware sub-panels (bot, requestId, logger) preserved as children. Removed `Globe`, `Layers`, `Server` icon imports. Kept `FLASH_BORDER`/`FLASH_BG` for sub-panel styling.
- `Level42RateLimiting.tsx`: Replaced `BotNode`, `CustomerNode`, `RlAppNode`. Rate limit sub-panels (ipLimit, userLimit) and badge preserved as children. Removed `Bot`, `Server`, `User` icon imports. Kept `FLASH_BORDER`/`FLASH_BG` for sub-panel styling.
- `Level43SoftDeletes.tsx`: Replaced `AdminNode`, `AppNode`, `DbNode`. DB rows list preserved as children. Removed `Database`, `Server`, `ShieldCheck` icon imports. Removed unused `FLASH_BORDER`/`FLASH_BG`.
- `Level44SafeMigrations.tsx`: Replaced `DeveloperNode`, `DatabaseNode`, `ApiNode`. Lock indicator (Lock/Unlock icons) and rowsLabel preserved as children. Removed `Database`, `Server`, `Terminal` icon imports. Kept `Lock`/`Unlock` imports and `FLASH_BORDER`/`FLASH_BG` for lock sub-panel.
- `Level45RecurringJobs.tsx`: Replaced `SchedulerNode`, `AppNode`, `DbNode`. Row count preserved as children. Removed `Calendar`, `Database`, `Server` icon imports. Removed unused `FLASH_BORDER`/`FLASH_BG`.
- `Level46DataLifecycle.tsx`: Replaced `CustomerNode`, `AppNode`, `DbNode`, `HotDbNode`, `ArchiveDbNode` (5 node types). Sublabels preserved as children. Removed `Database`, `Globe`, `HardDrive`, `Server` icon imports. Removed unused `FLASH_BORDER`/`FLASH_BG`.
- `Level47ErrorMonitoring.tsx`: Replaced `CustomerNode`, `AppNode`, `MonitorNode`. Error log, error count, grouped errors, context, alert status preserved as children. Removed `Monitor`, `Search`, `Server` icon imports. Kept `AlertTriangle`, `Bell`, `Bug` for children content. Removed unused `FLASH_BORDER`/`FLASH_BG`.

**Verification:** `bunx tsc --noEmit` zero errors, `bunx biome check --write` formatted, `bun run build` succeeds, all 1502 tests pass.
