# 2026-05-07

## L13 Strong Params: real security lesson via `to_unsafe_h` -> `params.expect`

Work spans 2026-05-06 (skill/rule commit) and 2026-05-07 (Phase A + Phase B curriculum changes). Case study dates inside this entry reference 2026-05-06 where those incidents occurred.

### What changed

**Skills + rules** (commit `7997523`):
8 drafts across 4 files codifying lessons learned from the multi-iteration L13 redesign:
- `pedagogy.md` § Cumulative patterns: replaced single-axis framing with two-axis (existence vs form). Added probes-are-problems subsection.
- `audit-level/SKILL.md`: added Question 7 to Step -1 (canonical bug class check), Type 2 disqualification gate, corrected wrong "Type 4 should be Type 2" bullet in audit-discipline case study.
- `design-level/SKILL.md`: added Step 0.5 (lock canonical purpose via WebFetch + citation, form-axis only).
- `audit-level/cumulative-patterns.md`: prepended two-axis framing intro, appended lesson-survival outcome to L7 case study.

Every new section has an L13 case study with Rails docs citations.

**L7-L12 controllers strip** (uncommitted):
Replaced explicit-field extraction with `params[:product].to_unsafe_h` (the naive Rails 8 shortcut from `ActionController::Parameters` docs):
- `Level7Controller.tsx` getActionBody (create + update) + comment.
- `level-7-controller.ts` railsCodeExample + the "note on parameter handling" paragraph.
- `Level8Serializers.tsx` (2 sites).
- `Level9Authentication.tsx` (4 sites: observe, build-step-1, after-step-4 controller for both create and update).
- `level-11-authorization.ts` (1 site).
- `Level12Validations.tsx` (2 sites: stage inspector + reward controller).
- `level-12-validations.ts` (1 site).

The earlier explicit-field strip was lesson-survival-incorrect: it left the pre-L13 form mass-assignment-safe by construction, neutering L13's security lesson. The corrected form (`to_unsafe_h`) carries forward the actually-vulnerable pattern that L13 fixes.

**L13 redesign** (uncommitted):
Full rewrite around mass-assignment exploits:
- Pre-state: controllers use `to_unsafe_h`. Marketing has added a `featured: boolean` column on Product (admin-only homepage flag).
- Three problem-probes (all real exploits, all blocked after the build):
  - `self-promote-create`: POST with `featured: true` -> product saved as featured.
  - `self-promote-update`: PATCH with `featured: true` -> existing product flipped.
  - `compound-frame`: PATCH with `featured: true, user_id: 99` -> featured AND ownership transferred to victim.
- Build phase: replace `to_unsafe_h` with `params.expect(product: [:name, :description, :price])`, centralized into `product_params` private method, called from both create and update.
- Reward: same three exploits now blocked; clean create/update still pass.
- DISCOVERY_DEFS, PROBES, PROBE_DISCOVERY_MAP, PROBE_PIPELINE_MAP, STAGE_INSPECTOR_MAP, STRESS_SCENARIOS, FILTERING_OPTIONS, WHITELIST_OPTIONS, OPTION_STEP_CONFIG, STEP_DEFS, PROBE_ACTIVE_CONNECTIONS, SCENARIO_ACTIVE_CONNECTIONS, getCodeFiles, header comment, and content/level-13-strong-params.ts (trigger, problem, codeExample, learningContent, hint) all rewritten.

### Why

The previous session's L13 design ran through three failure modes:
1. Pre-baked `params.require/permit` at L7 (earned-abstraction violation).
2. Stripped to explicit-field extraction (lesson-survival violation: form is mass-assignment-safe by construction; the bug L13 fixes does not exist in the before-state).
3. Initial fix proposal scoped `to_unsafe_h` to L13's before-state only — would have created a localized regression from L7-L12's safer form.

A separate misstep overgeneralized "every concept has a naive precursor" — false. Authentication (L9) is existence-axis with no precursor. The corrected framing: **cumulative-patterns operates on two orthogonal axes (existence vs form); only form-axis levels have precursors, and those precursors must satisfy both earned-abstraction (no future API) AND lesson-survival (the bug the future level fixes still exists).**

Per the [Rails Action Controller guide](https://guides.rubyonrails.org/action_controller_overview.html): *"With Action Controller Strong Parameters, parameters cannot be used in Active Model mass assignments until they have been explicitly permitted."* That is the canonical purpose; the L13 redesign is built around it.

### Verification

- `bunx tsc --noEmit`: clean (0 errors).
- `bunx biome check`: clean (1 file auto-formatted).
- `bun run build`: succeeds.
- `bun test`: 1522 pass / 0 fail / 4873 expect calls.

### Still pending

- **Phase C: myapp re-pass.** Update `project/myapp/` controllers L7-L12 from explicit-field to `to_unsafe_h`. Add migration for `featured` column at L13. Re-tag commits `level-7` through `level-13`.
- **audit-level skill check on L13.** The Skill should be invoked formally before commit per the audit-after-every-level rule.
- **Commit approval.** Phase A + Phase B are uncommitted in working tree.
