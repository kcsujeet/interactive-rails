# Session: 2026-02-24 #2

## Goal
Fix CORS narrative flow: swap L15/L16, reframe CORS as "connect the frontend," establish curl as the testing tool.

## Problem
The game's narrative had a contradiction: L15 CORS said "the React frontend cannot call the API" but if that were true, all prior levels were filtering requests that could never arrive from a frontend. In reality, developers test APIs with curl first, and CORS only matters when a browser-based frontend connects.

## Changes Made

### 1. `frontend/src/features/act1-foundation/content.ts` (L6 Controller)
- Updated trigger description to mention "curl is your client. No browser frontend yet."
- Added "Testing your API" section to conceptExplanation about using curl/Postman

### 2. `frontend/src/features/act2-users-security/content.ts`
- Swapped level order: Scopes & Enums is now L15, CORS is now L16
- Updated level IDs: `act2-level15-scopes-enums`, `act2-level16-cors`
- Updated level numbers accordingly
- Reframed CORS trigger: "Your API is secured, tested, and ready. Now a React frontend needs to call it"
- Reframed CORS observation: "You have been testing with curl, which bypasses browser security"
- Reframed CORS rootCause: "curl sends requests directly, so CORS never mattered until now"
- Updated whenToUse: "curl and mobile apps are not affected by CORS"
- Removed cors-node from Scopes & Enums startingPipeline (CORS not taught yet at L15)
- Updated Act 2 description to end with "then connect a React frontend"
- Updated file header comment for level ordering

### 3. `frontend/src/features/levels-registry.ts`
- Swapped registry mappings: `act2-level15-scopes-enums` -> Level16ScopesEnums, `act2-level16-cors` -> Level15CORS
- Updated comment header for act 2 level order

### 4. `frontend/src/features/act2-users-security/components/Level15CORS.tsx`
- Changed levelNumber from 15 to 16
- Rewrote scenario text to reference curl testing and "you never noticed"

### 5. `frontend/src/features/act2-users-security/components/Level16ScopesEnums.tsx`
- Changed levelNumber from 16 to 15
- Updated completeLevel ID from `act2-level16-scopes-enums` to `act2-level15-scopes-enums`

### 6. `docs/spec.md`
- Swapped rows 15 and 16 in the Act 2 table
- Updated CORS scenario text

## Verification
- `bun run build` passes cleanly
- No stale references to old level IDs in source code (only in historical session log)
