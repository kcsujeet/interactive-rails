# Session Log: 2026-07-12

## Homework exercises for Acts 6 and 7 (all 19 levels)

### What changed

Authored `learningContent.homework` arrays for every level in Act 6 (L40-L50) and Act 7 (L51-L58), mirroring the Act 1 pattern (`HomeworkExercise[]`: task, optional commands, verify). Files touched:

- Act 6 content: `level-40-middleware.ts` (3 exercises), `level-41-cors.ts` (3), `level-42-rate-limiting.ts` (3), `level-43-safe-migrations.ts` (2), `level-44-recurring-jobs.ts` (3), `level-45-data-lifecycle.ts` (2), `level-46-error-monitoring.ts` (3), `level-47-observability.ts` (3), `level-48-api-versioning.ts` (2), `components/level-49-deployment/data/content.ts` (3), `components/level-50-feature-flags/data/content.ts` (3).
- Act 7 content: `level-51-multi-database.ts` (2), `level-52-multi-tenancy.ts` (2), `level-53-sharding.ts` (3), `level-54-state-machines.ts` (3), `level-55-modular-monolith.ts` (3), `level-56-domain-events.ts` (2), `level-57-api-gateway.ts` (2), `level-58-architect.ts` (3).
- New tests: `act6-operations/__tests__/homework.test.ts` and `act7-scale/__tests__/homework.test.ts`, mirroring `act1-foundation/__tests__/homework.test.ts` (coverage, substantive-text, doc-verified anchor commands; Act 7 adds a guard that L54 never reinstalls PaperTrail).
- `src/lib/__tests__/cross-references.test.ts`: pinned two new entries (`level-22-eager-loading.ts` -> The N+1 Problem, `level-36-background-jobs.ts` -> Action Mailer). The Act 4/5 homework pass (uncommitted, parallel work) added "L21"/"L35" references in task text; both references verified accurate against the acts registry.

### Why

Homework bridges each level to the player's companion Rails app (renders on the post-completion screen). Acts 1-5 already had them; Acts 6-7 were the gap.

### Design decisions

- Heavy-infrastructure levels scale down to laptop versions: L51 uses a same-database `replica: true` entry (ReadOnlyError proves the role split), L53 uses two local PostgreSQL databases as shards, L49 validates `deploy.yml` with `kamal config` and a local `docker build` instead of a real server, L58 is a written-design exercise (extraction memo + staged migration plan with per-stage exit criteria and rollback) plus a second local Rails app on port 3001.
- Commands restricted to what each level teaches or standard CLI: rack-cors preflight verified with `curl -X OPTIONS` (browser-enforced semantics, disallowed origin still gets 200 but no permission header), lograge JSON formatter + custom_payload, OpenTelemetry `use_all` with the console exporter, deep `/up` gated on `SolidQueue::Process` heartbeat, Packwerk via `bundle add packwerk && bundle binstub packwerk` + `bin/packwerk init/check`, Flipper via `bin/rails generate flipper:setup`.
- L54 wires `has_paper_trail` without reinstalling the gem (already present from the audit-trail work); a test enforces this.
- L43 trips strong_migrations with a column rename (adapter-independent check) rather than a Postgres-only pattern.

### Checks

tsc clean, biome clean on all 22 touched files, build succeeds, `bun test src scripts`: 1817 pass / 0 fail (7 new homework tests). No em dashes.

## Answer-leak triage: fixed 43 wrong-option feedback strings across 16 levels

### What changed

Triaged all 139 candidates from `bun scripts/report-answer-leaks.ts` against the CLAUDE.md rule (wrong-option feedback must explain why the choice fails without naming the correct answer). 43 feedback strings were real leaks and got rewritten; the rest were generic-vocabulary noise. Every rewrite keeps the original teaching substance but replaces the "here is the right answer" sentence with consequence- or concept-based reasoning. Each edit was applied to both the component and its mirrored test string.

Files touched (component + mirror test where one exists): L20 error handling (2 strings; named the `error`/`code`/`message`/`details` response shape), L24 indexing (1; "published must be the leftmost column"), L25 counter caches (1; named `belongs_to`), L28 caching (2; named `expires_in` and `touch`), L10 encryption (2; named "Rails credentials", mirror lives in `Level36Encryption.test.ts`), L30 polymorphic (5; named `{polymorphic}`, `polymorphic: true`, `has_many`, the `as:` option, and `CreateReview.call`), L31 soft deletes (3; named `.kept` twice and `PaperTrail.request.whodunnit`), L32 transactions (4; identify-step feedback restated the correct label verbatim, plus "raise to abort" / "built-in rollback exception"), L34 active storage (1; named `has_one_attached`), L39 webhooks (2; mirrored the correct labels "atomic operation" and "Background jobs (Solid Queue)"), L40 middleware (5; named `config.middleware.use` twice, enumerated the winning "structured JSON + timing + request ID" combo, described the correct bot-blocking code), L42 rate limiting (3; named `within:`, "IP ranges instead", "Retry-After header"), L43 safe migrations (1; named `target_version` and `start_after`), L44 recurring jobs (3; "use explicit retry/discard", enumerated "counts, timing, structured", "use structured logging"), L45 data lifecycle (7; gave exact tier boundaries, "model layer", "hot table first then archive", "retention period", "always log destruction", "nightly window", "weekly window"), L48 API versioning (1; "delegate to a service and use a dedicated v1 serializer").

Judged noise (no edit): candidates where the flagged token is generic English ("result", "user", "error"), level-saturated context (the level/step title already names the concept, e.g. L37 "Generate Channel", L48 "Add Deprecation Headers"), or conceptual pointing of the kind CLAUDE.md's GOOD examples allow ("API-only mode" style). Content files were off-limits this pass; no real leaks were found in them.

### Why

The heuristic report existed but had never been fully triaged; several feedback strings handed the player the exact API token or restated the correct option's label, defeating the explore-and-learn loop.

### Checks

Report count 139 -> 84 (all remaining are noise). tsc clean, biome clean on all 47 touched files, build succeeds, `bun test src scripts`: 1817 pass / 0 fail. No em dashes in any new string.

## Main-loop items (same batch)

- **L54 redundant PaperTrail install removed.** L31 already ran the full installer; L54's step 4 re-ran it. The step is gone (5 steps now), the audit step acknowledges the existing versions table, and the probe tells the honest story (products versioned since L31; Order never opted in). The enum wrong-option now uses string-encoded values per the conventions doc. New Level54StateMachines.test.ts pins all of it (written first, red, then the edits).
- **Enum gap closed.** L54's content now teaches the ladder explicitly: bare string -> enum (predicates, bang setters, scopes, string-encoded always) -> state machine (transition rules, guards, audit). Enum has been in myapp since level-15 but was never taught anywhere; L54 is its natural home as the named precursor.
- **myapp catch-up: level-16 and level-17 tagged.** Real Rails work in project/myapp with RSpec TDD: AddSignupDefaultsToUsers migration + fat UsersController commit (pre-16 state), then UserRegistration + ApplicationService with a service spec and the thin controller keeping the session token (level-16, 14 examples green); AddFlaggingToProductsAndReviews migration + drifted Product/Review copies commit (pre-17 state), then the Flaggable concern with a concern spec (level-17, 17 examples green). Ground truth now matches both redesigned levels. L18+ remains future work.
- **Boss levels designed.** docs/boss-levels.md: act-end free-input recall tests validated by world-state predicates, a hint ladder that never reveals commands (concept -> shape -> level reference), full implementable spec for the Act 1 boss ("First Contract"), and one-line sketches for Acts 2-7. No implementation yet.
- **Verified full gauntlet over the combined tree** (four agent workstreams + main loop): tsc clean, 1817 unit tests pass, build green, 59 e2e pass, answer-leak report down from 139 to 84 (all remaining verified noise).
