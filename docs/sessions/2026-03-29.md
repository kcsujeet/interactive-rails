# Session: 2026-03-29

## Act 6 Level Design & Implementation

Designed all 7 levels in Act 6 (Reliability) with narrative arc, actor tables, visualization concepts, probe/reward pairs, and build steps. Then started implementing.

**Act 6 narrative arc:** Visibility (L41) -> Protection (L42) -> Safety nets (L43) -> Zero-downtime deploys (L44) -> Automated maintenance (L45) -> Data management (L46) -> Observability (L47)

### Level 41: Middleware & Rack - COMPLETE

Full rewrite from 4-step progression (no observe, no visualization) to three-phase React Flow visualization.

**Visualization:** 3-node vertical stack (Client -> Middleware Stack -> Rails App). In observe, middleware is "Empty Stack" with no layers. In reward, middleware shows three sub-panels: Bot Filter, Request ID, Logger.

**3 probes:** Customer reports 500 with no request ID, bot scrapes catalog undetected, debug with no middleware logging. Each shows requests flowing through empty middleware straight to Rails.

**4 reward scenarios:** Same 3 paired + health check showing full pipeline.

**6 build steps:** Create middleware file, insert into stack, implement request ID, implement logger, implement bot detection, configure ordering.

### Level 42: Rate Limiting - COMPLETE

Full rewrite from interactive strategy selector to three-phase React Flow visualization.

**Visualization:** 3-node fork layout (Bot/Attacker top-left, Customer bottom-left, Rails App right with IP/Login rate limit sub-panels). In observe, app has "No Rate Limiting". In reward, sub-panels show IP throttle and login throttle status.

**3 probes:** Bot floods API at 10K/sec, attacker brute-forces login, customer locked out during attack.
**4 reward scenarios:** Same 3 paired + internal service safelist.
**6 build steps:** Rails 8 rate_limit, install rack-attack, IP throttle, login throttle, safelist, 429 response.

### Level 43: Soft Deletes & Audit Trails - COMPLETE

Full rewrite to three-phase React Flow. 3-node horizontal (Admin, App, Database with mini-table rows).
3 probes: hard delete permanent, no restore, no audit trail. 4 reward scenarios.
6 build steps: discard gem, discarded_at migration, configure model, update queries, paper_trail, configure audit.

### Level 44: Safe Migrations - COMPLETE (via background agent)

Full rewrite to three-phase React Flow. 3-node (Developer, Database with lock sub-panel, API Requests).
3 probes: add_column default lock, change_column lock, add_index lock. 4 reward scenarios.
6 build steps: strong_migrations gem + generator, fix add_column, fix change_column, fix add_index, configure safety.

### Level 45: Recurring Jobs & Scheduling - COMPLETE (via background agent)

3-node (Scheduler, App, Database with row count). Probes show expired tokens accumulating, orphaned records, growing storage. Reward shows periodic cleanup.

### Level 46: Data Lifecycle - COMPLETE (via background agent)

3 nodes in observe (Customer, App, single DB with 50M rows), 4 nodes in reward (Customer, App, Hot DB, Archive DB). Probes show slow queries, old order lookup, 8hr backup. Reward shows hot table 2.5M rows, transparent archive reads, fast backups.

### Level 47: Error Monitoring - COMPLETE (via background agent)

2 nodes in observe (Customer, App with errors vanishing), 3 nodes in reward (Customer, App, Error Monitor). Probes show unnoticed 500s, ungrouped errors, no alerting. Reward shows captured errors with context, grouping, error budgets.

### Final Verification

All 7 Act 6 levels (L41-L47) implemented and passing:
- TypeScript: 0 errors across all 7 components
- Biome: clean
- Build: success
- Tests: 1105/1105 pass (3827 expect() calls)

Each level: 1593-1949 lines, 3-phase React Flow visualization, discovery gating, probes with stories, shuffled options, LevelHeader, StressTestPanel with individual props.

### Remaining: Audits

All 7 levels need auditing per the audit-level skill. Designs:
- L42 (Rate Limiting): Horizontal flood visualization, bot vs legitimate traffic
- L43 (Soft Deletes): Database table with records appearing/disappearing/flagged
- L44 (Safe Migrations): Table lock diagram with request queue
- L45 (Recurring Jobs): Accumulation counter that periodic job resets
- L46 (Data Lifecycle): Two-tier storage split (hot vs archive)
- L47 (Error Monitoring): Error flow timeline (vanishing vs captured)
