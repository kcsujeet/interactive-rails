# Session Log: 2026-07-05

## Session 1: Spec reconciliation + spec-to-registry consistency test

### Context

Discussed overall project direction. Key decision recorded to memory: this project is a personal learning tool (the goal is expert-level Rails for the author), not a product to ship. Agreed priority order for improvements: (1) spec reconciliation + consistency test, (2) executable validators (answer-leak linter, structural checks), (3) Playwright smoke tests per interaction archetype, (4) free-input terminal mode, (5) myapp homework bridge and act-end boss levels.

### What was changed

1. **`src/lib/__tests__/spec-consistency.test.ts` (new, TDD-first).** Parses `docs/spec.md` and pins it to the live curriculum in `src/lib/acts-registry.ts`:
   - act count, act names, and per-act level ranges from headings (`## ACT N: Name (Levels A-B)`)
   - every level number and name in every act table, exact and in order
   - the headline stats line (`**58 levels, 7 acts**`) and intro (`58-level`)
   - the Rails 8 Features Integration table: each feature row's level references are computed from the registry via a human-verified feature-to-level-name map, so renumbering can never silently invalidate the table
   - Written first and run against the old spec: 22 of 31 tests failed, confirming the drift detection works. All 31 pass after the rewrite.

2. **`docs/spec.md` (rewritten).** The old spec had drifted badly from the shipped curriculum:
   - claimed a "55-level progression" in the intro while stats said 58
   - Act 2 was titled "Guards & Gates" with 7 levels including CORS at L15; the real Act 2 is "Users & Security" with 6 levels, CORS lives in Act 6 as L41
   - Encrypted Attributes was listed at L36 (Act 5); it is actually L10 (Act 2)
   - Callbacks & Normalizations was listed at L11; it is actually L15 (Act 3, matching the current level-15-callbacks work)
   - Act 1 ordering was wrong (spec: CRUD L4, Routes L5, Controller L6, Associations L8; real: Associations L4, CRUD L5, Routes L6)
   - Action Mailer / Background Jobs were in Act 3; they are actually L35/L36 in Act 5
   - the Rails 8 feature table had `rate_limit` at L43 (real: L42) and Kamal at L42 (real: L49), plus stale numbers for `params.expect` (L14 vs real L13), `normalizes` (L11 vs L15), `generates_token_for` (L21 vs L35), Solid Queue (L22/L46 vs L36/L44), Solid Cache (L30 vs L28), `encrypts` (L36 vs L10), `Current` (L12 vs L11)
   - All tables rewritten from the registry. Scenario column condensed from each level's actual `trigger.description` (dumped from the registry, not paraphrased from memory). The stale per-act "Tests?" columns were dropped. Added a "Source of truth" note pointing at the registry and the new test. Noted that `act8-mastery` is an empty stub directory, not part of the live curriculum.

### Why

`docs/spec.md` is the document CLAUDE.md and the audit skills instruct every session to cross-reference for level scenarios and concepts. A drifted spec silently corrupts every audit that trusts it. Since the project's purpose is the author's own learning, content accuracy is the top priority; this closes the largest known accuracy hole and makes the whole class of drift a failing test instead of a human catch.

### Checks

- `bunx tsc --noEmit`: clean
- `bunx biome check --write` on the new test: clean
- `bun run build`: success
- `bun test`: 1667 pass, 0 fail (31 new)

### Learned

- The spec was not just numerically stale; the act composition itself had been restructured (CORS moved to Operations, Encryption pulled forward to Act 2, Callbacks pushed to Act 3) without the spec ever being updated. Registry code comments (`levels-registry.ts` header) were accurate; prose docs were not. Executable pins beat prose.
- Session log housekeeping: pruned the 5 oldest logs (2026-04-11 through 2026-04-26) to respect the 10-log cap.

## Session 2: Executable validators + free-input terminal + Playwright smoke + homework bridge

Four workstreams executed in one batch (user approval: "do all in one go, review at end").

### 1. Curriculum-wide validators (executable rules)

- **`src/lib/testing/level-source-analysis.ts` (new, TDD-first, 16 unit tests).** Parses level source with the TypeScript compiler API (most levels keep choice data as unexported module-scope consts, so imports cannot introspect them). Extracts option groups (arrays of objects with a `correct` flag), PipelineFlow JSX tags, and PROBE_DISCOVERY_MAP literals. Decision-modal `consequence` counts as the feedback surface.
- **`src/lib/__tests__/curriculum-quality.test.ts` (new sweep, 5 tests).** Hard-enforces across all 58 levels: exactly one correct option per group, correct never first in source order, every wrong option has feedback, no mixed option colors, feedback never quotes the correct answer verbatim, no discovery unlocked by two probes, every PipelineFlow tag passes activeConnections explicitly, no em dashes anywhere in src.
- **`scripts/report-answer-leaks.ts` + `bun run report:leaks` (new).** The token-level leak heuristic produces 160 candidates, mostly generic-Rails-vocabulary noise, so it ships as a report for the manual audit pass instead of a CI gate. The verbatim phrase-quote variant IS gated.
- **Violations found and fixed:**
  - L7 (Controller): PROBE_DISCOVERY_MAP was many-to-many; the observe gate could clear without firing the DELETE probe. Added a fifth discovery ("All five REST actions are dead") and made the map 1:1 (probes: get-fails / post-fails / all-actions-dead; stage clicks: routes-work / no-controller).
  - Correct-answer-first reorders: L31 soft-deletes (2 arrays), L54 state machines (1 array).
  - 6 real answer leaks rephrased (components + mirrored tests): L9 feedback named the built-in "authentication generator"; L21 named pg_query; L26 named OPTIONS/:limit and the pagy() call; L28 named db:prepare; L55 named bin/packwerk and init.
  - 69 em dashes purged across 25 src files (comma/colon replacements; describe() titles use colons).
  - PipelineFlow activeConnections wired in all 7 offending files: L20/L29/L35/L36 observe+reward now burst all edges on probe/scenario fire (probeTick / stressTest.results.length as animationTick) and stay dormant before; L49 observe/build pass explicit [] (their edges have no dots, so behavior-identical); L49 reward bursts on fire. Emptied the KNOWN_AUTO_ANIMATING_EDGES baseline in scripts/__tests__/level-reveal-consistency.test.ts.

### 2. Free-input terminal mode (recall over recognition)

- **`src/components/levels/terminal-input-matching.ts` (new, TDD-first, 8 tests).** Case-sensitive, whitespace-normalized matching of typed input against each command's `command` or `label`. `FREE_INPUT_MISS_LIMIT = 2`.
- **`SimulatedTerminal.tsx` reworked.** The prompt line is now a real input: the player types the command (recall). Typing a wrong option's command shows its feedback (counts as a wrong answer). Unrecognized input echoes a shell-style error and counts as a miss but NOT a wrong answer (a typo is not a choice). After 2 misses the option buttons appear as the recognition fallback. Uniform across every terminal level via TerminalChoiceStep (no per-level opt-outs, per the consistency rule).

### 3. Playwright smoke tests

- Installed @playwright/test + Chromium; `playwright.config.ts` boots `astro dev` automatically; `bun run test:e2e`.
- **`e2e/levels.smoke.spec.ts`:** (a) all 58 level play pages render without uncaught page errors, asserting the level name or the uniform "LEVEL n" header chip (4 levels shorten their header names); (b) full free-input loop on L1: wrong command shows feedback, two misses reveal options, correct command advances the step. 59/59 pass in ~25s.
- `scripts/dump-level-ids.ts` bridges the React-heavy registry into the node-based Playwright runner.
- `package.json` "test" script scoped to `bun test src scripts` so bun's runner does not swallow the .spec.ts e2e file.

### 4. Homework bridge (game -> real terminal)

- **`HomeworkExercise` type** added to LearningContent (`homework?: { task, commands?, verify }[]`).
- **Complete page** (`complete.astro`) renders a "Now do it for real" section between the code example and common mistakes.
- **Act 1 pilot: all 8 levels** got homework exercises pointing at a real practice app. Every command verified against canonical docs on 2026-07-05: mise (mise.jdx.dev installing-mise + getting-started), `rails new --api` (guides.rubyonrails.org/api_app.html), generate model / db:migrate / console / routes -g (guides.rubyonrails.org/command_line.html), `rails g serializer` (jsonapi-serializer README). Sources cited in `src/features/act1-foundation/__tests__/homework.test.ts`, which pins the anchor commands.

### Deliberately deferred

- **Boss levels** (act-end recall levels): designing 7 new levels in one pass would produce exactly the half-baked content this project exists to avoid; each needs the design-level skill pipeline. Homework for Acts 2-7 likewise authored later, act by act, with doc verification per act.
- **Token-level leak review**: 154 remaining report candidates need the human "does this name the answer?" judgment, one by one, in a dedicated audit pass.

### Checks

- `bunx tsc --noEmit` clean; `bunx biome check --write` clean (341 files); `bun run build` succeeds; `bun run test` 1699 pass / 0 fail (51 files); `bun run test:e2e` 59 pass / 0 fail.

### Learned

- The codebase already had a baselined activeConnections test (level-reveal-consistency.test.ts) whose baseline documented the exact 7 files the new sweep found; fixing the files emptied the baseline. Baselines document debt; sweeps force paydown.
- bun's test runner discovers `.spec.ts` files, so Playwright specs must live outside the `bun test` roots.
- The e2e render sweep caught a real class of assertion drift on day one: four levels shorten their header names relative to the registry (L29, L44, L46, L56).

## Session 3: Curriculum continuity fixes (learning-flow review follow-up)

A learning-flow review of level designs and placements found the design system strong (Act 4 exemplary) but flagged narrative continuity bugs from past reorderings. This session fixed the textual ones and assessed the one structural change.

### Timeline contradictions fixed (3 planned)

1. **L49 Deployment**: trigger said the app "still lives on your laptop," contradicting L42 (bots at 10K req/sec), L43 (deploy locks a table, 100K users affected), and L47 (3am pages). Reframed trigger + observe scenario to manual-scp-deploy framing (deploys exist, they are just bad), which the level's own problem/codeExample already used. spec.md row updated.
2. **L11 Authorization**: trigger referenced "data is validated" (L12) and "emails are normalized" (L15), both taught later; fossil from the pre-reorder curriculum. Now references only what exists by L11 (auth from L9, encryption at rest from L10).
3. **L41 CORS**: framed as "the first React frontend arrives," contradicting storefront/homepage narratives live since L13-L15. Reframed across trigger, observation, rootCause, and the component's scenario panel: the storefront and API always shared one origin (so CORS never triggered); the frontend splitting onto its own origin is what surfaces it. Kept rack-cors mentions: step 0 tests HOW to add the gem, not WHICH gem, so naming it is not an answer leak.

### Stale cross-references fixed (7 more, found during the swap assessment)

Number-based cross-references had rotted across reorderings:
- L58 capstone: "Domain Events (Act 7, Level 52)" -> Level 56.
- L46 (x3 surfaces): request logging cited as L41 -> L40 (Middleware); observability cited as L53 -> L47 (twice).
- L47: "RequestLogger middleware from L41" -> L40.
- L50: "APM (L52)" -> L47; "structured error monitoring (L48)" -> L46.
- L49: "L44 Safe Migrations" -> L43.
- L44: "Solid Queue (L22)" -> L36.
- L31 (forward reference): scenario claimed "protected from bots (L42)" as existing infrastructure at L31; fossil from when soft deletes lived in Act 6 after rate limiting. Rephrased to auth/authorization (which do exist by L31).

All other number references audited and confirmed correct (L4->L3, L7->L6, L13->L7, L15->L9/L10, L18->L16, L27->L16, L30->L18, L44->L36, L46->L40, L50->L46, L9->L2, L7->L20, L10->L34 forward-safe as learningContent).

### L52/L53 swap assessment (Sharding before Multi-Tenancy, report only, NOT executed)

Finding: L52's "shard by tenant" presupposes the tenant concept taught at L53 (same foundation-gap class as the L39 webhooks case). Swap blast radius is contained:
- 2 directory renames + 2 content file renames + ids inside (act7-level52-*, act7-level53-*), levelNumber fields, act.ts ordering, levels-registry keys + header comment, spec.md rows (pinned by the consistency test, so forced).
- No myapp tags exist for 5x levels (ground truth stops at level-15). No e2e coupling (smoke tests derive from the registry).
- Renaming ids invalidates stored completion for those two levels only (sole player; trivial).
- Both levels need a narrative pass post-swap (new L52 multi-tenancy trigger must not reference sharding; new L53 sharding leans on tenancy) plus audit-level on both.
Estimated one focused session. Not executed pending user decision.

### Remaining flow findings (from the review, not yet addressed)

- L4 Associations before L5 CRUD: L4 exercises console skills taught in L5; consider swap or verify L4 stays schema-level.
- Validations (L12) arguably belongs before Authorization (L11).
- L15/L16 welcome-email narrative leans on Action Mailer, 20 levels away at L35; verify framing doesn't imply the player can send email yet.
- L54 State Machines is a domain-modeling level in the Scale act; natural home is Act 5 near Transactions/Locking.
- Enum gap: the L15 tightening promised status enums "a proper home in a future modeling level"; no such level exists, so string-encoded enums (mandated by rails-conventions.md) are taught nowhere.

### Checks

- tsc clean, biome clean, build succeeds, bun test 1699 pass / 0 fail.

### Learned

- Number-based cross-references (L40, L47...) are the least refactor-safe content surface in the curriculum: every reorder silently invalidates them, and 7 of ~20 had rotted. Candidate future validator: pin each cross-reference to a level NAME in a test (numbers derived from the registry), like the spec feature table does.

## Session 4: Cross-reference pinning test + L52/L53 swap

### 1. Cross-reference pinning test (TDD-first)

`src/lib/__tests__/cross-references.test.ts`: scans every non-test file under src/features for level-number tokens (`L40`, `Level 56`, `Act 7, Level 56`). Self-references verify automatically against the file path; every cross-reference must be pinned to a level NAME in the test's map, with expected numbers derived from the acts registry. A second check verifies every "Act X, Level Y" pair against the registry's actId. "PCI Level 1" is the one declared false positive.

Written first; failed on 16 stale sites, which turned out to be double the 7 fixed in session 3:
- L58 capstone recap: 7 of 8 concept/level pairs were old-numbering (including two "Act 8" citations; there is no Act 8). All corrected (Multi-Database L51, State Machines L54, Multi-Tenancy L53, Observability Act 6 L47, Modular Monolith L55, API Gateway L57, Sharding L52).
- L58 component: "Solid Queue jobs (L22+)" -> L36.
- L54: "paper_trail installed in L43" -> L31.
- L53 (now L52) multi-tenancy content: "APM (L52)" -> L47.
- L22: "Level 23 exposed the N+1 problem" -> Level 21.
- L28: "HTTP caching belongs to Level 31" -> Level 29.
- L42: "In Level 41, you added middleware" -> Level 40.
- L46 + L47: two more stray "L41" middleware references -> L40 (session 3 caught only the first per file).
- L20: "L40 deprecation flow" -> L48 (API Versioning).

### 2. L52/L53 swap executed (Multi-Tenancy now L52, Database Sharding now L53)

Rationale: sharding's trigger says "shard by tenant"; the tenant concept must come first (same foundation-gap class as the L39 webhooks case). Mechanics, all via git mv + content rewrites:
- Renamed component dirs + files (Level52MultiTenancy.tsx, Level53Sharding.tsx), content files, exported const/function names, ids (act7-level52-multi-tenancy, act7-level53-sharding), levelNumber fields, registerLevelCode calls.
- act.ts import order, levels-registry keys/loaders + header comment, spec.md Act 7 rows.
- Sharding trigger now reads "...shard by tenant, the isolation boundary you drew in L52", pinned in the new test.
- The pinning test and the reveal-consistency path snapshot flagged exactly the follow-ups (re-pin two content files, update two snapshot paths): the designed workflow working on its first real reorder.
- Note: full structural audit-level passes on the two swapped levels were NOT rerun; mechanics and probes are untouched, only numbering and one trigger sentence changed. Both levels remain legacy single-file and are due a proper audit when next touched substantively.

### Also noted (not fixed)

- L54 State Machines step 4 re-runs `rails generate paper_trail:install`, but L31 already installed PaperTrail. Cumulative-infrastructure violation to resolve in a proper L54 audit.
- E2E gotcha: a stale `astro dev` on 4321 (left from a manual check) made all 59 smoke tests fail via `reuseExistingServer`; kill the orphan before judging e2e results.

### Checks

- tsc clean, biome clean, `bun run build` succeeds, `bun run validate:levels` passes (58 levels / 7 acts), `bun run test` 1701 pass / 0 fail, `bun run test:e2e` 59 pass / 0 fail.
