# Session: 2026-02-25 #1

## Goal

Remove Level 15 (Scopes & Enums) from the curriculum and renumber all downstream levels. The concept was too lightweight for its position after Authorization, Testing, and Strong Params. No replacement fits naturally; the security arc is complete at L14 Strong Params, and CORS closes Act 2.

## Changes

### Deleted

- `frontend/src/features/act2-users-security/components/Level15ScopesEnums.tsx`
- `frontend/scripts/renumber-levels.ts` (temporary renumber script, created and deleted in this session)

### Renumbered (41 component files)

All levels L16-L56 shifted to L15-L55:

- **Act 2:** L16 CORS -> L15 CORS
- **Act 3:** L17-L23 -> L16-L22 (Service Objects through Background Jobs)
- **Act 4:** L24-L32 -> L23-L31 (N+1 Problem through HTTP Caching)
- **Act 5:** L33-L40 -> L32-L39 (Polymorphic through API Versioning)
- **Act 6:** L41-L47 -> L40-L46 (Middleware through Error Monitoring)
- **Act 7:** L48-L53 -> L47-L52 (Multi-Database through Domain Events)
- **Act 8:** L54-L56 -> L53-L55 (API Gateway through The Architect)

Each renamed component had these internal references updated:
- Export function name and default export
- `levelNumber={N}` JSX prop
- `completeLevel('actX-levelN-slug')` IDs
- Header comment level numbers

### Content.ts updates (7 files)

For each act's content.ts (Acts 2-8):
- Level IDs: `actX-levelN-slug` decremented
- `levelNumber: N` decremented
- Variable names: `level{V}{Name}` decremented
- Section comment headers updated
- Act header comment ranges updated to new level ranges

### Act 2 content.ts (additional)

- Removed entire `level15ScopesEnums` definition (220 lines)
- Removed `level15ScopesEnums` from `actTwo.levels` array
- Removed `'scope'` from `actTwo.unlockedNodes`
- Updated act description to remove "query scopes"

### Enum/scope concepts woven into existing content

- **L10 Validations** (`act2-users-security/content.ts`): Added note to `conceptExplanation` about Rails 8 enums providing both validation and query scopes for fixed-value fields.
- **L19 Query Objects** (`act3-clean-architecture/content.ts`): Added "Scopes vs. query objects" paragraph explaining named scopes as the simpler alternative for single-purpose filters.

### Registry updates

- `frontend/src/features/levels-registry.ts`: Rewrote with 55 entries, updated all imports, registry keys, component references, and docstring (55 levels, new act ranges)
- `frontend/src/features/acts-registry.ts`: Updated count from 56 to 55

### Documentation updates

- `docs/spec.md`: Removed Scopes & Enums row, removed `enum` from Rails 8 Features table, renumbered all acts, updated stats to 55
- `docs/content-structure.md`: Updated hierarchy tree, status counts, component file list, pipeline references, topics by act
- `docs/game-mechanics.md`: Updated progression table with new ranges and counts

## Approach

1. Manually deleted L15 component and removed from Act 2 content.ts
2. Wrote a Bun script (`renumber-levels.ts`) for bulk renames. First run had a cascading replacement bug in content.ts (iterative high-to-low `replaceAll` caused numbers to cascade down). Fixed by switching to single-pass regex with `replace()` callback that decrements numbers >= 16 in one pass.
3. Manually rewrote `levels-registry.ts` (simpler than scripting due to the comment structure)
4. Dispatched three parallel agents for doc updates (spec.md, content-structure.md, game-mechanics.md)

## Verification

- `bun run build` passes with zero errors
- `grep -r "ScopesEnums" frontend/src/` returns nothing
- `grep -r "act2-level15-scopes\|act2-level16" frontend/src/` returns nothing
- `grep -r "Level56" frontend/src/` returns nothing
- `grep -r "56 levels" docs/` only returns historical session log (2026-02-23)
- Registry has exactly 55 entries
- Content.ts level counts: 8+7+7+9+8+7+6+3 = 55
- L14 Strong Params still references `:status` (unchanged)

## Curriculum (post-change)

| Act | Range | Count | Name |
|-----|-------|-------|------|
| 1 | L1-L8 | 8 | The Foundation |
| 2 | L9-L15 | 7 | Guards & Gates |
| 3 | L16-L22 | 7 | Clean Architecture |
| 4 | L23-L31 | 9 | Performance |
| 5 | L32-L39 | 8 | Production Features |
| 6 | L40-L46 | 7 | Reliability |
| 7 | L47-L52 | 6 | Scale |
| 8 | L53-L55 | 3 | Mastery |
| **Total** | | **55** | |
