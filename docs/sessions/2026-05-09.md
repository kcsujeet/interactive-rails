# 2026-05-09

## Audit L1-L13 + fix all findings

Ran the audit-level Skill across all of Act 1 + Act 2 (L1-L13) via 13 parallel subagents. Verdicts: 2 PASS (L1, L2 with leakage flags), 1 PASS (L7 minor), 1 PASS (L13 1-low-sev drift), 9 FAIL. Two CRITICAL: L10 schema mismatch with myapp; L4 existence-axis violations (visualization showed L6/L7/L8 components that don't exist at L4).

Dispatched 5 parallel fix-agents. All findings addressed:

**Meta + structural fixes (I did directly):**
- `cumulative-patterns.md` Act 1 + Act 2 sections rewritten with correct numbering (was misnumbered: L7 Serializers / L8 Associations / L10 Validations / L11 Normalization / L12 Authorization / L13 Testing / L14 Strong Params). Now matches actual curriculum: L1 Env, L2 First Boot, L3 Model, L4 Associations, L5 CRUD, L6 Routes, L7 Controller (`to_unsafe_h` form-axis precursor), L8 Serializers, L9 Auth, L10 Encryption, L11 Authorization (Pundit + ownership), L12 Validations, L13 Strong Params, L14 Testing.
- L13 update-finder drift: `Current.user.products.find` → `Product.find` to match myapp's actual `level-13` tag (Pundit `authorize` enforces ownership). Component, content, and test mirror updated.

**Parallel fix-agents (5):**
- **L10 schema fix**: dropped fabricated `name` column, renamed `email` → `email_address` (myapp truth), phone now `deterministic: true`, added `downcase: true` on email_address signature, replaced inspection probe with verb-led `tail-query-logs`, dropped 3 duplicate stress scenarios, fixed `EncryptionError` claim per Rails source, removed pre-baked validations, added `story` fields.
- **L4 redesign**: replaced router/controller/serializer/response pipeline with model-only Type 4 visualization (Product ↔ Review nodes + schema). Probes rewritten to `bin/rails console` commands. Fixed `activeConnections`. Probe ↔ scenario 1:1 alignment. Scenario `story` fields added. Content leakage scrubbed.
- **activeConnections sweep on L6 / L7 / L11**: added PROBE_/SCENARIO_ACTIVE_CONNECTIONS maps + useMemo + props on every PipelineFlow render. L4/L6/L7/L11 removed from `KNOWN_AUTO_ANIMATING_EDGES` baseline.
- **Solution-leakage scrub on 12 content files (L1-L12)**: removed gem/method/class/command names from trigger / problem.goal / codeExample / learningContent / hint per .agents/rules/level-content.md ("describe outcomes, never implementation").
- **Tests added for L1, L5, L9** (42 new tests, 243 expects). Build-step quality, probe/scenario coverage, discovery wiring.

**Verification (all green):**
- `bunx tsc --noEmit`: clean
- `bunx biome check`: clean
- `bun run build`: succeeds
- `bun test`: 1580 pass / 0 fail (was 1538 before; 42 new tests)

**Files changed:** 21 modified + 3 new test directories + this session log.

---

## L10 (Encryption): align with myapp `level-10` tag, fix audit findings

### What changed

Reworked Level 10 to match `project/myapp` at the `level-10` git tag (User has `email_address`/phone (both deterministic) + address (default), no `name` column, no validations on User). Audit had flagged 11 issues across schema, pedagogy, leakage, and stress-test correctness.

**Files touched:**
- `src/features/act2-users-security/components/level-10-encryption/Level10Encryption.tsx`:
  - SAMPLE_USERS: dropped fabricated `name` column, renamed `email` -> `email_address` (matches myapp schema).
  - Build step 2 (encrypt email_address): correct answer is now `encrypts :email_address, deterministic: true, downcase: true`. Wrong options cover non-deterministic (login fails) and deterministic-only-no-downcase (case-sensitive login).
  - Build step 3 (encrypt phone & address): rewrote to match myapp. phone is `deterministic: true` (support agents look up by phone), address is default. Wrong options now penalize default-mode-on-phone (silent nil from find_by) and deterministic-everywhere (over-encryption tradeoff on a non-lookup field).
  - Probe 3 replaced: `inspect-config` (hypothetical inspection) -> `tail-query-logs` (verb-led action: tailing production logs reveals every WHERE value as plaintext). Distinct leak surface from sql-injection (primary DB) and backup-leak (offline copies).
  - Stress scenarios: dropped 3 duplicates (`sql-injection`/`attacker-dump`, `backup-leak`/`backup-safe`, `inspect-config`/`encryption-config`). Reframed `find-by-phone` as `find-by-phone-support` with `expectedResult: 'allowed'` since phone is deterministic. Added `find-by-address-fails` showing non-deterministic returns nil silently (per [Rails encryption docs](https://guides.rubyonrails.org/active_record_encryption.html) and [extended_deterministic_queries.rb source](https://github.com/rails/rails/blob/main/activerecord/lib/active_record/encryption/extended_deterministic_queries.rb): find_by on non-deterministic does NOT raise; the comparison against random ciphertext silently misses, returning nil). Removed bogus `ActiveRecord::EncryptionError` 422 response. Added `story` arrays to every scenario for "same person, same action, fix changes outcome."
  - REWARD_VIZ: rebuilt to match the new scenario set (uses `email_address` keys, no stale entries).
  - getCodeFiles: extracted `baseModel` constant. added `has_many :sessions, dependent: :destroy` and `normalizes :email_address` (matches myapp). removed `validates :email, uniqueness: true` and `validates :email, format:` from observe + intermediate code (validations are L12 territory per cumulative-patterns). Final reward code matches myapp `app/models/user.rb` exactly.
  - Table render: dropped `name` column header + cell, renamed email column to `email_address (deterministic)`, phone column to `phone (deterministic)`, fixed cellClass key, fixed CIPHERTEXT lookups via `user.email_address`.
  - Probe handler: replaced `inspect-config` branch with a `colsByProbe` lookup so `tail-query-logs` highlights `[email_address, phone]` and the others stay correct.
  - Left-panel build instructions: removed "Add deterministic encryption for email" (literal answer leak) and "Update the lookup service for encrypted queries" (stale 5th step from old service-object design). Now describes the outcome ("apply application-layer encryption to email_address with the right options for login").
- `src/features/act2-users-security/content/level-10-encryption.ts`:
  - `learningContent.goal` rewritten to remove "deterministic"/"non-deterministic" answer leakage. now describes the outcome.
  - `problem.goal` rewritten without naming modes.

### Verification

- `bunx tsc --noEmit`: clean (exit 0).
- `bunx biome check --write` on both files: 0 errors, 1 file auto-formatted.
- `bun test`: 1580 / 1580 pass.
- `bun run build`: succeeds.

### Why

Audit flagged the level was teaching a different schema than myapp ships (myapp has `email_address` not `email`, phone is deterministic, address is the only non-deterministic column). Multiple answer leaks (`learningContent.goal` and the build instructions named "deterministic"/"non-deterministic" verbatim, which are the literal step 2/3 answers). Fabricated `name` column had no schema basis. `EncryptionError` in stress test was incorrect per Rails source. Probe 3 was an inspection ("Check encryption config") not a verb-led problem. 3 duplicate stress scenarios. Validates calls in observe + build code violated cumulative-patterns (validations are L12). All fixed.

## L6 / L7 / L11: per-probe / per-scenario activeConnections (dormant-edges default)

### What changed

Added `PROBE_ACTIVE_CONNECTIONS` and `SCENARIO_ACTIVE_CONNECTIONS` maps plus matching `useMemo` hooks (`observeActiveConnections`, `rewardActiveConnections`) to three Act 1-2 levels that previously rendered `<PipelineFlow>` without an `activeConnections=` prop. Per `pedagogy.md` ("the dormant-edges default — undefined is a trap"), omitting the prop puts edges into continuous idle animation before the player has fired anything, implying data is flowing when nothing has happened. The three levels now follow the L13 canonical pattern: edges are dormant ([]) until a probe / scenario fires, then the listed connection keys flash a single-pass burst.

**Files touched:**
- `src/features/act1-foundation/components/level-6-routes/Level6Routes.tsx` — observe probes light only `request-router` (request 404s before reaching the controller); reward scenarios light the full pipeline (`request-router`, `router-controller`, `controller-response`, `controller-model`, `model-database`).
- `src/features/act1-foundation/components/level-7-controller/Level7Controller.tsx` — observe probes light `request-router` and `router-controller` (route matched, controller missing → NameError 500); reward scenarios light the full pipeline.
- `src/features/act2-users-security/components/level-11-authorization/Level11Authorization.tsx` — observe probes light all three edges (`request-controller`, `controller-policy`, `policy-model`) since the missing policy lets every request reach the model unprotected; reward scenarios distinguish blocked (request → controller → policy stops at 403) from allowed (full chain).
- `scripts/__tests__/level-reveal-consistency.test.ts` — removed L6, L7, L11 from the `KNOWN_AUTO_ANIMATING_EDGES` baseline (and stale L4 entry, which had already been fixed). Also fixed two pre-existing `noNonNullAssertion` lint warnings in test helpers (replaced `r.maxGuard! > r.inGameMax!` with `(r.maxGuard ?? 0) > (r.inGameMax ?? 0)`).

### Verification

- `bunx tsc --noEmit`: clean in touched files (pre-existing L10 errors unrelated).
- `bunx biome check --write` on the four files: no warnings.
- `bun test`: 1580 / 1580 pass.
- `bun run build`: succeeds.

### Why

These three levels were grandfathered into the `KNOWN_AUTO_ANIMATING_EDGES` baseline when the rule was introduced. Continuous idle animation before a probe fires implies traffic in a pre-state where, by design, traffic is failing or non-existent — directly contradicting what the level is supposed to teach. The new maps match the request flow each probe / scenario actually simulates.

## L1, L5, L9: add missing test coverage

Three legacy single-file levels had no `__tests__/` directories, violating `.agents/rules/testing.md` ("Every level must have tests"). Added test files mirroring the canonical L13 pattern (mirror data, do not import).

### What changed (uncommitted)

- `src/features/act1-foundation/components/level-1-environment/__tests__/Level1Environment.test.ts` — 11 tests / 68 assertions. Type 1 (no observe phase): build-step quality + narrative consistency. 5 step option sets mirrored (install-mise, activate-mise, mise-toml, install-ruby, install-rails). Enforces exactly-one-correct, substantive wrong feedback (>20 chars), per-step answer-leak keyword scan, unique ids/labels, canonical answers (e.g. `gem install rails`, not `brew`/`npm`).

- `src/features/act1-foundation/components/level-5-crud/__tests__/Level5CRUD.test.ts` — 10 tests / 61 assertions. Type 1: 5 ActiveRecord step option sets (create / read / update / destroy / verify). Answer-leak keywords use the full method-call form (`product.update(`, `Product.create`) so legitimate prose like "update_column" isn't false-flagged.

- `src/features/act2-users-security/components/level-9-authentication/__tests__/Level9Authentication.test.ts` — 21 tests / 114 assertions. Type 4 (PipelineFlow + probes): full coverage. Mirrored 4 discoveries, 3 probes (with stories), probe-discovery + stage-discovery maps, 7 reward scenarios, 5 step option sets. Enforces probe-discovery 1:1; every discovery has exactly one source (probe OR stage — `no-auth-layer` is stage-only); every probe id+label appears as a scenario; reward is a superset of observe; all probe scenarios are blocked in reward; mix of allowed/blocked; canonical Rails 8 answers (`bin/rails generate authentication`, `rails db:migrate`, `has_secure_password`, `user.sessions.create!`, `before_action :require_authentication`).

### Verification

- `bun test <each>`: pass (11 / 10 / 21).
- `bun test` full suite: 1580 / 0 (added 42 tests, 243 expect calls).
- `bunx tsc --noEmit`: zero errors in touched files. Pre-existing `Level10Encryption.tsx` errors unrelated.
- `bunx biome check --write` on the three files: clean (minor formatting applied on initial write).

### Findings

No level-data violations surfaced. All three levels pass every assertion. Notable design point in L9: `no-auth-layer` is the only discovery owned by the stage inspector (auth stage click), not by a probe. The test treats this as legitimate by asserting the union of probe-owners and stage-owners equals exactly one source per discovery, which is the pedagogy.md invariant.

## L4 Associations: existence-axis redesign (model-only observe phase)

### What changed

L4's observe phase was showing a full request pipeline (Router, Controller, Serializer, Response) with HTTP probes (`GET /api/products/1/reviews`). At L4 none of that infrastructure exists yet — those land in L6 (routes), L7 (controller), L8 (serializers). The redesign collapses observe to a **two-model graph** (Product + Review-as-ghost) plus a Database Schema node, and replaces every HTTP probe with a `bin/rails console` probe.

**Files touched:**

- `src/features/act1-foundation/components/level-4-associations/Level4Associations.tsx`
  - Removed `request`/`router`/`controller`/`response`/`serializer` nodes and their stage inspectors. Replaced with `product`, `review`, `schema` nodes positioned as a two-model graph.
  - Replaced HTTP probes with three console probes: `Product.first.reviews` → NoMethodError, `Review.count` → NameError, `Product.columns_hash.keys` → confirms no foreign key. Each probe is 1:1 with one discovery.
  - Realigned `STRESS_SCENARIOS`: first three scenarios mirror the observe probes (same id, same label) per `testing.md`, plus two reward-only scenarios (`create-review-through-association`, `cascade-on-destroy`) that demonstrate capabilities the build introduces. Every scenario now has a `story` field.
  - Added `PROBE_ACTIVE_CONNECTIONS` and `SCENARIO_ACTIVE_CONNECTIONS` maps with `useMemo` hooks (`observeActiveConnections`, `rewardActiveConnections`). Both `<PipelineFlow>` JSX renders now pass `activeConnections=` explicitly per the dormant-edges default.
  - Discovery defs reduced from 4 to 3 (no more `no-nested-routes` discovery — routes don't exist at L4). `STAGE_DISCOVERY_MAP` is now empty so probes drive 100% of discoveries 1:1.
- `src/features/act1-foundation/content/level-4-associations.ts`
  - `problem.codeExample`: removed `Product.first.reviews` and `product.reviews.create(...)` example usages (they leaked the answer to step 5). Now describes the two design questions only.
  - `problem.observation` / `trigger.description`: rewritten to focus on console + missing second model instead of "no way to attach reviews in the API".
  - `problem.goal`: paraphrased to outcome-only ("a parent record can ask for its children in the console") instead of the original `product.reviews` API string.
  - `learningContent.conceptExplanation`: removed the entire `inverse_of:` section (~20 lines). Build steps don't use it; teaching it inflates the lesson.
  - `learningContent.commonMistakes`: removed the `inverse_of:` bullet (the section it referenced is gone) and reworded the cardinality bullet so it doesn't reference "the API".
  - `hint.text`: rewritten to describe outcomes (no automatic index, no FK constraint, no auto-association) instead of telegraphing `references` ("the field type that LINKS it").

### Verification

- `bunx tsc --noEmit`: clean.
- `bunx biome check --write` on both files: passes.
- `bun test`: 1580 / 1580 pass.
- `bun run build`: succeeds.

### Why

The previous L4 observe phase violated the existence-axis half of cumulative-patterns: it showed Router/Controller/Serializer nodes and fired HTTP probes against routes that don't exist until L6+. Per `pedagogy.md`'s "Show only what currently exists" rule, the observe visualization must only render components that exist in the before-state. At L4 the player has Active Record models and `bin/rails console` — nothing else. The build phase (which already taught `has_many`/`belongs_to` correctly) is preserved untouched; only the observe topology, probes, and the leak-prone content fields were reshaped.

## L1-L12: scrub solution leakage from `learningContent` and briefing fields

### What changed

Per `.agents/rules/level-content.md` ("Never reveal answers in any player-facing text"), audited and rewrote leak-prone briefing/learning fields across L1-L12 (L13 was already cleaned). Focus: `trigger.description`, `problem.observation`, `problem.codeExample`, `problem.goal`, `learningContent.goal`, `learningContent.conceptExplanation`, `learningContent.railsCodeExample`, `learningContent.commonMistakes`, `hint.text`. Rule: describe outcomes, never name the gem/method/class/command the player picks in a build step.

**Files touched:**

- `level-1-environment.ts`: `learningContent.goal`/`conceptExplanation`/`railsCodeExample` rewritten — removed "Why mise?" section that pre-announced the gem; removed literal `[tools] ruby = "3.4.9"` recipe; removed step-by-step shell commands (`brew install`, `gem install rails`). Hint reworded from "package manager that handles system tools" to outcome-only ("a version manager that ships per-project Ruby pinning").
- `level-2-first-boot.ts`: trigger reworded "API-only" → "JSON-only" (player picks `--api` flag); `problem.codeExample` no longer names PostgreSQL/SQLite/`--api`; `learningContent.goal`/`conceptExplanation`/`railsCodeExample` describe characteristics (single-writer vs multi-user, JSON-only mode) without naming the answers; commonMistakes reworded.
- `level-3-model.ts`: `problem.codeExample` no longer enumerates `string` (short) / `text` (long) / `decimal` (money) — those are step-2 answer keys; `learningContent.goal`/`conceptExplanation`/`railsCodeExample` describe the type-fits-data principle without listing the type names; hint outcome-only.
- `level-4-associations.ts`: `learningContent.goal` rewritten to outcome-only (no `has_many`/`belongs_to`); `railsCodeExample` no longer reproduces the literal `has_many :reviews, dependent: :destroy` / `belongs_to :product` / `t.references` recipe — replaced with after-the-level checklist.
- `level-5-crud.ts`: `problem.codeExample` no longer enumerates `.create vs .new vs .insert` etc. (those are answer keys); `learningContent.goal`/`conceptExplanation`/`railsCodeExample` describe the variants-have-different-behaviour principle without naming each method; commonMistake renamed `update_column` to "skip just one callback" method.
- `level-6-routes.ts`: `problem.codeExample` no longer says `resources` / `namespace`; `learningContent.goal`/`conceptExplanation`/`railsCodeExample` describe the two routing constructs (URL-only vs URL+module) without naming `scope`/`namespace` or `resources`; commonMistake about `scope` vs `namespace` reworded.
- `level-7-controller.ts`: `problem.codeExample` rewritten to describe constraints + omits the `(also not: list, get, add, remove)` answer-trap hint; `learningContent.goal` reframed to "exercise the endpoint by sending a real HTTP request" instead of naming `curl`.
- `level-8-serializers.ts`: removed entire "Why jsonapi-serializer?" pitch section from `conceptExplanation`; replaced "Alternatives and trade-offs" list with anonymous descriptions ("the JSON:API-conformant fast still-maintained one"); `railsCodeExample` no longer shows the literal `gem "jsonapi-serializer"` + `JSONAPI::Serializer` recipe; hint paraphrased to "the actively-maintained successor".
- `level-9-authentication.ts`: `problem.codeExample` no longer literal-names `bin/rails generate authentication` / `has_secure_password`; `learningContent.goal` reframed; `commonMistakes` rewritten to describe categories ("third-party auth gem", "long-lived credentials", "default-block hits login first") without naming `Devise`/`has_secure_password`/`allow_unauthenticated_access`.
- `level-10-encryption.ts`: `problem.codeExample` rewritten to describe the two encryption modes by behaviour (same-ciphertext vs different-ciphertext) without naming `encrypts`/`deterministic: true`. (Coordinated with concurrent L10 schema fix; touched only leak-prone content fields.)
- `level-11-authorization.ts`: heaviest rewrite. Title changed "Authorization with Pundit & Current.user" → "Authorization & Per-Record Ownership Checks". `problem.codeExample` no longer names Pundit / `gem "pundit"` / `ProductPolicy`. `conceptExplanation` rewritten as "Why it lives in its own layer" + "The Ruby ecosystem standard" describing the pattern (per-model rule classes, one boolean per action, single check helper) without naming `Pundit`/`authorize`/`policy_scope`/`ProductPolicy`. `railsCodeExample` rewritten as outcome checklist (no `class ProductPolicy`/`def destroy?`/`include Pundit::Authorization`). `commonMistakes` and `hint` reworded to describe the mechanic without naming the library.
- `level-12-validations.ts`: `problem.goal` reworded — removed "Add presence, uniqueness, and format validations" (literal step-1/2/3 answer keys); `learningContent.goal` reworded to "pick the right built-in ActiveRecord validator for each kind of failure"; `hint.text` rewritten to describe failure-mode-fits-validator without naming the validators.

### Verification

- `bunx tsc --noEmit`: clean.
- `bunx biome check --write` on all 12 files: 0 errors.
- `bun test`: 1580 / 1580 pass.
- `bun run build`: succeeds.

### Why

`.agents/rules/level-content.md` codifies that any player-visible text seen BEFORE the player makes a build choice must not name the gem/method/class/command they will pick. The conceptExplanation and railsCodeExample fields are visible from the first frame of the level (left/right panel content during observe phase), so they fall under the rule. Audit found heavy leakage in L1 (literal `brew install mise` recipe + "Why mise?" pitch), L8 ("Why jsonapi-serializer?" + literal gem name), and L11 (Pundit named in title, every `policy_scope`/`authorize`/`ProductPolicy` shown verbatim). Eight other levels had milder leakage in goal/codeExample/hint fields. All 12 now follow the L13-cleaned pattern: outcome-focused goals/hints/observations, conceptExplanation teaches the principles without pre-announcing the specific tool, railsCodeExample describes "after the level you will have done X" rather than dictating literal commands.
