# Session: 2026-03-04 Session 1

## Summary

Landing page visual overhaul, theme system upgrade, and Button component fix.

## Changes

### 1. Fixed Dark Mode Not Applying

- `BaseLayout.astro` was missing `class="dark"` on `<html>`, so the entire `.dark` CSS theme was never applied
- Page was rendering in light mode with washed-out default shadcn colors
- Added inline `<script>` in `<head>` to read `localStorage('theme')` and apply dark class before paint

### 2. Discovered Radix Slot + Astro SSR Incompatibility

- CTA buttons on landing page were invisible (no background/border)
- Root cause: `@radix-ui/react-slot` does not merge className onto children during Astro's React SSR rendering
- Tested both `@radix-ui/react-slot` and `radix-ui` packages, both failed
- Even `React.cloneElement` approach failed in Astro's renderer
- Works in plain Node SSR but not in Astro's specific React renderer

### 3. Removed asChild/Slot Pattern Entirely

- Simplified `Button.tsx` to always render as `<button>`, removed Radix Slot dependency
- Removed all `asChild` usages across 7 files:
  - `Header.astro`, `login.astro`, `signup.astro`, `dashboard.astro`
  - `SandboxApp.tsx`, `ActsListApp.tsx`, `LevelHeader.tsx`
- Added warnings to `CLAUDE.md` and `MEMORY.md` about this incompatibility

### 4. Theme Color Updates (Three Iterations)

- **Warm brown -> Neutral cool**: Changed dark theme backgrounds from warm hue 30 to neutral hue 260
- **Amber -> Gold**: Shifted primary from dull amber to brighter gold
- **Gold -> Ruby red**: Final theme, primary `oklch(0.65 0.25 18)` dark / `oklch(0.55 0.25 18)` light
- Updated all hardcoded oklch references across `index.astro`, `login.astro`, `signup.astro`, `ActsListApp.tsx`
- Updated favicon colors to ruby red (`#dc2626` spike, `#f87171` accent)
- Updated selection highlight and shimmer animation colors

### 5. Light/Dark Theme Toggle

- Added sun/moon toggle button in Header, outside `#header-auth` div (so auth script innerHTML replacement doesn't destroy it)
- `BaseLayout.astro` inline script reads localStorage before paint to prevent flash
- Defaults to light mode (no class = light)
- Both light and dark mode now use ruby red primary

### 6. Header Height Fix

- Increased header from `h-12` to `h-16` for better proportions

### 7. Hero Section Visual Improvements

- Stronger glows, semi-transparent white borders (`border-white/[0.08]`)
- Better pipeline card depth with layered shadows
- Hero gradient text changed from `via-amber-400` to `via-rose-400`

## Files Modified

| File | Changes |
|------|---------|
| `src/components/ui/Button.tsx` | Removed asChild/Slot, always renders `<button>` |
| `src/layouts/BaseLayout.astro` | Theme persistence script, defaults to light |
| `src/components/ui/Header.astro` | Height h-16, theme toggle button + script |
| `src/styles/global.css` | Full theme rewrite: ruby red primary, neutral backgrounds |
| `src/pages/index.astro` | Hero overhaul, color updates, asChild removal |
| `src/pages/login.astro` | asChild removal, color updates |
| `src/pages/signup.astro` | asChild removal, color updates |
| `src/pages/dashboard.astro` | asChild removal |
| `src/features/sandbox/components/SandboxApp.tsx` | asChild removal |
| `src/features/acts/components/ActsListApp.tsx` | asChild removal, color updates |
| `src/components/levels/LevelHeader.tsx` | asChild removal |
| `public/favicon.svg` | Ruby red colors |
| `CLAUDE.md` | Slot warning, theme checklist, updated button examples |

## Lessons Learned

- Radix Slot is completely broken in Astro's React SSR renderer. Do not use `asChild` in any Astro-rendered component.
- When changing theme colors, there are 5 places to update: `global.css` (light + dark), component hardcoded oklch values, `favicon.svg`, `index.astro` hero gradients, and auth page gradients.
- Theme toggle must be placed outside any container whose innerHTML gets dynamically replaced.

---

# Session: 2026-03-04 Session 2

## Summary

Fixed 10 light mode visual issues on the landing page. Used cmux browser API for inspection.

## Changes

### 1. Body dot pattern (global.css + BaseLayout.astro)
- Moved dot pattern from CSS `body {}` to Tailwind classes on `<body>` in BaseLayout
- Light mode: black dots (`oklch(0 0 0 / 4%)`), dark mode: white dots (`oklch(1 0 0 / 4%)`)
- CSS variable approach failed (Tailwind v4 tree-shakes unused custom properties)

### 2. Light mode shadow variables (global.css)
- Added `--shadow-sm`, `--shadow-md`, `--shadow-lg`, `--shadow-glow` to `:root`
- Were only defined in `.dark`, causing `.hover-card` to have no shadow in light mode

### 3. Nav button colors (Header.astro + Button.tsx)
- Added `muted` color option to Button: `[--btn:var(--muted-foreground)]`
- Nav ghost buttons use `color="muted"` when inactive (gray text instead of ruby red)

### 4. Hero/CTA radial gradient overlays (index.astro)
- All `oklch(0.65 ... / 0.05)` overlays now have `dark:` variants
- Light mode uses `oklch(0.55 ... / 0.08)` (darker, higher opacity) for visibility

### 5. Terminal header bg (index.astro)
- Changed from `bg-white/[0.03]` (invisible on white) to `bg-zinc-800/50`

### 6. H2 spacing fix (index.astro)
- "Discover Problems<br/>Through Exploration" had no space on mobile (Astro strips whitespace)
- Fixed with space before `<br class="hidden lg:block" />`

### 7. Learning path circles (index.astro)
- Replaced `bg-background` (conflicted with `bg-{color}-500/15`) with `shadow-[0_0_0_4px_var(--background)]`
- Circle text: added `dark:` variants (`text-emerald-600 dark:text-emerald-400`)

### 8. Selection highlight (global.css)
- Light mode: `oklch(0.55 0.25 18 / 20%)` without forced white text
- Dark mode: `oklch(0.65 0.25 18 / 30%)` with white text

### 9. CTA button shadows (index.astro)
- Dual-mode shadows: lighter in light mode, stronger in dark mode

## Architecture Notes

- Tailwind v4 tree-shakes CSS custom properties not referenced by utilities. Can't use CSS vars in raw `body {}` for theme-switching. Use Tailwind classes on the element instead.
- `bg-background` and `bg-{color}/15` conflict in Tailwind v4 since both set `background-color`. Use `shadow-[0_0_0_Npx_var(--background)]` as a masking ring instead.

---

# Session: 2026-03-04 Session 3

## Summary

Redesigned the Level Info/Briefing page and added Alert component variants.

## Changes

### 1. LevelInfoApp redesign (two iterations)
- **First attempt:** Two-column grid layout. Left column cramped, too many Card borders created visual clutter. Reverted.
- **Final design:** Single column `max-w-4xl` with improved spacing and hierarchy:
  - Header: larger badge (`w-16 h-16`), `text-3xl` title, "Act N" context label
  - Scenario: uses shadcn `Alert` component with `info` variant and `Info` icon
  - Problem code: full-width dark card (`bg-zinc-50 dark:bg-zinc-950`) with neutral header
  - Goal + Learning Goals: side-by-side on `lg:` screens via 2-col grid
  - CTA: `shadow-lg shadow-primary/20` glow
  - "What You'll Learn" section: parses `learningContent.goal` markdown bullets

### 2. Added shadcn Alert component with custom variants
- Installed via `bunx shadcn@latest add alert`, renamed to PascalCase
- Added `warning` (amber), `info` (blue), `success` (emerald) variants
- Updated `destructive` with matching border/bg tint (was plain `bg-card`)
- All variants have proper light/dark mode colors

### 3. CLAUDE.md: mandatory session logging rule
- Added "Always update session logs after every change" as a non-negotiable critical rule

## Files Modified

| File | Changes |
|------|---------|
| `src/components/pages/LevelInfoApp.tsx` | Full redesign: single column, Alert for scenario, learning goals section |
| `src/components/ui/Alert.tsx` | New component: shadcn Alert with warning/info/success/destructive variants |
| `CLAUDE.md` | Added mandatory session logging rule |
| `.claude/skills/audit-act/skill.md` | Full rewrite: added act calibration, file path conventions, cross-act concept overlap, content quality checks, linear progression checks |

---

# Session: 2026-03-04 Session 4

## Summary

Audited Level 10 (Validations) against the three-phase sequential flow standard and performed a complete rewrite.

## Audit Findings

Level 10 had **no observe phase** and **no reward phase**. It was a flat 4-step progression with a static before/after comparison at the end. Specific issues:

1. **No phase state machine** (no `observe | build | activate | reward`)
2. **No PipelineFlow visualization** in any phase
3. **No ProbeTerminal** for interactive problem discovery
4. **No StageInspector** for clicking pipeline stages
5. **No useDiscoveryGating** hook or DiscoveryChecklist
6. **No "Build the Fix" button** gated behind discoveries
7. **No useStressTest** or StressTestPanel for the reward phase
8. **Star display used emoji** instead of Lucide Star component
9. **Step 1 (uniqueness)** used TerminalChoiceStep for a Ruby code line (not a shell command)
10. **StepProgress** missing `onStepClick`/`currentStep` props
11. **content.ts hint** referenced old pipeline-builder interaction
12. **Em dashes** in content.ts Level 10 section (5 occurrences)

## Changes

### 1. Complete Level 10 rewrite (Level10Validations.tsx)
- Added full 4-phase state machine: `observe -> build -> activate -> reward`
- **Observe phase**: PipelineFlow (Controller -> Model -> Database) with `onNodeClick`, 3 StageInspector cards (controller code, model without validations, database garbage), ProbeTerminal with 3 probes (empty post, duplicate email, malformed email), DiscoveryChecklist with 4 discoveries, "Build the Fix" gated behind `discoveryGating.isUnlocked`
- **Build phase**: 3 OptionCard steps (presence, uniqueness, format) + 1 TerminalChoiceStep (test invalid record in irb>). Converted uniqueness from TerminalChoiceStep to OptionCard since it's Ruby code, not a shell command. Added `onStepClick`/`currentStep` to StepProgress
- **Activate phase**: Lucide Star rating + "Visualize Validations" button
- **Reward phase**: PipelineFlow showing Validations node flipping green (valid, saved) / red (invalid, rejected 422), StressTestPanel with 5 scenarios (2 valid, 3 invalid), dual counters (Saved/Rejected)
- Pipeline node variants react dynamically to `lastProbeId` (observe) and `stressTest.results` (reward) via `useMemo`
- Code preview evolves across phases: Post model gains validations, User model gains email checks, controller shows error response pattern

### 2. content.ts updates
- Fixed hint text from "Add a Validation node to the pipeline" to match new interactive exploration flow
- Replaced 5 em dashes (`--`) with colons/periods in Level 10 content section

## Files Modified

| File | Changes |
|------|---------|
| `src/features/act2-users-security/components/Level10Validations.tsx` | Complete rewrite with three-phase flow |
| `src/features/act2-users-security/content.ts` | Fixed hint text, removed em dashes |

---

# Session: 2026-03-04 Session 5

## Summary

Audited Level 11 (Callbacks & Normalizations) against the three-phase sequential flow standard and performed a complete rewrite.

## Audit Findings

Level 11 had **no observe phase** and **no reward phase**, identical structural issues to Level 10. It was a flat 4-step OptionCard progression with a static before/after HTML comparison at the end. Specific issues:

1. **No phase state machine** (no `observe | build | activate | reward`)
2. **No PipelineFlow visualization** in any phase
3. **No ProbeTerminal** for interactive problem discovery
4. **No StageInspector** for clicking pipeline stages
5. **No useDiscoveryGating** hook or DiscoveryChecklist
6. **No "Build the Fix" button** gated behind discoveries
7. **No useStressTest** or StressTestPanel for the reward phase
8. **Star display used emoji** (`'★'.repeat()`) instead of Lucide Star component
9. **Hardcoded `bg-zinc-900`** in before/after cards (only works in dark mode)
10. **content.ts hint** referenced old pipeline-builder interaction ("Add a Callback node connected to the User model")
11. **Per-step selected state** tracked with individual `useState` hooks instead of using stepper

## Changes

### 1. Complete Level 11 rewrite (Level11Callbacks.tsx)
- Added full 4-phase state machine: `observe -> build -> activate -> reward`
- **Observe phase**: PipelineFlow (Input -> Normalizes -> User Model -> Callbacks) with `onNodeClick`. Normalizes and Callbacks stages start as `'inactive'` (dashed, missing). 4 StageInspector cards for each stage. ProbeTerminal with 3 probes (messy email signup, case-sensitive lookup, empty mailer queue). DiscoveryChecklist with 4 discoveries (raw-stored, lookup-fails, no-welcome, no-hooks). Pipeline nodes react to probes via `useMemo` + `lastProbeId`. "Build the Fix" gated behind `discoveryGating.isUnlocked`
- **Build phase**: Same 4 OptionCard steps (normalization, callback, ordering, pitfall) refactored into unified `OPTION_STEP_CONFIG` map. Used `handleOptionClick` callback instead of 4 separate handlers. Consistent `color="violet"` matching L12 reference
- **Activate phase**: Lucide Star rating + "Visualize Lifecycle" button
- **Reward phase**: PipelineFlow showing Normalizes node (green when data cleaned) and Callbacks node flipping green (side effect fired) / red (rollback prevented CRM sync, badge "SAFE"). StressTestPanel with 5 scenarios: messy signup, case lookup, welcome email, update skips welcome, rollback prevents CRM. Dual counters (Processed/Prevented)
- Removed all per-step `useState` hooks and per-step click handlers in favor of unified stepper pattern
- Removed static before/after HTML comparison and "Complete Level" button

### 2. content.ts updates
- Fixed hint text from old pipeline-builder reference to match new interactive exploration flow

## Files Modified

| File | Changes |
|------|---------|
| `src/features/act2-users-security/components/Level11Callbacks.tsx` | Complete rewrite with three-phase flow |
| `src/features/act2-users-security/content.ts` | Fixed hint text |

---

# Session: 2026-03-04 Session 6

## Summary

Audited Level 9 (Authentication) against the three-phase sequential flow standard and performed a complete rewrite.

## Audit Findings

Level 9 had **no observe phase** and **no reward phase**. It was a flat 4-step progression with a static before/after comparison at the end. Specific issues:

1. **No phase state machine** (no `observe | build | activate | reward`)
2. **No PipelineFlow visualization** in any phase
3. **No ProbeTerminal** for interactive problem discovery
4. **No StageInspector** for clicking pipeline stages
5. **No useDiscoveryGating** hook or DiscoveryChecklist
6. **No "Build the Fix" button** gated behind discoveries
7. **No useStressTest** or StressTestPanel for the reward phase
8. **Star display used emoji** instead of Lucide Star component
9. **content.ts hint** referenced old pipeline-builder interaction ("Add an Authentication node")
10. **content.ts learning content** used wrong Rails 8 concern name (`ApiAuthentication`/`authenticate_user` instead of `Authentication`/`require_authentication`)
11. **content.ts** used `skip_before_action` instead of Rails 8's `allow_unauthenticated_access`
12. **Em dashes** in content.ts concept explanation
13. **Variable naming**: `level8Authentication` for Level 9

## Changes

### 1. Complete Level 9 rewrite (Level9Authentication.tsx)
- Added full 4-phase state machine: `observe -> build -> activate -> reward`
- **Observe phase**: PipelineFlow (Request -> Auth[missing/inactive] -> Controller -> Model) with `onNodeClick`, 4 StageInspector cards, ProbeTerminal with 3 probes (DELETE without token, POST without token, check identity), DiscoveryChecklist with 4 discoveries, "Build the Fix" gated behind `discoveryGating.isUnlocked`
- **Build phase**: Kept existing 4 steps. Reordered correct answer positions so correct is never first
- **Activate phase**: Lucide Star rating + "Visualize Authentication" button
- **Reward phase**: PipelineFlow with Auth node flipping green/red, StressTestPanel with 5 scenarios, dual counters, PipelineLegend

### 2. content.ts updates
- Renamed `level8Authentication` to `level9Authentication`
- Fixed hint to match explore interaction
- Updated `railsCodeExample` to use correct Rails 8 names (`Authentication`, `require_authentication`, `allow_unauthenticated_access`)
- Fixed `commonMistakes` to reference `allow_unauthenticated_access`
- Replaced em dashes in concept explanation

## Files Modified

| File | Changes |
|------|---------|
| `src/features/act2-users-security/components/Level9Authentication.tsx` | Complete rewrite with three-phase flow |
| `src/features/act2-users-security/content.ts` | Fixed variable name, hint, learning content, Rails 8 method names, em dashes |

---

# Session: 2026-03-04 Session 7

## Summary

Audited Level 15 (CORS) against the three-phase sequential flow standard and performed a complete rewrite.

## Audit Findings

Level 15 had a **passive SVG animation** for the observe phase and a **passive auto-incrementing counter** for the reward phase. No interactive components at all. Specific issues:

1. **Observe phase**: Custom SVG animation with zero interactivity. No PipelineFlow, no ProbeTerminal, no StageInspector, no useDiscoveryGating, no DiscoveryChecklist. "Build the Fix" button was always visible (no discovery gating).
2. **Reward phase**: Passive `setInterval` counter incrementing "blocked" count. No useStressTest, no StressTestPanel, no dynamic pipeline nodes. Single blocked counter instead of dual allowed/blocked.
3. **content.ts**: Variable named `level14CORS` instead of `level15CORS`.

## Changes

### 1. Complete Level 15 rewrite (Level15CORS.tsx)
- Added full 4-phase state machine: `observe -> build -> activate -> reward`
- **Observe phase**: PipelineFlow (Browser -> Preflight -> CORS Middleware[inactive/missing] -> Rails API) with `onNodeClick`, 4 StageInspector cards, ProbeTerminal with 3 probes (GET from React frontend, DELETE preflight failure, curl bypass), DiscoveryChecklist with 4 discoveries, "Build the Fix" gated behind `discoveryGating.isUnlocked` (3 of 4 required)
- Pipeline nodes react to probes: CORS Middleware shows "No headers sent"/"BLOCKED" on fetch probe, Preflight shows "OPTIONS rejected"/"FAIL" on delete probe, API shows "200 OK" on curl probe
- **Build phase**: Kept existing 3 steps (bundle add rack-cors, configure origins, allow methods). Reordered correct answer positions so correct is never first
- **Activate phase**: Lucide Star rating + "Visualize Protection" button with `stressTest.reset()`
- **Reward phase**: PipelineFlow with CORS Middleware node flipping green ("Allow-Origin: localhost:3001") for allowed origins / red ("Origin rejected"/"BLOCKED") for malicious origins. StressTestPanel with 6 scenarios (3 from localhost:3001 = allowed, 3 from evil/unknown origins = blocked). Dual counters (Allowed/Blocked) in left panel. PipelineLegend
- Removed custom CORSVisualization SVG component and CORSLegend
- Removed passive `setInterval` blocked counter

### 2. content.ts updates
- Renamed `level14CORS` to `level15CORS` (both declaration and usage in levels array)

## Files Modified

| File | Changes |
|------|---------|
| `src/features/act2-users-security/components/Level15CORS.tsx` | Complete rewrite with three-phase flow |
| `src/features/act2-users-security/content.ts` | Renamed level14CORS to level15CORS |

---

# Session: 2026-03-04 Session 8

## Summary

Audited Level 13 (Testing) against the three-phase sequential flow standard and fixed all issues.

## Audit Findings

Level 13 had a **passive static pipeline** for the observe phase and **passive auto-incrementing counters** for the reward phase. Specific issues:

1. **Observe phase**: Static PipelineFlow with no interactivity. No `useDiscoveryGating`, no `DiscoveryChecklist`, no `ProbeTerminal`, no `StageInspector`, no `onNodeClick`. "Build the Fix" button always visible (no discovery gating, no `animate-in fade-in duration-500`).
2. **Reward phase**: Passive `setInterval` counters (`caughtCount`, `deployedCount`). No `useStressTest`, no `StressTestPanel`, no dynamic pipeline nodes via `useMemo`. Pipeline stages were static constants, not reactive.
3. **content.ts**: `codeExample` showed the exact request spec that is the correct answer in Step 5 (player can read ahead).

## Changes

### 1. Level 13 rewrite (Level13Testing.tsx)
- Added full interactive observe phase:
  - `useDiscoveryGating` with 4 discoveries (no-framework, empty-specs, broken-deploy, no-test-data), minRequired 3
  - `ProbeTerminal` with 3 probes: deploy broken login (500 NoMethodError), deploy broken create (500 NameError), deploy dropped column (500 StatementInvalid)
  - `StageInspector` with 4 inspectable stages: Code, Build, Test Gate (with code block showing empty spec/), Production
  - `DiscoveryChecklist` in left panel
  - `PipelineFlow` with `onNodeClick` and dynamic `observeStages` via `useMemo` reacting to `inspectedStages` + `lastProbeId`
  - Production node flips to `'danger'` with badge "500!" when probes fire, Test Gate stays `'inactive'`
  - "Build the Fix" button gated behind `discoveryGating.isUnlocked` with `animate-in fade-in duration-500`
- Added full interactive reward phase:
  - `useStressTest` with 5 scenarios: clean refactor (allowed), broken token rename (blocked), clean validation (allowed), missing params (blocked), clean endpoint (allowed)
  - `StressTestPanel` below pipeline in center panel
  - Dynamic `rewardStages` via `useMemo`: RSpec node flips green ("2 specs, 0 failures") for allowed / red ("FAILURE", badge "1 FAILED") for blocked commits
  - Production node updates: "deployed" (active) vs "(blocked)" (default)
  - Dual counters (Deployed/Caught) in left panel
- Removed passive `setInterval` counter and `caughtCount`/`deployedCount` state
- Removed static `OBSERVE_STAGES` and `REWARD_STAGES` constants (now dynamic via `useMemo`)

### 2. content.ts updates
- Replaced `codeExample` that showed the exact request spec answer with the broken controller code instead (teaches context without revealing the Step 5 answer)

## Files Modified

| File | Changes |
|------|---------|
| `src/features/act2-users-security/components/Level13Testing.tsx` | Full rewrite: interactive observe + reward phases |
| `src/features/act2-users-security/content.ts` | Fixed codeExample to not reveal Step 5 answer |

---

# Session: 2026-03-04 Session 9

## Summary

Audited Level 14 (Strong Params) against the three-phase sequential flow standard and performed a complete rewrite.

## Audit Findings

Level 14 had a **passive SVG animation** for the observe phase and a **passive auto-incrementing counter** for the reward phase. The component contradicted its own content.ts:

1. **Narrative contradiction**: content.ts correctly says "your controller uses params.expect, but the whitelist is too broad" (building on L6). Component showed raw `params[:post]` with zero filtering, ignoring L6 entirely.
2. **Observe phase**: Custom SVG with zero interactivity. No PipelineFlow, ProbeTerminal, StageInspector, useDiscoveryGating, or DiscoveryChecklist. "Build the Fix" always visible.
3. **Reward phase**: Passive `setInterval` counter. No useStressTest, StressTestPanel, or dynamic pipeline nodes. Zero player interaction.
4. **Build steps misaligned**: Had player set up params.expect from scratch instead of audit/tighten existing whitelist.

## Changes

### 1. Complete Level 14 rewrite (Level14StrongParams.tsx)
- **Observe phase**: PipelineFlow (Request -> Controller -> params.expect -> Post Model) with `onNodeClick`, 4 StageInspector cards, ProbeTerminal with 3 probes (user_id injection, admin escalation, both), DiscoveryChecklist (4 discoveries, 3 required), gated "Build the Fix" button
- **Build phase**: 3 steps redesigned to build on L6: Audit the Whitelist, Tighten the Whitelist, Set Ownership Safely
- **Reward phase**: StressTestPanel with 5 scenarios, dynamic pipeline nodes (params.expect flips green/red), dual counters
- Removed custom SVG and passive `setInterval`

### 2. content.ts: added `:status` to codeExample whitelist for consistency

## Files Modified

| File | Changes |
|------|---------|
| `src/features/act2-users-security/components/Level14StrongParams.tsx` | Complete rewrite with three-phase flow |
| `src/features/act2-users-security/content.ts` | Added :status to codeExample whitelist |

---

# Session: 2026-03-04 Session 10

## Summary

Rewrote audit-level skill to stop forcing PipelineFlow on every level. Custom visualizations are now the preferred approach.

## Problem

The audit-level skill's checklist items treated PipelineFlow + ProbeTerminal + StressTestPanel as mandatory for every level. When 7 parallel Claude instances audited Act 2, they replaced unique custom visualizations (like L15 CORS's SVG animation) with cookie-cutter pipeline views. Every level ended up looking the same.

## Changes

### audit-level skill rewrite (.claude/skills/audit-level/SKILL.md)

**Phase 1 (Observe):**
- Replaced mandatory PipelineFlow checklist with "Visualization approach: custom first, PipelineFlow when it fits"
- Custom SVG/animation, interactive diagrams, before/after comparisons are all valid approaches
- New rule: "If a level already has a custom visualization that teaches the concept well, keep it. Add interactivity rather than replacing it."
- ProbeTerminal is now one option among several discovery mechanisms (clickable regions, interactive controls)
- Core requirements distilled to: must be interactive (not passive), must use discovery gating, must have "Build the Fix" button
- PipelineFlow-specific checks moved to a conditional "When PipelineFlow IS used" section

**Phase 3 (Reward):**
- Replaced mandatory StressTestPanel checklist with flexible interactivity options
- Custom interactive controls and replay/comparison controls are valid alternatives
- Core rule: "every click from the player must produce a visible reaction in the visualization"
- StressTestPanel-specific checks moved to conditional "When StressTestPanel IS used" section

**Output format:**
- Added "Visualization assessment" as item 3: explicitly asks whether the current visualization is unique/concept-appropriate, and recommends keeping custom visualizations

## Files Modified

| File | Changes |
|------|---------|
| `.claude/skills/audit-level/SKILL.md` | Full rewrite of Phase 1 and Phase 3 sections to prefer custom visualizations over PipelineFlow |

---

# Session: 2026-03-04 Session 11

## Summary

Audited Level 9 (Authentication) and implemented improvements from the audit report.

## Changes

### 1. Added `rails db:migrate` step to Level 9 (Level9Authentication.tsx)
- Inserted new Step 1 "Run Migrations" between generator and password strategy steps
- 3 command options: `rails db:seed` (wrong), `rails db:setup` (wrong), `rails db:migrate` (correct)
- Output shows CreateUsers and CreateSessions migration output
- Teaches that generators create migration files that still need to be run
- All subsequent step indices bumped (old 1->2, 2->3, 3->4)
- `SHELL_STEP_MAP` updated to include both terminal steps with cumulative history
- `getCodeFiles` thresholds shifted to match new step indices
- Level now has 5 steps instead of 4

### 2. Fixed content.ts
- Fixed comment "Level 9: Validations" -> "Level 10: Validations"
- Updated `problem.goal` to mention running migrations

## Files Modified

| File | Changes |
|------|---------|
| `src/features/act2-users-security/components/Level9Authentication.tsx` | Added db:migrate step, bumped step indices |
| `src/features/act2-users-security/content.ts` | Fixed comment, updated goal text |

---

# Session: 2026-03-04 Session 12

## Summary

Re-audited Level 10 (Validations) after earlier rewrite. Found and fixed 3 content.ts issues.

## Audit Findings

The component (Level10Validations.tsx) **passes all three-phase checks** after its Session 4 rewrite. All phases are interactive, discovery-gated, and follow the golden standard. Issues were only in content.ts:

1. **Variable naming**: `level9Validations` for Level 10 (off-by-one from renumbering)
2. **Trigger missing L9 context**: Jumped straight to "Users submit empty posts" without acknowledging authentication landed in L9
3. **Problem goal too brief**: Just said "Add validations" without describing the full build workflow

## Changes

### 1. content.ts fixes
- Renamed `level9Validations` to `level10Validations` (declaration + usage in act export)
- Updated trigger to acknowledge L9: "Authentication is live, but users submit empty posts..."
- Expanded problem.goal to describe all steps: "Add presence, uniqueness, and format validations to your models, then inspect error messages in the Rails console."

## Files Modified

| File | Changes |
|------|---------|
| `src/features/act2-users-security/content.ts` | Renamed variable, updated trigger and goal |

---

# Session: 2026-03-04 Session 13

## Summary

Re-audited Level 11 (Callbacks & Normalizations) after its Session 5 rewrite. Found and fixed 3 minor issues.

## Audit Findings

The component (Level11Callbacks.tsx) **passes all three-phase checks** after its Session 5 rewrite. All phases are interactive, discovery-gated, and follow the golden standard. Issues were minor:

1. **content.ts `problem.goal` incomplete**: Only mentioned normalizing email and welcome email, but the build phase has 4 steps covering normalization, callback, ordering, and after_commit pitfall
2. **content.ts `furtherReading` URL**: Normalizes link pointed to generic Rails 8.0 release notes instead of the actual `ActiveRecord::Normalization` API docs
3. **Step 2 description trivia-framed**: "Which sequence is correct?" was a knowledge quiz rather than a practical decision

## Changes

### 1. content.ts fixes
- Updated `problem.goal` to describe all 4 build steps: normalization, after_create callback, lifecycle ordering, and after_commit for external side effects
- Changed normalizes `furtherReading` URL from release notes to `https://api.rubyonrails.org/classes/ActiveRecord/Normalization/ClassMethods.html`

### 2. Step 2 description reframed (Level11Callbacks.tsx)
- Changed from quiz-style "Which sequence is correct?" to practical framing: "Your normalizes must run before validation checks uniqueness, and the welcome email must fire only after the record is persisted. Which lifecycle order does Rails actually follow?"

## Files Modified

| File | Changes |
|------|---------|
| `src/features/act2-users-security/components/Level11Callbacks.tsx` | Reframed Step 2 description |
| `src/features/act2-users-security/content.ts` | Updated problem.goal, fixed normalizes furtherReading URL |

---

# Session: 2026-03-04 Session 14

## Summary

Re-audited Level 13 (Testing) after its Session 8 rewrite. Found and fixed 1 content.ts naming issue.

## Audit Findings

The component (Level13Testing.tsx) **passes all three-phase checks** after its Session 8 rewrite. All phases are interactive, discovery-gated, and follow the golden standard. Cross-referenced rspec-rails and factory_bot_rails READMEs to verify step accuracy. Single issue found:

1. **content.ts variable naming**: `level12Testing` for Level 13 (stale from renumbering). Comment also said "Level 12: Testing"

## Changes

### 1. content.ts fixes
- Renamed `level12Testing` to `level13Testing` (declaration, comment, and usage in act export array)

## Files Modified

| File | Changes |
|------|---------|
| `src/features/act2-users-security/content.ts` | Renamed level12Testing to level13Testing |

---

## Session 15: Continued Conversation - Cleanup and Verification

### Context
Resumed from a compacted conversation. Previous sessions had spawned 3 Claude Code instances via cmux (surfaces 14, 15, 16) to re-audit L10, L11, L13 with the updated audit-level skill.

### Verification
- All 3 sub-instances completed successfully:
  - **L10 (surface 14)**: Fixed content.ts trigger and problem.goal
  - **L11 (surface 15)**: Fixed content.ts problem.goal, furtherReading URL, Step 2 description
  - **L13 (surface 16)**: Fixed content.ts variable naming (level12Testing -> level13Testing)
- Closed all 3 audit panes
- Build passes cleanly (`bun run build` - Complete!)

---

# Session: 2026-03-04 Session 16

## Summary

Replaced PipelineFlow with custom visualizations in L10, L11, and L15 observe/reward phases to differentiate them from pipeline-natural levels (L9, L12, L13, L14).

## Problem

All Act 2 levels used PipelineFlow for observe/reward visualizations, making them look cookie-cutter. L10 (Validations), L11 (Callbacks), and L15 (CORS) teach concepts that aren't natural pipeline flows and deserve unique visual metaphors.

## Changes

### 1. L10 Validations: "Data Gate" Visualization

Replaced PipelineFlow with a vertical three-zone layout:
- **Input zone** (top): Shows incoming data card, clickable for controller discovery
- **Model gate** (middle): Dashed border when no validations, turns red on probe, shows validation rules in reward
- **Database zone** (bottom): Shows save result, green (SAVED) or red (422 Unprocessable Entity)
- Connected by ArrowDown icons that change color based on state
- Reward phase: gate shows validation rules, result zone shows SAVED/REJECTED per stress test

### 2. L11 Callbacks: "Data Transform Lane" Visualization

Replaced PipelineFlow with a vertical four-zone layout:
- **Raw User Data** (top): Shows incoming email data
- **Normalizes** (middle): Dashed when missing, shows strip+downcase in reward
- **User Model** (middle): Shows validates + saves
- **Callbacks** (bottom): Dashed when missing, shows after_create/after_commit behavior in reward
- Reward phase: normalizes zone active green, callbacks zone flips green/red based on scenario

### 3. L15 CORS: "Browser-Server Handshake" Visualization

Replaced PipelineFlow with a horizontal two-tower layout:
- **Browser tower** (left): Traffic-light chrome header, URL bar (localhost:3001), Globe icon, fetch() snippet
- **Origin boundary** (center): Dashed vertical line with "Origin Boundary" label, request result indicator (BLOCKED/200 OK)
- **Server tower** (right): Three stacked zones: Preflight (OPTIONS), CORS Middleware (dashed when missing, solid green in reward), Rails API
- Reward phase: Browser shows current origin, CORS middleware shows Allow/Reject, API shows method+path or "not reached"

### What Was Preserved

- All data maps (DISCOVERY_DEFS, PROBES, STAGE_INSPECTOR_MAP, etc.) unchanged
- All hooks (useDiscoveryGating, useStepGating, useStressTest) unchanged
- Build phases (OptionCard steps, TerminalChoiceStep) completely untouched
- Activate phases unchanged
- Left panel (scenario, discovery checklist, step progress, counters) unchanged
- Right panel (code preview) unchanged
- StageInspector overlay still works on clickable zones
- ProbeTerminal and StressTestPanel still present

### What Was Removed

- PipelineFlow import and usage in all 3 files
- PipelineConnection, PipelineStage type imports
- OBSERVE_CONNECTIONS, REWARD_CONNECTIONS constants
- observeStages and rewardStages useMemo hooks (replaced with simpler state)
- useMemo import (no longer needed)

## Files Modified

| File | Changes |
|------|---------|
| `Level10Validations.tsx` | PipelineFlow -> Data Gate vertical visualization |
| `Level11Callbacks.tsx` | PipelineFlow -> Transform Lane vertical visualization |
| `Level15CORS.tsx` | PipelineFlow -> Browser-Server Handshake horizontal visualization |

## Design Decisions

- Used Tailwind transitions (`transition-all`) and state-driven classes instead of complex CSS keyframe animations. Simpler, more maintainable, and still provides clear visual feedback.
- Each clickable zone shows a pulsing `?` indicator when not yet inspected (matching PipelineFlow's `inspectable`/`inspected` behavior).
- CSS keyframes (bounce-back, draw-arrow) from original plan were not needed since the state-driven approach provides sufficient visual feedback.

---

# Session: 2026-03-04 Session 17

## Summary

Added sequential flow animation to L10, L11, and L15 custom visualizations. When a probe fires (observe phase) or stress test scenario fires (reward phase), data now visually flows through each zone one by one with highlighting, scaling arrows, and explanatory messages.

## Changes

### Sequential Flow Animation System

Added to all three custom visualization levels (L10 Validations, L11 Callbacks, L15 CORS):

**State and logic:**
- `flowPhase` state (-1 = idle, even numbers = zone highlights, odd numbers = edge/arrow highlights)
- `flowMessage` state (text explaining what happens at the current zone)
- `flowTimeoutsRef` for cleanup on unmount
- `runFlow(messages: string[])` helper that advances phases with ~450ms delays
- `clearFlow()` cleanup function with useEffect unmount handler

**Data maps per level:**
- `OBSERVE_FLOW`: maps probe IDs to zone message arrays for observe phase
- `REWARD_FLOW`: maps scenario IDs to zone message arrays for reward phase
- Messages indicate data source (e.g., "POST /api/v1/posts from client") and what happens at each stage

**Visual effects per zone when active:**
- `ring-2 ring-primary/60 shadow-lg shadow-primary/10` highlight ring
- Flow message text appears with `animate-in fade-in duration-300`
- Color-coded: primary for neutral, destructive for failures, success for passes

**Visual effects per arrow/edge when active:**
- `scale-150` scaling on ArrowDown icons
- Color changes to match flow context

**Phase counts:**
- L10 (3 zones): 5 phases (0-4)
- L11 (4 zones): 7 phases (0-6), uses 400ms delay for faster animation
- L15 (3 zones): 5 phases (0-4), horizontal layout with origin boundary highlighting

### Handlers Updated
- `handleProbe` now calls `runFlow(OBSERVE_FLOW[probeId])` after setting probe state
- `handleFireScenario` / `onFire` now calls `runFlow(REWARD_FLOW[scenarioId])` after firing

## Files Modified

| File | Changes |
|------|---------|
| `Level10Validations.tsx` | Added useRef, flow maps, flow state/logic, observe + reward JSX flow highlighting |
| `Level11Callbacks.tsx` | Same pattern with 4 zones (7 flow phases) |
| `Level15CORS.tsx` | Same pattern adapted for horizontal browser-server layout |

## Design Decisions

- Used setTimeout chains (not requestAnimationFrame or CSS keyframes) for sequential zone-by-zone highlighting. Simple, deterministic, easy to clean up.
- Flow animation automatically ends after the last zone + a brief hold, resetting flowPhase to -1.
- The `?` indicators on uninspected zones are hidden during active flow phases to avoid visual clutter.
- L15's horizontal layout uses the origin boundary center column for flow messages (phase 2) instead of a zone card.

---

# Session 18: Flow Animation Refinements (continued)

## Summary

Iterative refinements to the sequential flow animations for L10, L11, L15 based on user feedback. Fixed message persistence so messages never disappear once shown.

## Changes

### 1. Slowed Down Animation Timing
- L10/L15: 450ms -> 750ms -> 1200ms per phase
- L11: 400ms -> 650ms -> 1000ms per phase

### 2. Disabled Interactions During Animation
- Added `disabled` prop to `ProbeTerminal` and `StressTestPanel` components
- All three levels pass `disabled={flowPhase !== -1}` to block firing during animation

### 3. Auto-Mark Stages Inspected After Probe
- After a probe fires, `setInspectedStages` is called with all zone IDs to remove `?` indicators
- L10: controller, model, database
- L11: input, normalizes, model, callbacks
- L15: browser, preflight, cors, api

### 4. Fixed Message Blinking (persist-once-shown pattern)
- **Problem:** Messages appeared at exact `flowPhase === N`, disappeared at `N+1`, then reappeared as persisted messages, causing a blink
- **Solution:** Replaced dual state (`flowMessage` + `persistedMessages`) with single `flowMessages` array
- Messages now show when `flowPhase >= 2*i || flowPhase === -1` (monotonically inclusive)
- Active phase gets `animate-in fade-in duration-300`, post-active gets `opacity-70`
- Applied to all three levels (L10, L11, L15) across observe and reward phases

## Files Modified

| File | Changes |
|------|---------|
| `ProbeTerminal.tsx` | Added `disabled` prop |
| `StressTestPanel.tsx` | Added `disabled` prop |
| `Level10Validations.tsx` | Timing, disabled prop, inspected stages, flowMessages pattern |
| `Level11Callbacks.tsx` | Timing, disabled prop, inspected stages, flowMessages pattern |
| `Level15CORS.tsx` | Timing, disabled prop, inspected stages, flowMessages pattern |

---

# Session: 2026-03-04 Session 8

## Summary

Replaced static ArrowDown icons and dashed border edges with animated dot connectors in L10, L11, and L15.

## Changes

### 1. Created FlowConnector component (`components/levels/FlowConnector.tsx`)
- Reusable animated dot connector rendering a thin gray line with CSS-animated dots
- Props: `direction` (vertical/horizontal), `active`, `dotColor`, `dotCount`, `className`
- Dots animate using CSS `@keyframes` with staggered `animation-delay`
- When inactive, shows just the static gray line (no dots)

### 2. Added CSS keyframes to global.css
- `flow-dot-down`: animates dots from top to bottom with fade in/out
- `flow-dot-right`: animates dots from left to right with fade in/out

### 3. Updated Level 10 (Validations)
- Replaced 4 `ArrowDown` icons with `FlowConnector` (2 observe, 2 reward)
- Observe: dots activate on flowPhase, color switches to destructive when probes fire
- Reward: dot color reflects stress test result (success/destructive)
- Removed `ArrowDown` from imports

### 4. Updated Level 11 (Callbacks)
- Replaced 6 `ArrowDown` icons with `FlowConnector` (3 observe, 3 reward)
- Same flowPhase-based activation pattern
- Removed `ArrowDown` from imports

### 5. Updated Level 15 (CORS)
- Replaced 2 dashed border origin boundary divs with `FlowConnector`
- Used `className` prop with absolute positioning to match existing layout
- Dot color varies: destructive for blocked, success for allowed/curl bypass

## Files Modified

| File | Changes |
|------|---------|
| `components/levels/FlowConnector.tsx` | **NEW** reusable animated dot connector |
| `styles/global.css` | Added `flow-dot-down` and `flow-dot-right` keyframes |
| `Level10Validations.tsx` | Replaced 4 ArrowDown with FlowConnector, removed ArrowDown import |
| `Level11Callbacks.tsx` | Replaced 6 ArrowDown with FlowConnector, removed ArrowDown import |
| `Level15CORS.tsx` | Replaced 2 dashed borders with FlowConnector, added import |

## Debugging: CSS Keyframe Not Animating

**Root cause:** Two issues compounded:

1. **Lightning CSS strips `@keyframes` with `var()` references.** The initial keyframes used `calc(var(--track-length) - 8px)` which Lightning CSS silently dropped. Fixed by using percentage-based `top: 100%` / `left: 100%` instead.

2. **Tailwind v4 `@theme` changes require dev server restart.** Adding new `--animate-*` entries to `@theme inline {}` is not picked up by HMR. The dev server must be restarted for new theme tokens to take effect. The `animate-flow-dot-down` class and its referenced `flow-dot-down` keyframe were absent until restart.

**Key learnings:**
- Never use `var()` inside `@keyframes` with Tailwind v4 / Lightning CSS
- New `@theme` entries need a dev server restart (not just HMR)
- `cmux browser eval` + `getComputedStyle` is effective for debugging CSS animations
- `top: 100%` in keyframes is relative to the parent element, making it perfect for percentage-based track animation
