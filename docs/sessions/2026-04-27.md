# Session: 2026-04-27

## Summary

Continuing the staff-engineer secondary-list pass. Started Batch A
of the Feature Flags new-level project: renumber the L49-L57 cascade
to L50-L58, then add a Batch A scaffold for the new L49 in Act 6.
Real observe / build / reward content lands in Batch B.

## Batch 24: L49 Feature Flags scaffolding (Batch A)

### Renumber cascade

Inserting a new level into the middle of the curriculum requires
shifting every downstream level by +1. The cascade:

- L49 Multi-Database -> L50
- L50 State Machines -> L51
- L51 Multi-Tenancy -> L52
- L52 Observability -> L53
- L53 Modular Monolith -> L54
- L54 Domain Events -> L55
- L55 API Gateway -> L56
- L56 Sharding -> L57
- L57 The Architect (capstone) -> L58

For each level, five things rename:

- The component directory (`level-XX-slug` -> `level-(XX+1)-slug`).
- The component file inside it (`LevelXXSuffix.tsx` ->
  `Level(XX+1)Suffix.tsx`).
- The content file (`level-XX-slug.ts` -> `level-(XX+1)-slug.ts`).
- The class name in the component file (`LevelXXSuffix` ->
  `Level(XX+1)Suffix`).
- The exported variable in the content file (`levelXXSuffix` ->
  `level(XX+1)Suffix`).
- The `levelNumber:` and `id:` fields inside the content file.

Plus references to all of those in `act7/content/act.ts`,
`act8/content/act.ts`, and `src/lib/levels-registry.ts`.

Done with a single `perl -i -pe` script working highest-to-lowest
(L57 renamed first to free up the L57 namespace for the L56-to-L57
rename, etc.). The script renames file paths via `git mv` and
substitutes patterns across all `.ts` and `.tsx` files in `src/`.

One bug found and fixed: my first pass lowercased the suffix's
first letter when computing the variable form, but the actual
variable convention is `level<NN>PascalCase` (no leading-letter
lowercase). Fixed with a second targeted `perl -i -pe` pass.

The curriculum-overview comment block at the top of
`levels-registry.ts` updated to reflect the new ranges (Act 6
L41-L49 now 9 levels; Act 7 L50-L55; Act 8 L56-L58).

### L49 stub

Added a minimal scaffold so the wiring works:

- `src/features/act6-operations/content/level-49-feature-flags.ts`
  with full Level metadata: trigger, problem, learningContent
  (briefing-screen summary; full body lands in Batch B), hint,
  furtherReading.
- `src/features/act6-operations/components/level-49-feature-flags/Level49FeatureFlags.tsx`
  rendering a placeholder center panel and a "Mark Complete
  (Scaffold)" button. Real interactive phases come in Batch B.
- `src/types/game.ts`: added `feature_flags_configured` to the
  `successConditions.type` union (proper type addition, not a
  borrowed unrelated type).
- `src/features/act6-operations/content/act.ts`: imports and
  array entry.
- `src/lib/levels-registry.ts`: import and registry entry.

The scaffold deliberately does not use the per-level structure
(no separate phase components, no `data/` directory, no test
file). That split happens in Batch B alongside the real content.

### Verification

- `bunx tsc --noEmit`: clean
- `bunx biome check --write`: clean
- `bun test`: 1576 pass / 0 fail
- `bun run build`: success

### Next

Batch B: design and implement the real L49 observe / build /
reward phases, split into the per-level structure, write the
strict tests, and update the briefing-screen content body.

---

## Batch 26: pedagogy guardrail (rule + test helper)

### Why

While implementing L49 Batch B locally, the observe phase shipped
with three probes that update the left-panel discovery checklist
but do NOT visibly mutate the center-panel PipelineFlow. The
player fires a probe, reads the response in the terminal, and
sees nothing change in the diagram itself. This violates the
three-phase pedagogy rule: the observe phase exists so the player
ACTIVELY discovers the problem through visible feedback in the
visualization.

The structural tests I wrote for L49 (probe-discovery 1:1,
probe-scenario coverage, scenario uniqueness, code preview
boundaries, production-safe-default invariants) all passed. None
of them looked at the question "does anything change on screen
when the player fires a probe?".

The deeper guidance for this check already exists in the
`audit-level` skill ("Step 3: Probe-by-probe playthrough" and
the "Gate Check: Does the Observe Phase Teach the Concept?"
section) and in the `design-level` skill ("Probe
Differentiation" and "The Literal Screen Test"). Both skills are
extensive and correct. The failure was that they are
invoke-on-demand: I built L49 without consulting them,
structural tests passed, and the violation was caught only when
the user noticed nothing happens on screen.

### Layered fix

Two interventions, neither replacing the existing skills.

1. **Always-loaded rule file** at `.agents/rules/pedagogy.md`,
   path-scoped to `**/Level*.tsx`, `**/content.ts`,
   `**/data/*.ts`. Loads automatically when any level file is
   touched. Intentionally short (~70 lines): states the hard gate
   ("write down what the player literally sees in the center panel
   for every probe; if your honest answer is 'the probe terminal
   shows response lines', the level is broken"), names the
   required data structure (probe-keyed visualization state map),
   and points back to the deeper skills. The L49 failure is the
   worked example.
2. **CI test helper** at `src/lib/testing/probe-pedagogy.ts`
   exporting two functions:
   - `expectEveryProbeDrivesVisualChange<TState>({ probes,
     probeStateMap, validate })` -- fails if any probe lacks an
     entry or has an entry the validator rejects as "no visible
     delta". The validator is level-specific so it can know the
     state shape.
   - `expectEveryProbeDrivesDistinctChange<TState>({ probes,
     probeStateMap, serialize })` -- fails if two probes produce
     identical visual state. Mirrors the design-level skill's
     "Probe Differentiation" requirement.

The pair is belt-and-suspenders: rule catches at design time, the
helper catches the regression at commit time.

### What this does NOT add

- No new skill. The existing `audit-level` and `design-level`
  skills already document the playthrough requirement deeply and
  correctly. Duplicating that content in a new skill would just
  add reading-and-recalling load.
- No PreToolUse hook. Hooks fire mechanically on every tool
  invocation; using one for a judgment check ("did you do the
  playthrough?") would be noise. Rules + tests already cover the
  right moments.

### Files added

- `.agents/rules/pedagogy.md`
- `src/lib/testing/probe-pedagogy.ts`

### Verification

- `bunx tsc --noEmit`: clean
- `bunx biome check --write`: clean
- `bun test`: 1610 pass / 0 fail (no regressions; the new helper
  has no callers yet)

### Next

Use the new pedagogy rule + helper while fixing L49's observe
phase. The L49 test file will be the first caller of the helper,
which will confirm the helper catches the original bug if it is
reintroduced.
