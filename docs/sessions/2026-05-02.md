# Session Log: 2026-05-02

## Curriculum ground-truth pass: Act 1 (L1–L8) executed against a real Rails project

Per plan `wiggly-wiggling-sifakis.md`, executing every L1–L58 build step against a real `project/myapp/` Rails 8.1.3 app to (a) generate ground-truth files for the codebase viewer, (b) capture real terminal output to compare against the levels' simulated `outputLines`, and (c) prove a player who follows L1→L58 ends up with a working app. Today: Act 1 done.

Decisions locked with the user before starting: project at `<repo>/project/myapp/`, app name `myapp` (matches L2), install mise per L1 (curl installer required because brew couldn't write to `/opt/homebrew`), stay on existing PostgreSQL 14.20, run autonomously, fix levels inline as discrepancies surface, one mixed commit per act.

### Environment setup

- mise: `brew install mise` failed (`/opt/homebrew is not writable`); fell back to mise's canonical curl installer (`curl https://mise.run | sh`), which lands the binary at `~/.local/bin/mise`. Source: <https://mise.jdx.dev/getting-started.html>.
- Activated via `eval "$(/Users/sujeetkc1/.local/bin/mise activate zsh)"` in `~/.zshrc` (full-path variant required because mise wasn't on PATH yet).
- `mise install` required `mise trust` first (mise security feature for new config files); not mentioned by L1 step 4 — friction noted.
- Ruby 3.4.9 compiled from source by `mise install` (~6 min). Rails 8.1.3 + 39 dependent gems via `gem install rails`.
- PostgreSQL: stayed on existing PG 14.20. Created a `sujeetkc1` superuser role (existing cluster only had `sujeetkc` / `postgres`) so Rails defaults work.

### Real `rails new myapp --api --database=postgresql`

`rails new` does much more than L2 shows — it auto-runs `bundle install --quiet`, `bundle exec kamal init`, and the Rails 8 solid_cache/queue/cable installers, all in one shot. Generated `project/myapp/` with Dockerfile, kamal/, config/cable.yml, db/cache_schema.rb, db/queue_schema.rb, db/cable_schema.rb, etc.

### L1 → L8 execution

| Level | Command(s) executed | Status |
|-------|---------------------|--------|
| L1 | brew install mise (failed, switched to curl) → `eval` in zshrc → `.mise.toml` `[tools] ruby = "3.4.9"` → `mise trust && mise install` → `gem install rails` | Working ruby 3.4.9 + rails 8.1.3 |
| L2 | (PG 14 already running) → `rails new myapp --api --database=postgresql` → `ls -F myapp` → `bin/rails db:create` → `bin/rails server` | Server boots, /up returns 200 |
| L3 | `bin/rails generate model Product name:string description:text price:decimal` → `bin/rails db:migrate` | products table exists, schema dumped |
| L4 | `bin/rails generate model Review body:text product:references` → `bin/rails db:migrate` → edit `app/models/product.rb` to add `has_many :reviews, dependent: :destroy` → `bin/rails runner` to verify `product.reviews.create!(body: "Nice!")` | Association working, count: 1 |
| L5 | `bin/rails runner` smoke-test of `Product.create / find / update / destroy / count` | All five operations match level outputs |
| L6 | Edit `config/routes.rb` to wrap `resources :products` in `namespace :api do; namespace :v1 do; ...; end; end` → `bin/rails routes -g products` | api/v1 routes present |
| L7 | `bin/rails generate controller Api::V1::Products` → fill in the 5 RESTful actions + `private; def product_params; params.require(:product).permit(:name, :description, :price); end` → boot server → `curl localhost:3000/api/v1/products` → `200 OK` `[]` | Controller responds |
| L8 | WebFetched canonical jsonapi-serializer docs (<https://github.com/jsonapi-serializer/jsonapi-serializer>) → `bundle add jsonapi-serializer` → wrote `app/serializers/base_serializer.rb` and `product_serializer.rb` → updated controller to call `ProductSerializer.new(...).serializable_hash.to_json` → seed product → `curl /api/v1/products` returns valid JSON:API document | `{"data":[{"id":"3","type":"product","attributes":{...}}]}` |

### Level-data fixes applied (real-vs-snapshot diffs)

| Level | Fix |
|-------|-----|
| L1 | `27 gems installed` → `39 gems installed` (real `gem install rails` count) |
| L2 | `gem "rails", "~> 8.0"` → `~> 8.1.3` and added bootsnap, kamal, thruster, image_processing, tzinfo-data, debug, bundler-audit, brakeman, rubocop-rails-omakase to the snapshot Gemfile (real Rails 8.1 default) |
| L2 | `config/database.yml`: `pool` → `max_connections` (Rails 8.1 default key), added the production multi-DB block (primary/cache/queue/cable) that Rails generates by default |
| L2 | `config/application.rb`: added `require_relative "boot"`, `require "rails/all"`, `Bundler.require(*Rails.groups)`, `config.autoload_lib(...)`, `config.load_defaults 8.1` |
| L2 | `ls -F myapp` output: added `Dockerfile`, `script/`, `storage/`, `vendor/` (all in real output but missing from level) |
| L2 | Directory layout snapshot: added the same 4 entries with descriptions |
| L2 | `rails new` output: rewrote to representative form including kamal init + solid_cache/queue/cable rails installers (real `rails new` runs all these automatically) |
| L3 | `app/models/product.rb` snapshot: dropped the `# Attributes:` comments — the Rails 8.1 generator does NOT add them; real file is just `class Product < ApplicationRecord; end` |
| L3 | Migration snapshot: `ActiveRecord::Migration[8.0]` → `[8.1]`; dropped `precision: 10, scale: 2` because the generator does not add it without the `decimal{10,2}` curly-brace syntax (which the level doesn't use). Migration filename uses `<timestamp>` placeholder instead of `20240101000000`. |
| L3 | `db/schema.rb` snapshot: `[8.0]` → `[8.1]`, alphabetized column order (Rails 8 schema dump now sorts columns), added `enable_extension "pg_catalog.plpgsql"` line, dropped `price` precision/scale |
| L3 | Generator output: added `create test/fixtures/products.yml` line (real generator emits it); migration filename → `<timestamp>` placeholder |
| L3 | Migration banner: real format includes the timestamp (`== 20260502144556 CreateProducts: migrating ...`); updated to use `<timestamp>` placeholder |
| L4 | Same migration `[8.0]` → `[8.1]`, generator output got `create test/fixtures/reviews.yml` and `create test/models/review_test.rb`; migration timestamp → placeholder |
| L4 | Migration banner format: timestamp-prefixed |
| L5 | Destroy step output: dropped fictional `(destroyed)` suffix (Rails doesn't print this); now shows the inspect string |
| L8 | Gemfile snapshot in `getCodeFiles`: `rails ~> 8.0` → `~> 8.1.3`, pinned `pg ~> 1.1` and `puma >= 5.0` to match the real Gemfile baseline |

### False positives (no level change)

- L6 routes output: real includes a separate PUT row alongside PATCH (Rails generates both for `resources`). Level shows only PATCH. Acceptable simplification because PATCH is the canonical Rails 8 verb for updates and PUT is just a legacy alias. Not a fix.
- L7 curl output: real includes more security headers (x-frame-options, x-xss-protection, etc.) that the level omits. Acceptable simplification.

### What this revealed about the levels

- The L3 model + migration snapshots were the most divergent — the level shipped synthetic decorations (attribute comments in the model, `precision: 10, scale: 2` on the price column) that the real Rails 8.1 generator does NOT produce. A first-time player who ran the command and compared their file to the level's snapshot would be confused.
- L2's Gemfile, database.yml, and application.rb snapshots were minimal/aspirational rather than real. Real Rails 8.1 produces ~16 default gems, a full multi-DB production block, and several boilerplate config calls. The codebase viewer would show a much sparser file than reality.
- The L1 mise-trust step is real friction every player will hit on first install. Possible follow-up: split L1 step 4 into "trust the config" and "mise install", or have step 4's command be `mise trust && mise install`.

### Verification

- `bunx tsc --noEmit`: clean
- `bunx biome check --write` on changed files: clean (autofixed L2 formatting)
- `bun run build`: succeeds in 9.08s
- `bun test`: 1453 pass / 0 fail
- Real `project/myapp/` is a working Rails 8.1.3 API: `bin/rails server` boots, `curl /api/v1/products` returns valid JSON:API serialized response, all 8 act-1 levels' state is on disk for inspection.

### Stopping for inspection

Per the cadence, stopping here. User can `cd project/myapp && bin/rails server` and poke at the API before approving Act 2.
