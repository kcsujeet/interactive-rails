# Session: 2026-03-27

## Level 39 Webhooks & Idempotency - Full Redesign

### Changes

**Removed activate phase.** Build phase now transitions directly to reward on last step's "Next Step" button. No star rating, no "Visualize Webhook Security" interstitial.

**Replaced static gate boxes with React Flow 4-node visualization.** Old design used vertical stacked div boxes with FlowConnector (wrong props: `direction="down"`, `variant`). New design uses FlowDiagram with 4 custom React Flow nodes:
- Stripe/Attacker node (left) with dynamic icon (Globe vs ShieldAlert)
- App Server node (center-left) with label + badge
- Database node (upper-right) with mini-table rows
- Job Queue node (lower-right) with status label

Three edges with AnimatedDots, edge labels, and reversePath for response flows.

**Rewrote probes as user stories.**
- "POST webhook (forged, no signature)" -> "Attacker forges payment webhook"
- "POST webhook (duplicate event_id)" -> "Stripe retries payment event (network hiccup)"
- "POST webhook (slow handler)" -> "Monthly invoice webhook (slow handler)"
- Added `story: string[]` fields with 4-6 bullet points each

**Added multi-frame animations.** Each probe has 4-7 animation frames showing the mechanism step by step: dots flowing between nodes, labels updating, colors changing. Animation speed scaled to 2x (4 nodes + 3 edges), with duplicate-event probe at 2.5x due to having the most frames.

**Rewrote stress scenarios to replay observe probes.** Old scenarios were feature demos (generic "POST payment.succeeded (valid)"). New scenarios are paired with observe probes:
- "Attacker forges payment webhook (with verification)" - same attacker, rejected at signature
- "Stripe retries payment event (with dedup)" - same retry, caught by dedup check
- "Monthly invoice webhook (with async)" - same slow invoice, processed in <50ms
- Plus 2 reward-only scenarios (valid-subscription, bad-payload)
- Added `responseLines` and `story` fields to all scenarios

**Fixed code preview index.** Changed from `furthestStep` to `isCurrentStepCompleted ? currentStep : currentStep - 1`.

**Fixed ErrorFeedback placement.** Moved above options, not below.

**Fixed "Next Step" button.** Changed to `className="gap-2" size="sm"` matching standard.

**Fixed StressTestPanel props.** Changed from passing `stressTest` object to individual props (results, allowedCount, blockedCount, etc.) matching the actual interface.

**Fixed DiscoveryChecklist props.** Changed from custom `{found: boolean}` mapping to `discoveryGating.discoveries` with proper `status` field.

**Fixed StepProgress props.** Changed from `STEP_DEFS.map(s => s.title)` strings to `stepper.steps` with proper `Step` objects.

**Fixed OptionCard props.** Changed from nonexistent `code`/`label`/`correct`/`onSelect` props to actual API: `name`/`mono`/`disabled`/`selected`/`onClick`.

**Added LevelHeader.** Custom level components must include their own `LevelHeader` inside `CenterPanel`. Added with `onComplete`, `onReset`, and `onValidate` handlers (requires 3+ stress test fires to submit).

## LevelHeader Missing from L38 and L40

Found that L38 (External APIs) and L40 (API Versioning) were the only 2 of 56 level components missing `LevelHeader`. Fixed both:

**L38:** Added `LevelHeader` import, `handleValidate`/`handleComplete`/`handleReset` callbacks, and `<LevelHeader>` inside `<CenterPanel>`. Replaced raw `<Button>Submit</Button>` in the left panel with an auto-fire toggle (submit is now in the header).

**L40:** Added `LevelHeader` import, handler callbacks, and `<LevelHeader>` inside `<CenterPanel>`. Changed `_props` to destructure `onComplete`.

**Skill updates:** Added "Component Structure" checklist section to both `design-level/SKILL.md` and `audit-level/SKILL.md` making `LevelHeader` a non-negotiable requirement with the exact pattern to follow.

### Verification
- TypeScript: 0 errors in L38, L39, L40 component files
- Biome: clean
- Build: passes
- Tests: 1054/1054 pass (all)

## Level 44 Safe Migrations - Full Rewrite to Three-Phase Pattern

Complete rewrite of Level44SafeMigrations.tsx from the old toggle/card-based design to the standard three-phase React Flow pattern matching L41-L43.

**Old design:** Single-phase with manual toggle buttons, no React Flow visualization, no discovery gating, no stress testing. Used custom MigrationOperation state with risk levels and a strong_migrations toggle.

**New design:** Full observe -> build -> reward three-phase flow with:

**Visualization:** 3-node horizontal React Flow layout:
- Developer (left, Terminal icon) - runs migrations
- Database (center, Database icon, larger) - shows lock state with Lock/Unlock sub-panel and row count
- API Requests (right, Server icon) - shows request status (serving/blocked/failing)

Two edges: Developer<->Database and Database<->API Requests with animated dots.

**3 probes (observe):**
1. "Add column with default on 5M rows" - ACCESS EXCLUSIVE lock, 30s, all requests return 500
2. "Change column type on orders table" - Exclusive lock, full row rewrite, 500 errors
3. "Add index on large table" - SHARE lock, writes blocked 45s

**6 build steps:**
0. Terminal: bundle add strong_migrations
1. Terminal: rails generate strong_migrations:install
2. OptionCard: Fix unsafe add_column (3 options: disable lock timeout / add without default + backfill / raw SQL)
3. OptionCard: Fix unsafe change_column (3 options: SET DATA TYPE / safety_assured / new column + backfill + swap)
4. OptionCard: Fix unsafe add_index (3 options: partial index / CONCURRENTLY + disable_ddl_transaction! / CONCURRENTLY without disable_ddl_transaction!)
5. OptionCard: Configure strong_migrations (3 options: disable all / only timeout / target_version + start_after + lock_timeout)

**4 reward scenarios:** 3 paired with observe probes (safe patterns) + "Validate constraint safely" (NOT VALID + VALIDATE)

**Implementation follows L41 exactly:** types, defaults, discovery defs, probes with stories, observe/reward animation frames, build step defs, terminal step map, stress scenarios, code preview builder, custom React Flow nodes with FLASH_BORDER/FLASH_BG, custom edge with AnimatedDots, main component with all three phases.

### Verification
- TypeScript: 0 errors in Level44
- Biome: clean
- Build: passes
- Tests: 1054/1054 pass

## Audit Failure: L39 Concept Foundation Gap

L39 audit initially passed but was correctly caught by user review. The level assumes the player knows what webhooks are, but no earlier level teaches the concept. L38 teaches outbound Stripe API calls. L39 jumps to inbound Stripe callbacks without bridging the gap. The player would not understand:
- What a webhook is
- Why Stripe needs to call back
- When/how the webhook endpoint was set up
- Why the app needs it

**Root cause:** The audit-level skill's narrative reasoning questions asked "how did the player get here?" but never explicitly asked "would the player even know what this concept is?" The check existed implicitly but was easy to skip.

**Fix:** Added Question 2 ("Would the player even know what this concept is?") to both audit-level and design-level skills as a mandatory check with L39 as the case study. Also added to the design-level checklist. Saved feedback memory for future reference.

L39 component still needs a narrative redesign to introduce webhooks before exposing their vulnerabilities.

## Fix L39 Audit Failures

Fixed both audit failures with surgical text/code changes (no structural redesign):

**Fix 1: Introduce webhooks concept.** Added bridge paragraph to left panel scenario text explaining what webhooks are ("Stripe processes the charge and then notifies your app by sending an HTTP POST to your /webhooks/stripe endpoint. This inbound callback is called a webhook."). Updated content.ts trigger and problem.observation to introduce the concept before describing the incident.

**Fix 2: Service pattern compliance.** Refactored observe "before" code from single file (business logic in WebhooksController) to two files: thin controller (`HandleStripeWebhook.call(payload:)`) + naive service (`HandleStripeWebhook < ApplicationService` with `Result = Data.define`). Matches L38's `ProcessPayment` service pattern. Updated content.ts codeExample to match.

### Verification
- TypeScript: 0 new errors
- Biome: clean
- Build: passes
- Tests: 1054/1054 pass

## Design Checklist Worksheet

Created `.claude/skills/design-level/design-checklist.md`, a fill-in worksheet that must be completed when designing any level. Unlike the reference checklist in SKILL.md, this requires written output at every step. Key gates: actor table (Section 1C), actor-to-node cross-reference (Section 2B), concept foundation trace (Section 1B), code preview transition table (Section 4B). Updated SKILL.md Step 0 to reference it as the first thing to do.

## L39 Attacker Node Fix

Added a dedicated Attacker node separate from Stripe. Previously, the Stripe node doubled as the Attacker (changing icon/label per probe), which confused the player about where forged events come from.

**Changes:**
- Replaced `StripeVizState` (with `isAttacker` flag) with `SimpleNodeState` used by both Stripe and Attacker
- Added `AttackerNodeData`, `WebhookAttackerNode` component (ShieldAlert icon, always "Attacker")
- Simplified `WebhookStripeNode` (Globe icon, always "Stripe", no conditional rendering)
- Added `attackerState` + `edge0State` to visualization state, `resetViz`, `applyFrame`, cleanup
- Updated all forged-webhook and bad-payload frames (observe + reward) to use `attacker` + `edge0` instead of `stripe` + `edge1`
- Layout: Attacker at (0, 0), Stripe at (0, 100), both connect to App at (260, 40) via separate edges
- 5 nodes now match 5 actors: Attacker, Stripe, App, Database, Job Queue

## Customer Node + Complete Flow Animations

Added 6th node (Customer) and rewrote ALL animation frames to show the complete causal chain.

**Problem:** The duplicate-event and slow-processing probes started mid-flow (Stripe sends webhook) without showing what triggered it (Customer pays). The player couldn't see the causal chain: Customer -> App -> Stripe -> webhook callback -> problem.

**Changes:**
- Added `WebhookCustomerNode` component (User icon), `customerState`, `edgeCState` (Customer<->App)
- Changed edge1 from Stripe->App to App<->Stripe (bidirectional via `reverse` flag) to support both outbound payment and inbound webhook on the same edge
- Rewrote duplicate-event observe: 10 frames showing Customer pays -> App forwards to Stripe -> Stripe processes -> Stripe sends callback -> App credits -> 200 lost -> Stripe retries -> DOUBLE CREDIT
- Rewrote slow-processing observe: 8 frames showing Customer subscribes -> App -> Stripe -> invoice.paid callback -> sync processing with timeout counter
- Rewrote all matching reward frames with same complete flow, diverging at the webhook callback stage
- Forged-webhook and bad-payload probes unchanged (attacker doesn't involve Customer or Stripe)
- 6 nodes: Customer, Attacker, Stripe (left column), App (center), Database, Job Queue (right column)
- Animation speed increased to 2.5x base, 3x for duplicate-event (most frames)

Updated design-level skill with "missing actor" case study (Customer node omission).

## Option Shuffler

Created `src/lib/shuffleOptions.ts`: a seeded shuffle utility that randomizes option positions per session while ensuring the correct answer is never first. Uses a session-level random salt + step index as seed so positions are stable within a render but vary across page reloads.

Applied to L39: all 6 build steps (2 terminal + 4 OptionCard) now pass through `shuffleOptions`. Updated CLAUDE.md, design-level/SKILL.md, audit-level/SKILL.md, and design-checklist.md to require `shuffleOptions` instead of hand-positioned correct answers.

### Verification
- TypeScript: 0 errors
- Biome: clean
- Build: passes
- Tests: 1054/1054 pass

## Level 40 API Versioning - Full Redesign

Complete rewrite of Level40APIVersioning.tsx following the design checklist.

**Fixed issues (same pattern as old L39):**
- Removed activate phase (star rating + "Visualize API Versions" interstitial)
- Replaced static FlowConnector boxes with React Flow 3-node visualization (v1 Partner, v2 Partner, Rails App with internal v1/v2 sub-panels)
- Fixed StressTestPanel to use individual props (not broken `stressTest` object)
- Fixed OptionCard to use correct API (`name`/`mono`/`onClick`)
- Fixed DiscoveryChecklist/StepProgress to use hook data directly
- Added `shuffleOptions` for all terminal and OptionCard steps
- Fixed code preview index to `isCurrentStepCompleted ? currentStep : currentStep - 1`
- Added `LevelHeader` with validate/complete/reset handlers
- Removed `as const` from mutable arrays

**Visualization design:**
- 3 nodes: v1 Partner (top-left, Users icon), v2 Partner (bottom-left, Users icon), Rails App (right, Server icon with v1/v2 sub-panels in reward)
- Fork layout: two clients on left, one server on right. Visually distinct from L39's 6-node 3-column layout.
- Observe: App shows "Single Controller" with no version sub-panels
- Reward: App expands to show v1 (cents + deprecation) and v2 (money object) sub-panels

**3 probes (with paired reward scenarios):**
1. v1 partner fetches order (format changed) -> v1 gets NaN because format changed to object
2. v1 partner checks for deprecation notice -> no Deprecation/Sunset headers
3. v2 partner wants money object format -> 404, no v2 namespace exists

**5 reward scenarios:** 3 paired + "v1 and v2 coexist" (both partners served simultaneously) + "GET /api/v3 (unknown version)" returns 404

**6 build steps:** Add v2 routes, generate v2 controller, create v2 serializer, add deprecation headers, add Sunset header, freeze v1 with dedicated serializer

## L40 Trigger Fix: Solution Name in Problem Statement

L40's trigger said "Product wants breaking changes for v2" but the player has never heard of "v2." Versioning is the SOLUTION, not the PROBLEM. Fixed trigger and scenario text to describe the problem using concepts the player knows (format change breaks 200 partners) without naming the solution. Updated design-checklist.md Section 1B to check for this.

### Verification
- TypeScript: 0 errors
- Biome: clean
- Build: passes
- Tests: 1054/1054 pass

## Level 45, 46, 47 Test Files

Wrote strict test files for Levels 45, 46, and 47 (Act 6: Reliability).

**Files created/rewritten:**
- `src/features/act6-reliability/__tests__/Level45RecurringJobs.test.ts` (new, 57 tests, 232 assertions)
- `src/features/act6-reliability/__tests__/Level46DataLifecycle.test.ts` (rewritten to match current component data, 58 tests, 238 assertions)
- `src/features/act6-reliability/__tests__/Level47ErrorMonitoring.test.ts` (new, 59 tests, 256 assertions)

All data mirrored (not imported) from components. Tests cover: exact discovery/probe/step/scenario counts and strings, unique IDs, probe-to-discovery mapping completeness, build step quality (3+ options, 1 correct never first, feedback non-empty and does not leak answer), stress scenario allowed/blocked mix, cross-phase consistency, observe zones exclude reward-only zones, and no em dashes.

### Verification
- Level 45: 57/57 pass
- Level 46: 58/58 pass
- Level 47: 59/59 pass
- Full suite: 1478/1479 pass (1 pre-existing failure in Level44SafeMigrations)

## Rewrite Test Files for L38, L39, L40

Rewrote strict test files for Levels 38, 39, and 40. Previous test files had stale/incorrect mirrored data (wrong labels, wrong response line colors, wrong commands). New tests mirror exact data from the component files.

**Files rewritten:**
- `src/features/act5-production/__tests__/Level38ExternalAPIs.test.ts` (56 tests, 134 assertions)
- `src/features/act5-production/__tests__/Level39Webhooks.test.ts` (60 tests, 150 assertions)
- `src/features/act5-production/__tests__/Level40APIVersioning.test.ts` (63 tests, 146 assertions)

**Test coverage per level:**
- Discovery definitions: exact count, unique IDs, unique labels, exact ID list
- Probe definitions: exact count, exact IDs, exact response line counts, exact response text assertions
- Probe-to-discovery mapping: completeness in both directions, exact mapping values
- Build step quality: 3 options per set, 1 correct per set, correct never first, all wrong have feedback
- Answer leak prevention: feedback strings checked against correct answer's distinctive identifiers
- Stress scenarios: exact count, unique IDs/labels, exact response line counts, exact expected results, allowed/blocked mix
- Cross-phase consistency: probe-to-scenario matching, extra reward-only scenarios identified

### Verification
- All 1502 tests pass across 46 files

## Test Files for Levels 41-44

Created strict test files for Levels 41 (Middleware), 42 (Rate Limiting), 43 (Soft Deletes), and 44 (Safe Migrations).

**Files created:**
- `src/features/act6-reliability/__tests__/Level41Middleware.test.ts` (54 tests)
- `src/features/act6-reliability/__tests__/Level42RateLimiting.test.ts` (54 tests)
- `src/features/act6-reliability/__tests__/Level43SoftDeletes.test.ts` (60 tests)
- `src/features/act6-reliability/__tests__/Level44SafeMigrations.test.ts` (67 tests)

**Test coverage per level:**
- Discovery definitions: exact count, unique IDs, exact label text
- Probe definitions: exact count, unique IDs, exact responseLines count, exact IDs
- Probe-to-discovery mapping: bidirectional coverage (every probe maps to a discovery, every discovery is reachable)
- Build step quality: 3 options each, exactly one correct, wrong options have feedback > 20 chars, correct answer not first (L41), feedback does not reveal correct answer
- Stress scenarios: exact count, unique IDs, responseLines non-empty, allowed/blocked mix, exact IDs and expectedResults
- Cross-phase consistency: every probe ID has matching stress scenario

**Feedback-reveals-answer checks (per level):**
- L41: request_id_tracker.rb, config.middleware.use RequestIdTracker, Process.clock_gettime, BOT_PATTERNS
- L42: bundle add rack-attack, rate_limit to: 5 within:
- L43: bundle add discard, Discard::Model, paper_trail:install, set_paper_trail_whodunnit
- L44: bundle add strong_migrations, strong_migrations:install, algorithm: :concurrently, target_postgresql_version

### Verification
- All 4 new test files pass individually
- Full suite: 1502 tests pass across 46 files
- Biome: formatted and clean
