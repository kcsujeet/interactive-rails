# Session Log: 2026-05-03

## Codebase validator: design + build

After yesterday's Act 1 ground-truth pass shipped, evaluated three approaches for keeping level snapshots in sync with real myapp going forward:

1. **Per-level git-tag snapshots → JSON manifest → React consumer** (spike). Built end-to-end yesterday. Works, but 3.5MB manifest at scale, requires refactoring `getCodeFiles` reveal logic, and per-step granularity becomes its own problem.
2. **Continue manual ground-truth audits per act**. Brittle and high-touch.
3. **Validator script that compares hand-authored level snapshots against real myapp at git tags, reports drift**. Levels stay hand-authored and pedagogically tuned; correctness is enforced by a separate, runnable check.

User picked (3). Spike scaffolding from yesterday's experiment was reverted in commit a97dbcc to keep the slate clean.

### Design (docs/codebase-validator.md)

- **Scope (v1)**: validate only `registerLevelCode`'s output (the codebase viewer's snapshot). Per-step `getCodeFiles` content is a superset; deferred.
- **Mapping**: `act{X}-level{N}-{slug}` → `level-{N}` git tag in `project/myapp/`. Missing tag = SKIP, not failure.
- **Per-CodeFile classification**: REAL (real path) / PLACEHOLDER (`<timestamp>` / `<sha>`) / PSEUDO (filename has spaces or matches a known label like `Stack`, `Generator Command`) / GLOBAL (`~/.zshrc` etc., out of scope).
- **Comparison**: normalize whitespace, resolve placeholders to regex, every non-blank line of the level's snippet must appear (regex-matched) in the real file. Order-independent. Catches drift; tolerates legitimate simplification.
- **Out of scope (v1)**: `--fix` mode, per-step validation, CI gating. Run on demand via `bun run validate:codebase`.

### Implementation

- `scripts/validate-codebase.ts` (~270 lines, Bun script). Uses `Bun.Glob` for level discovery (cited <https://bun.com/docs/api/glob>), `Bun.$` for git shell calls (cited <https://bun.com/docs/runtime/shell>). Exports pure functions (`classifyFilename`, `tagForSlug`, `lineToRegex`, `normalize`, `compare`) so tests can import them.
- `scripts/__tests__/validate-codebase.test.ts` (~150 lines, 26 tests). Covers classification, slug-to-tag derivation, regex-meta escaping with placeholder substitution, normalization, line-containment match (incl. drift detection on single + multiple lines, whitespace tolerance, regex-meta safety).
- `src/lib/codebase-registry.ts`: added `getRegisteredLevelIds()` and `getLevelCode(id)` helpers so the validator can introspect the registry after triggering imports.
- `package.json`: new `validate:codebase` script.

### First end-to-end run found a real drift

```
$ bun run validate:codebase
...
  act1-level8-serializers            FAIL  4/5 matched
     - app/controllers/api/v1/products_controller.rb
         missing:                    .serializable_hash.to_json
         missing:     products = Product.all
         missing:     render json: ProductSerializer.new(products)
         (+1 more)
```

The level's `getCodeFiles` for the serializers controller showed only 2 actions (show + index) with the render call broken across 2 lines. Real `myapp` (per L7 + L8 execution yesterday) has 5 actions inline (index, show, create, update, destroy) plus a private `product_params`. The level's snapshot was stuck in a pedagogically-simplified shape that contradicted what the player actually ends up with.

Fix: rewrote L8's `getCodeFiles` controller snapshot to match reality (5 actions, single-line render calls, private params). After the fix:

```
Summary: 1 ok / 0 fail / 57 skip
```

L1 and L5 skip because their `registerLevelCode` returns []. L2–L7 skip because their `level-N` tags don't exist yet — those tags get created as we execute Acts 2-7 going forward.

### Verification

- `bunx tsc --noEmit`: clean
- `bunx biome check --write`: clean
- `bun run build`: succeeds in 9.38s
- `bun test`: 1479 pass / 0 fail (1453 prior + 26 new validator tests)
- `bun run validate:codebase`: 1 ok / 0 fail / 57 skip, exit 0

### L8 observe phase: serializer shown as a "missing" node (also fixed)

User flagged a third L8 bug: the observe phase rendered a `Serializer` node with sublabel "Missing!" connected to the controller. Per `.agents/rules/pedagogy.md` ("show only what currently exists"), the observe phase must only show components that exist in the "before" state. The serializer doesn't exist yet — that's the whole premise of the level. Showing it as a placeholder is misleading.

Fix:
- Removed the `serializer` stage from `observeStages`.
- Removed the `controller -> serializer` edge from `OBSERVE_CONNECTIONS`.
- Removed the `serializer` entry from `STAGE_INSPECTOR_MAP` and `STAGE_DISCOVERY_MAP`.
- Removed `serializerSublabel` from `PROBE_PIPELINE_MAP` (the `'Bypassed!'` overlay was identical across all 3 probes anyway, so no per-probe differentiation lost — `responseBadge` already carries that).
- Dropped the now-orphan `no-serializer` discovery from `DISCOVERY_DEFS` (its only unlock source was the about-to-be-removed stage click). The 3 remaining discoveries stay 1:1 with the 3 probes.

The reward phase still includes the serializer (correctly — it exists by then). The probe responses already convey "no serializer exists" through raw, unfiltered JSON dumps — that's how the absence is communicated, per the pedagogy rule.

### L8 step-reveal off-by-one (separately reported by user, fixed together)

While reviewing, the user noticed that completing the last step of L8 (`update-controller`, step index 4) showed `product_serializer.rb` in the right panel instead of the controller. Root cause: `getCodeFiles` in `Level8Serializers.tsx` had guards `>= 1` through `>= 5`, but the right panel passes a 0-based completed-step index per the CLAUDE.md convention (max 4 in this 5-step level). The controller guard `>= 5` was unreachable during gameplay; only the codebase-viewer call (`registerLevelCode` passes `STEP_DEFS.length = 5`) ever satisfied it. There was also a duplicate `Gemfile` push at `>= 2`.

Fix: dropped the duplicate Gemfile, shifted the remaining guards down by 1 so each file appears at the correct step (Gemfile after step 1 = install-gem; BaseSerializer after step 2 = base-serializer; ProductSerializer after step 3 = define-attributes; controller after step 4 = update-controller). The validator continues to pass (registry call still matches all 4 real files).

### Regression tests for the L8 bug class

Added `scripts/__tests__/level-reveal-consistency.test.ts` (16 tests) to catch this class of bug going forward:

1. **Static: stranded `getCodeFiles` guards** — for every level component file, parses `STEP_DEFS` length, detects which step convention the render-side `getCodeFiles` call uses (`stepper.currentStep` = 0-based, max `length - 1`; `stepper.furthestStep` = 1-based, max `length`), finds the largest `>= N` guard inside the file, and asserts the guard is reachable in-game.
2. **Runtime: duplicate filenames in `registerLevelCode` output** — for every registered level, asserts no `CodeFile.filename` repeats.
3. **Static: observe phase shows only what currently exists** — for every level, scans for textual fingerprints of the "render a placeholder for a thing the build phase will introduce" anti-pattern: inspector titles ending `(Missing)` / `(Missing!)`, stage labels ending `(Missing)` / `(Missing!)`, bare `sublabel: 'Missing'` / `'Missing!'`. Distinguishes legitimate "No X" sublabels (which describe an absent property of an existing node) from the bug.

All three tests use a **baseline** (`KNOWN_STRANDED_GUARDS`, `KNOWN_DUPLICATE_FILENAMES`, `KNOWN_OBSERVE_PHASE_PLACEHOLDERS`) of levels that already exhibit the bug, so the test catches *new* regressions while documenting existing technical debt to fix later. Each test also fails if a baseline entry has been fixed but not removed (no stale entries).

The first run surfaced:
- 12 levels with stranded guards (L4, L6, L7, L11, L13, L14, L16, L17, L18, L19, L20, L41).
- 1 level with a duplicate filename (L29).
- 10 levels with observe-phase placeholder nodes (L7, L9, L11, L13, L20, L28, L29, L35, L41, L47).

All baselined as TODO. The bugs are real and should each be fixed in a focused pass; the tests now make sure the count only goes down.

### Next

When we resume Act 2 execution against `project/myapp/`, each level boundary creates a `level-N` tag (cheap — `git -C project/myapp tag level-N`). After Act 2, running `bun run validate:codebase` should turn 6 of the 57 skips into oks (or surface drift to fix). By the end of Act 7, all 58 levels are validated. Separately, the 13 baselined L8-class bugs should get fixed in a focused pass (one commit per act probably). Each fix removes a baseline entry; the test enforces no regression.

## Act 2 start: L9 Authentication ground-truth pass

Started Act 2 execution against `project/myapp/`. L9 surfaced a substantial pedagogical fabrication that needed a real-world fix.

### What the level claimed vs what Rails 8 actually does

L9 taught a token-based API auth pattern: `Session.token` column, `before_create { self.token = SecureRandom... }`, `Authorization: Bearer <token>` header parsing, JSON `{ token }` response. The level claimed `bin/rails generate authentication` produced this. **It does not.**

Source: <https://raw.githubusercontent.com/rails/rails/main/railties/lib/rails/generators/rails/authentication/authentication_generator.rb>. The Rails 8 generator produces:
- `User`, `Session`, `Current` models — Session with NO token column (just `belongs_to :user` + ip/user_agent strings).
- `app/controllers/concerns/authentication.rb` that uses `cookies.signed[:session_id]` for session lookup.
- `SessionsController#create` that calls `start_new_session_for(user)` (sets a signed cookie) and `redirect_to`.
- `helper_method :authenticated?` line — fails on `ActionController::API` (not present in API mode).

I confirmed the breakage at runtime: with the unmodified generator output, `POST /session` from our API-only myapp hits `NoMethodError: undefined method 'helper_method' for class ApplicationController`. **The vanilla Rails 8 auth generator is broken in API-only mode.**

### Decision

User confirmed: rewrite L9 to teach a real Rails 8 pattern, and ensure the generated app actually works (`rails s` should serve the API end to end). We picked: keep the generator as the starting point, then customize for API + bearer-token auth. This matches the level's existing pedagogical intent (token-based) while being honest about the customization required.

### myapp customization (committed at `level-9` git tag in project/myapp)

- `bin/rails generate migration AddTokenToSessions token:string:uniq` + `bin/rails db:migrate`. Real `AddTokenToSessions` migration on disk, real schema updated.
- `app/models/session.rb`: added `before_create do; self.token = SecureRandom.urlsafe_base64(32); end`.
- `app/controllers/concerns/authentication.rb`: replaced cookie lookup with `request.headers["Authorization"]&.start_with?("Bearer ")` + `Session.find_by(token: ...)`. Dropped `helper_method` (incompatible with API). Added explicit `head :unauthorized` when no session.
- `app/controllers/sessions_controller.rb`: `User.authenticate_by(params.permit(:email_address, :password))` + `user.sessions.create!(ip_address:, user_agent:)` + `render json: { token: session.token }`. Dropped redirects (incompatible with API).
- `config/routes.rb`: `resource :session, only: %i[create destroy]` (drop the auto-generated `new`/`show` routes that target views; drop `resources :passwords` since we haven't adapted that flow).

### End-to-end verification

```
$ POST /session  body={"email_address":"joe@example.com","password":"secret123"}
{"token":"zLZXLu8KZiQTRbYUBqDifRJN_ZvGoELXau-6lXAqhv8"}

$ GET /api/v1/products  (no Authorization)         -> 401 Unauthorized
$ GET /api/v1/products  Bearer <token>             -> 200 OK
```

The API now works as a real bearer-token-protected JSON service.

### L9 component fixes (this commit)

Aligned the level's snapshots with what real myapp actually contains at the `level-9` tag, so the codebase viewer stops showing fabricated APIs and the validator passes:

- `getCodeFiles` reward state now returns the real `user.rb` (with `normalizes :email_address`), real `session.rb` (with the customized before_create), real `authentication.rb` concern (bearer-header version with `class_methods do; def allow_unauthenticated_access; end`), real `sessions_controller.rb` (JSON token render).
- Fixed the off-by-one: the Authentication concern guard was `>= 5` (stranded — render path passes 0-based currentStep with max 4); shifted to `>= 4`. The Session model + SessionsController moved from `>= 4` to `>= 3`. Last-step reveal now actually appears in-game.
- Step 3 createSessionOutput updated: dropped the misleading `Authorization: Bearer abc123...def789` line (which made it look like that's what `user.sessions.create!` returns); now shows the real inspect output and explains what `before_create` does.
- Step 3 wrong-cookie option's feedback corrected: the original said "Cookies are for browser apps" (factually wrong for Rails 8 native auth, which IS cookie-based for non-API apps). Now: "API-only Rails apps do not include cookie middleware by default" — the real reason cookies aren't viable for our app.
- Step 0 generateAuthOutput: representative real generator output (kamal init, mailer files, gsub on Gemfile/application_controller, timestamp placeholders).
- Step 1 runMigrationsOutput: timestamp placeholders + the `add_index(:users, :email_address, {unique: true})` line that the real migration emits.

### What's still NOT done in L9 (follow-up)

The 5-step pedagogy still has a gap: a player following L9's OptionCards alone does not actually edit the concern, controller, or Session model. The right panel SHOWS the customized files, but the level doesn't walk the player through producing them. To honor the user's "rails s should work at the end" goal, the level needs new build steps that walk through the customization (add token migration, edit Session model, edit Authentication concern, edit SessionsController). That's a step-structure rewrite (5 → 6 or 7 steps) — substantial enough to be its own commit.

### Verification

- `bunx tsc --noEmit`: clean
- `bunx biome check --write`: clean
- `bun run build`: succeeds in 9.46s
- `bun test`: 1495 pass / 0 fail
- `bun run validate:codebase`: **2 ok / 0 fail / 56 skip** (L8 and L9 both validated against real myapp at their respective tags)

## Edge animations: dormant by default, burst on probe/scenario fire

User flagged: edge animations shouldn't run automatically; they should only animate when a probe (observe) or scenario (reward) fires. This is the existing pedagogy rule from `.agents/rules/pedagogy.md` ("the dormant-edges default — undefined is a trap"). The fix surface: pass `activeConnections={[]}` to `<PipelineFlow>` when nothing has fired, and `activeConnections={[edge-ids]}` (with an `animationTick` that bumps on each fire) for a single-pass burst.

### Regression test (4th in the consistency suite)

Added `static: PipelineFlow edges do not animate by default` test (7 tests total in this group) to `scripts/__tests__/level-reveal-consistency.test.ts`:
- Helper `hasAutoAnimatingPipelineFlow(src)` regex-scans every `<PipelineFlow ...>` opening tag in a level file and returns true if ANY tag is missing an `activeConnections=` prop.
- Test scans every `.tsx` under `src/features/act*/components/level-*-*/**/*.tsx` (expanded glob picks up split-architecture levels like L49/L50 with separate ObservePhase / RewardPhase / BuildPhase files).
- `KNOWN_AUTO_ANIMATING_EDGES` baseline holds 13 existing offenders (L4, L6, L7, L11, L13, L14, L20, L29, L35, L36, plus all three L49 phase files).
- Stale-baseline check fails if anyone fixes an entry without removing it from the set.

Inventory: 14 levels currently have continuously-animating edges. This commit fixes 2 (L8 + L9, the two we recently validated against real myapp); the remaining 12 stay baselined as TODO for a future focused pass.

### L8 + L9 fix pattern

For both levels:
1. Defined a `…_ACTIVE_EDGES` constant listing the edge IDs (`from-to` pairs derived from each `PipelineConnection`) that animate during the request lifecycle the level's probes / scenarios simulate.
2. Added `const [animationTick, setAnimationTick] = useState(0)` to the component. Bumped on every `handleProbe` and `handleFireScenario` call — `PipelineFlow.animationTick` triggers the SVG `beginElement()` restart so the same probe re-firing re-runs the burst (per pedagogy's "Re-firing the same probe must restart the animation" rule, citing <https://developer.mozilla.org/en-US/docs/Web/API/SVGAnimationElement/beginElement>).
3. Wired both `<PipelineFlow>` renders (observe + reward) to pass `activeConnections={ lastFired ? [...edges] : [] }` and `animationTick={animationTick}`. Default = empty array = dormant; on fire = burst on the listed edges.

Behavior: in observe phase before any probe fires, edges are still. Click a probe → request lifecycle edges burst once. Re-click same probe → burst again. Same for stress scenarios in reward.

### Verification (this commit)

- `bunx tsc --noEmit`: clean
- `bunx biome check --write`: clean
- `bun run build`: succeeds in 9.14s
- `bun test`: **1502 pass / 0 fail** (1495 prior + 7 new edge-animation tests)
- `bun run validate:codebase`: **2 ok / 0 fail / 56 skip** (still clean against real myapp)

## Terminal auto-scroll regression

User flagged: auto scroll not working in L9's probe terminal. Root cause: `useEffect(() => { scrollRef.current.scrollTop = ...; }, [])` — the empty dependency array means the effect only runs on mount. New probe responses, animated output lines, etc. don't trigger the scroll. ProbeTerminal AND SimulatedTerminal both shipped with this bug. StressTestPanel was correct (deps `[resultCount]`).

### Fix

Both terminals now have `useEffect(() => { /* scroll */ }, [history, visibleLines])`. The deps aren't read inside the effect — they're scroll triggers. Biome's `useExhaustiveDependencies` rule wants to remove them, so each effect carries an explicit ignore comment with the intent. Without the trigger deps the effect goes back to mount-only, which is the bug.

### Regression test (`src/components/levels/__tests__/terminal-auto-scroll.test.ts`)

9 tests. Helper `hasEmptyDepsScrollEffect(src)` walks each `}, []);` (close of an empty-deps useEffect callback) in the source and confirms (a) the preceding ~600 chars contain a `useEffect(() => {` opener and (b) the body contains a scroll token (`scrollTo`, `scrollTop`, `scrollIntoView`). If both, flag.

The static-scan test covers `ProbeTerminal.tsx`, `SimulatedTerminal.tsx`, `StressTestPanel.tsx`. Add new terminal components to the `TERMINAL_COMPONENTS` list when they're introduced.

Per project rule, no DOM rendering — pure source-level analysis. Unit tests on the helper cover: scrollTop / scrollTo / scrollIntoView all flagged with empty deps; non-empty deps OK; non-scroll empty-deps useEffects ignored; mixed files (one offender among many) detected; multiple proper scroll effects pass.

### Verification (this commit)

- `bunx tsc --noEmit`: clean
- `bunx biome check`: clean (with intentional ignore comments)
- `bun run build`: succeeds in 8.70s
- `bun test`: **1511 pass / 0 fail** (1502 prior + 9 new auto-scroll tests)

## L9 protection chain visibility

User completed L9's build phase, looked at the right panel, and asked: "I do not know how the authentication is working in the products controller at all... I cannot see how it's all used in the generated Rails code section." Fair complaint — my prior L9 commit showed only the auth-scaffolding files (User, Session, Authentication concern, SessionsController). It did NOT show:

- `app/controllers/application_controller.rb` — the `include Authentication` line that wires the `before_action :require_authentication` from the concern onto every controller via `ApplicationController` inheritance.
- `app/controllers/api/v1/products_controller.rb` — the controller that's now protected purely through inheritance, with no auth code in its own body.

Without these two files, the player has no way to see how `Authentication` reaches `ProductsController`. The reward-phase stress tests fire requests against `/api/v1/products` and the player just has to take it on faith that they're protected.

### Fix

Added both files to L9's `getCodeFiles` reward state. They reveal at `furthestStep >= 4` (after the protect-endpoint step). Validator confirms 6/6 files match real myapp at level-9 tag.

Also rewrote the protect-endpoint option's description to make the inheritance chain explicit: "Default-block: the Authentication concern auto-applies this before_action via `included do`. ApplicationController includes the concern, so every controller inherits the bearer-token check automatically."

The pedagogical comment ("# No auth code in this file. Protection inherited from...") was originally part of the products controller snapshot — but the validator (correctly) flagged it as drift from real myapp. The explanation now lives in the level's narrative text instead, which is the right place for it: per the design-level rule, file snapshots stay close to real, narrative carries the teaching.

### Verification

- `bunx tsc --noEmit`: clean
- `bunx biome check`: clean
- `bun test`: 1511 pass / 0 fail
- `bun run validate:codebase`: 2 ok / 0 fail / 56 skip — L9 now matches **6/6** files at level-9 tag

### Regression test (5th in the consistency suite)

Added `static + runtime: stress scenarios show their target controller` to `level-reveal-consistency.test.ts` (6 new tests). Helpers:
- `extractStressPaths(src)` regex-pulls `path:` values from a level's `STRESS_SCENARIOS` array.
- `resourceFromPath(path)` strips dynamic segments (`:id`, numbers) and returns the last meaningful segment.
- `snapshotCoversResource(filenames, resource)` checks if any filename matches `{resource}_controller.rb` (with naive plural / singular variants).

Test scans every level, extracts paths, derives resources, and asserts the level's runtime `getLevelCode(slug)` output includes a controller for each. Baselined 40 existing offenders — many are false positives (L43 Safe Migrations uses `path:` for migration DSL strings, L44 Recurring Jobs for job names, L37 Real-Time for channel names) plus genuine gaps (L41 CORS, L46 Error Monitoring) where the level's pedagogy could benefit from showing the target controller. Each baseline entry is a TODO: either fix or audit-and-remove.

The L9 `me` resource still appears in offenders because L9 has a stress scenario referencing `/api/v1/me` — a fictional endpoint that doesn't exist in real myapp. That's a separate bug (fictional endpoints in stress scenarios) and is part of why L9 stays in the baseline for now.

### Verification (this commit)

- `bunx tsc --noEmit`: clean
- `bunx biome check`: clean
- `bun run build`: succeeds in 9.20s
- `bun test`: **1517 pass / 0 fail** (1511 prior + 6 new stress-target tests)
- `bun run validate:codebase`: 2 ok / 0 fail / 56 skip

## L10 Encryption ground-truth pass

Continuing Act 2 execution. Verified canonical Active Record encryption API against <https://guides.rubyonrails.org/active_record_encryption.html>: Rails 8 uses `encrypts :attr` (not `attr_encrypted`); options `deterministic: true`, `downcase: true`, `ignore_case: true` (the last requires an `original_<col>` column); `bin/rails db:encryption:init` generates exactly three keys (primary_key, deterministic_key, key_derivation_salt) into the `active_record_encryption:` block of credentials.

### myapp customization (committed at `level-10` git tag)

- `bin/rails db:encryption:init` to generate keys.
- Wrote keys into `config/credentials.yml.enc` programmatically via `Rails.application.encrypted(...).write(...)` (the standard `bin/rails credentials:edit` opens an interactive editor that doesn't fit a script context).
- `bin/rails generate migration AddPiiToUsers phone:string address:string` + migrate. Real `phone` and `address` columns now on the users table.
- Added to `app/models/user.rb`:
  ```ruby
  encrypts :email_address, deterministic: true, downcase: true
  encrypts :phone, deterministic: true
  encrypts :address
  ```

### End-to-end verification

`User.create!(email_address:, password:, phone:, address:)` succeeds. Raw SQL inspection confirms the columns store ActiveRecord encryption JSON envelopes (`{"p":"...","h":{"iv":"...","at":"..."}}`), not plaintext. `User.find_by(email_address: "joe@example.com")` round-trips through the deterministic key and matches the stored ciphertext, returning the user. Address lookups by ciphertext correctly fail (non-deterministic).

### L10 component fixes (this commit)

The level's reward `getCodeFiles` was carrying a lot of premature pedagogy and a fabricated attribute name:

- `encrypts :email` → `encrypts :email_address` (the auth generator uses `email_address`, not `email` — caught by validator drift against real myapp).
- Dropped the `has_one_attached :avatar do ... end` block (Active Storage is L34, not L10).
- Dropped the `validates :email, uniqueness:` and `validates :email, format:` lines (premature, and bound to the wrong attribute).
- Dropped the `app/services/find_user.rb` snapshot that wraps the lookup in a service object using `ApplicationService` + `FindUserContract` + `Result = Data.define(...)`. Service objects are L16, validation contracts are L18, and `Result = Data.define` is also L16+. None of those exist yet at L10. The lookup-by-encrypted-attribute pedagogy now lives in a "Rails console" pseudo file showing the find_by call directly.
- Renamed `config/credentials.yml.enc` to a pseudo filename ("Credentials Entry (decrypted view)") because the real on-disk file is encrypted gibberish — what the player needs to see is the decrypted YAML they would type into `bin/rails credentials:edit`. The pseudo filename flags it as a pedagogical view, not a real path the validator can compare against.
- Added the missing `has_many :sessions, dependent: :destroy` and `normalizes :email_address` lines from the auth generator's User model.

Validator now: **3 ok / 0 fail / 55 skip** (L8 + L9 + L10 all match real myapp at their respective git tags).

### What's still NOT done in L10 (follow-up)

The level's per-step intermediate `getCodeFiles` returns (during the build phase) still contain the premature `has_one_attached :avatar` block and `email`/validates noise. Cleaning those up to match the simplified reward state requires a substantial rewrite of the per-step reveal logic. Deferred. Same shape as L9: snapshot final state matches reality; intermediate per-step states still need work.

### Verification (this commit)

- `bunx tsc --noEmit`: clean
- `bunx biome check --write`: clean
- `bun run build`: succeeds in 9.04s
- `bun test`: 1517 pass / 0 fail
- `bun run validate:codebase`: **3 ok / 0 fail / 55 skip**

### L10 trigger narrative fix

User flagged: the level's trigger said "GDPR audit flagged" but at L10 we haven't deployed (Kamal is L49), have no real users (auth was just added in L9), and no audit could have happened. Re-framed to a pre-launch security review: every PII column on `users` (email_address, phone, address) is plaintext, encrypt before the first signup. The trigger now matches the curriculum's narrative state. Also aligned column references to `email_address` (real myapp) instead of `email`.

Other places in the L10 content still use `email` instead of `email_address` in code snippets within the learningContent (multiple lines). Those are pedagogical examples that need a broader content sweep — deferred.

## TDD enforcement hook

User requested an always-on TDD reminder. Added `.claude/hooks/enforce-tdd.sh` mirroring the existing `research-before-guessing.sh` pattern: a `UserPromptSubmit` hook that emits a JSON `additionalContext` payload prepending a non-negotiable TDD rule to every turn.

The injected text: "practice TDD. Before writing or modifying production code (functions, classes, methods, services, validators, helpers, scripts), write or update the test FIRST. Run the test, see it fail, then implement. ... Exceptions: docs, config files (.json, .yml, .toml), pure data/content definitions, snapshot updates that match real source, level component composition (rendering JSX without logic). For everything else, test first."

Reasoning over hard PreToolUse blocking: the codebase already has `.agents/rules/testing.md` ("write tests WHILE building, not after"), the validator + reveal-consistency tests catch level-shape regressions, and PreToolUse blocking on every Write/Edit would fight legitimate edits to non-logic files (level data, content, snapshot updates, JSX composition). The soft injection keeps the rule hot in context without false positives. If hard blocking is wanted later, a second hook with a `PreToolUse` matcher on `Write|Edit` and a path filter (e.g. `src/lib/`, `scripts/`) can layer on top.

Wired in `.claude/settings.json` under `hooks.UserPromptSubmit` as a second array entry alongside the doc-rule reminder. Path resolution via `$CLAUDE_PROJECT_DIR` per <https://code.claude.com/docs/en/hooks>. Verified: bash syntax-clean, jq-validates the emitted JSON, settings.json parses.

Note: the hook script + settings.json change technically landed in the previous commit (35e3225) alongside the L10 work; this entry exists so the session log records what the hook is and why.
