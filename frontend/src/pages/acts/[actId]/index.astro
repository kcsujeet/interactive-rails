---
import ActDetailApp from '@/components/pages/ActDetailApp';
import { getAct } from '@/features/acts-registry';
import GameLayout from '@/layouts/GameLayout.astro';

const { actId } = Astro.params;
const actIdNum = parseInt(actId || '1', 10);
const act = getAct(actIdNum);
const title = act ? `Act ${act.id}: ${act.name}` : 'Act';
---

<GameLayout title={title}>
  <div class="p-6">
    <ActDetailApp client:load actId={actIdNum} />
  </div>
</GameLayout>
